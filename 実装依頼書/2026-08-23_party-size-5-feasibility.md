# パーティ 5 人以上の可否調査

**ステータス**: **完了** (2026-08-23) — dev-loop 4 項目で完走 (STEP1 `a1558ba` / STEP2 `83ac18f` / STEP3 `01eac91` / STEP4 = 本コミット)。結論とすべての実測値は末尾の「[実装結果](#実装結果)」節
**種別**: **調査チケット**。本番の挙動は変えない。成果物は「測った数値」と「サイズごとの可否判定」。

---

## 目的

`recruitCountOf()` の clamp 上限 3 (= パーティ 4 人) を **将来外せるかどうか** を、
engine を実測して判定する。**外すこと自体は目的ではない。**

判定は 1 つの ○× ではなく **サイズごと** に出す:

- **5 人** (主人公 1 + NPC 4) は行けるか
- **6 人** (主人公 1 + NPC 5) は行けるか
- 行けないなら、**何を直せば行けるか** を箇条書きで残す

⭐ **「上限 3 のままが正しい」も正式な結論。** この調査は上限を上げるための地ならしではない。
⭐ 出発点は `tavern.html:3270` の「5人以上を試したくなったら **別の調査チケット** を切ること」という
コード側からの名指しの指示。本チケットがその調査チケット。

---

## 背景・現状

- 人数を決めているのは `recruitCountOf()` ただ 1 本 (`tavern.html:3273`)。
  `const MIN = 1, MAX = 3` (`tavern.html:3274`) → `Math.max(MIN, Math.min(MAX, n))` (`tavern.html:3284`)。
- 実際に formation を作るのは `regeneratePartyMembers()` (`tavern.html:4595`)。
  `let partySize = PARTY_SIZE` → 募集 ON なら `1 + recruitCountOf(prepScenario)` (`tavern.html:4602-4603`)
  → `orderFormation(buildParty(heroKey, partySize))` (`tavern.html:4604`)。
- 出来た formation は `sessionStorage["dragonfighters.partyMembers"]` で index へ渡る
  (読み口 = `index.html:31537`)。**index は人数を自分で決めていない**。
- ★4 の `dragon-lair` でも NPC は 3 人で頭打ち = 最難関でもパーティ 4 人のまま、という設計上の宿題が残っている。

### ⚠ ただし「4 人前提」の根拠として書かれている 4 点は、大半が現在のコードでは成立していない

`tavern.html:3266-3270` は上限を外すなという警告と、その理由 4 点を列挙している。
**起草時 (2026-08-23) に本番の関数で 1 つずつ測ったところ、4 点のうち 3 点は既に事実と違っていた。**

| # | コメントの主張 | 実測した実物 | 判定 |
|---|---|---|---|
| 1 | index.html の**味方スポーンタイル割り当て**が 4 人前提 | `resolveAllyInitTile()` (`index.html:31518`) は固定 4 枠の表ではない。**職業ごとの希望オフセット 1 個** (`ALLY_INIT_OFFSETS` `index.html:31503-31508`) + **代替候補 24 個** (`ALLY_FALLBACK_OFFSETS` `index.html:31510-31514`) + 使用済みタイルの Set (`:31515`)。`isTileWall` を見て歩ける床だけを取り、全部塞がった時だけ頭に重ねる (`:31531`)<br>⚠⚠ **【2026-08-23 実装時に訂正】配置点はこれ 1 本ではない。** `resolveAllyInitTile()` は**起動時 1 回**しか通らない。実プレイのほぼ全部を担っているのは **`placeNodeParty()` (`index.html:33230` 付近)** で、`enterNode` から**ノード遷移のたび**に走り、候補は RING **16 個**。⭐ STEP2 で**両方**を掃いて違反 0 件を確認したので、判定そのものは変わらない | ⛔ **成立しない**。原理上 **24 体まで** 別タイルに置ける(`placeNodeParty` 側は RING 16 個。実測は下記「実装結果」) |
| 2 | `orderFormation` の「**頭 = 最も狙われる位置**」の前提 | `orderFormation` (`index.html:12035` / `tavern.html:3314`) は zone ランクの安定ソートだけで人数に依存しない。実際の被狙われやすさは **zone 係数** `ZONE_PULL[c.zone]` (`index.html:24662` 前衛バイアス) であって配列の添字ではない | ⛔ **成立しない**。人数非依存 |
| 3 | **準備画面のキャラタブ・装備 UI の枠**が 4 人前提 | `renderCharTabs()` (`tavern.html:4784`) が回すのは `PARTY_SLOTS` = **全 6 職固定**(コメント自身が「職業別プリセット board: 全6職のタブを出す」と書いている)。パーティ構成員ではない。CSS も `.partyTabs { repeat(auto-fit, minmax(90px,1fr)) }` (`tavern.html:621-626`) で自動折返し。かつて構成員ごとに出していた `.partyComp` / `.partyMemberToggle` (`tavern.html:629-693`) は **CSS だけが残った死んだ枠**(HTML にも JS にも生成点が無い。唯一の参照は効果音の `closest()` `tavern.html:5943`) | ⛔ **成立しない**。人数を増やしても準備画面は 1 ピクセルも変わらない |
| 4 | **カメラ・視界・描画コスト** | ✅ **ここだけ本物**。詳細は下の「本命のリスク」 | ⭕ **要調査** |

⭐⭐⭐ **この表がこのチケットの一番の成果物である。**
「engine が一度も通していない」という言い伝えのうち実際に危ないのは 1 点だけ、と分かったので、
調査の範囲は **カメラに絞ってよい**。残る 3 点は「変わらないことを 1 回測って終わり」でよい。

⚠ 逆向きの証拠も 1 つある: `index.html:11962` の `PARTY_SIZE` には
`// 主人公1 + NPC3 (コードで変更可、推奨 2〜6)` と engine 側の作者が書いている。
`tavern.html` の「一度も通していない」と **2 つのコメントが食い違っている**。
どちらも実測ではないので、**どちらも根拠に使わない**。

### 本命のリスク = カメラの bbox クランプ

- `computeCameraTarget()` (`index.html:6273`) は隊列 (先頭 + 生存仲間、戦闘中は交戦敵も) の
  bbox にカメラをクランプする (`index.html:6297-6338`)。
- **bbox が画面より大きいとクランプ区間が空になり、「bbox 中央寄せ」に落ちる**
  (`loCx <= hiCx` が偽の枝、`index.html:6333-6334`)。
- ⚠⚠ これは机上の心配ではなく **過去に一度踏んでいる**。`index.html:31494-31497` に
  「隊列 bbox が 21 タイル幅 → 画面 (13 タイル) に収まらず … カメラが恒久未探索の黒い岩盤へ振られる」
  と、味方の初期配置を絶対座標から相対オフセットへ作り替えた理由として記録されている。
- ⭐ **スマホ (compact) は 4 人の時点で既に区間が空**。`index.html:6339-6347` のコメントが
  「味方 4 + 交戦敵で常にそれより大きいので『区間が空 → bbox 中央寄せ』に落ち」と明記し、
  実測値 **主人公が画面外のフレーム率 25.9%(廃坑) / 16.5%(竜の巣) / desktop 1280 は 0%** を残している。
  救済の `[compact-hero-anchor]` 節 (`index.html:6354-6368`) は
  **bbox を「主人公と行動キャラの 2 点」に絞る人数非依存のハードクランプ**なので、人数が増えても効き方は変わらない。
- ⛔ **したがって危ないのは desktop**。desktop は今 0% = 区間がまだ成立している。
  5 人目が bbox を広げて区間が空へ落ちるなら、**それが上限を上げられない唯一の理由**になる。
- 併せて見るべき近傍: `MAX_LAG = 480` (`index.html:16840`。これを超えて離れた仲間がいると `heroAI` が待つ) と
  置き去り仲間のワープ救済 (`index.html:16934`。`r<=3` で最大 ≈407px < 480 `:16951`)。
  人数が増えると隊列が伸びる = 待ちとワープの発動率が上がる可能性がある。

### ⚠⚠⚠ 測定道具は「在ることになっているが、無い」

- `index.html:6352` は「既存のカメラ回帰 (driver_camera)」と書いているが、
  **`tools/` に `driver_camera*.js` は 1 本も無い**(実測: `ls tools/ | grep -i camera` が空)。
- 検証シームとして用意されている `window.__camAnchorOff` (`index.html:6353`) を
  **使っている道具も 0 件**(実測: `grep -rn "__camAnchorOff" tools/*.js` が空)。
- フレーム時間を測るシーム (`__perf` / `frameMs` 等) も index.html には**無い**
  (`frameMs` のヒットはスプライトのアニメ再生尺であって計測ではない)。

→ **カメラとフレームの測定装置は、このチケットで新規に作る**。「既存の回帰があるはず」で流さないこと。

### ⭐ 測定台は #8 の物を使い回す (作り直さない)

`tools/sweep_recruit_balance.js` が既に **「酒場を本番どおり通して `departToScenario()` へ遷移させる」**
測定台を持っている:

- `tavern.html?autoplay=N` を開く (`tools/sweep_recruit_balance.js:210`)
- `prepScenario` を差して `regeneratePartyMembers()` → `departToScenario()` (`:163`)
- index 側で二重起動しないための guard (`:198`)
- `--negative` を**道具に内蔵**し、赤くならなければ exit 1 (`:81` / `:99` / `:468-474`)

⛔⛔ **index.html を直接開く測定台を作らないこと。** `isRecruitOn()` / `recruitCountOf()` は
`tavern.html` にしか無いので、index 直起動の腕は**人数の指定が一切効かず全部 4 人になる**
(#8 が実際にこれで 1 回転してから気づいた)。

⚠⚠⚠ **【2026-08-23 実装時に追記】`?autoplay` はカメラの測定台に使えない。**
`focusCameraOn()` は `__autoplay` のとき**丸ごと早期 return する**(`index.html:6657` / 呼び口 `:28507`)。
つまり `?autoplay` を付けた腕はカメラの振る舞いが本番と別物になり、受入条件 7. / 8. が測れない。
→ 代わりに **`RUN.auto`**(本番に実在する `?graph=auto` と同じ枝・`index.html:32754`)で自走させる。
⚠ 併せて: **`currentPhase` は戦闘の目印にならない**。竜の巣では `encounterActive === true` のまま
`currentPhase === "explore"` が続く。戦闘の判定は `encounterActive` を主に置くこと。

---

## 変更範囲

### 触る

| ファイル | 何を |
|---|---|
| `tavern.html` | `regeneratePartyMembers()` (`:4595`) に **調査シーム 1 行**。既定は恒等 |
| `tools/probe_party_size.js` | **新規**。測定装置。`sweep_recruit_balance.js` の腕起動部を流用 |
| 本依頼書 | 末尾に「実装結果」節(= 調査報告そのもの) |

### 触らない

| ファイル | 理由 |
|---|---|
| `recruitCountOf()` の `MAX = 3` (`tavern.html:3274`) | ⛔ **結論が出るまで 1 ビットも触らない**。本チケットは測るだけ |
| `index.html:11962` の `PARTY_SIZE = 4` | ⛔ index 直起動を基準にしている既存ドライバ 20 本以上のベースラインが黙って動く |
| `computeCameraTarget()` (`index.html:6273`) | 測るだけ。直すなら別チケット |
| `resolveAllyInitTile` / `orderFormation` / `renderCharTabs` | 実測で人数非依存と分かったので、**触る理由が無い** |
| `.partyComp` / `.partyMemberToggle` の死んだ CSS | 掃除は別件(このチケットで混ぜると調査の差分が読めなくなる) |

---

## STEP 構成 (dev-loop 4 項目)

### STEP1 — 調査シームと測定装置の骨組み

1. `tavern.html` に `devPartySizeOverride(n)` を足し、`regeneratePartyMembers()` の
   `partySize` 決定の**直後**に 1 行だけ挟む。

   ```
   let partySize = PARTY_SIZE;
   if (isRecruitOn() && prepScenario) partySize = 1 + recruitCountOf(prepScenario);
   partySize = devPartySizeOverride(partySize);   // ← 調査シーム。?party 無しでは恒等
   ```

2. `devPartySizeOverride` の作法:
   - `?party=N` を読む。`N` が 2〜8 の整数**でなければ、引数をそのまま返す**。
   - ⚠⚠ **silent fail-open を作らないこと**。読めなかった / 範囲外だったときは
     `console.log("[DIAG] party override: ignored ...")` を**必ず 1 行**出す
     (`recruitCountOf` の `[DIAG]` `tavern.html:3281` と同じ作法)。黙って 4 に戻ると
     「シームが効いていないのに緑」という偽の緑が出る。
   - `location.search` を**毎回読む**。モジュール直下の `const` にしない
     (`tavern.html:3255-3257` が記録している TDZ の罠と同じ)。

3. `tools/probe_party_size.js` を新規作成。腕の起動は `sweep_recruit_balance.js:99-215` を流用する。
   この STEP では**空セクションと受入条件 1〜3 だけ**を通す。

**この STEP の受入条件**: 1. / 2. / 3.

### STEP2 — 静的・幾何の測定 (人数非依存の 3 点を「本当に変わらない」と 1 回で確定させる)

- スポーンタイル (受入条件 4.)
- 隊列順と zone (受入条件 5.)
- 下部 HP ミニバーの枠 (受入条件 6.)

⭐ ここは長時間プレイ不要。`departToScenario()` 直後の 1 フレームで採れる。

**この STEP の受入条件**: 4. / 5. / 6.

### STEP3 — 実プレイのペア比較 (本命)

- カメラ: 主人公が画面外のフレーム率 (受入条件 7.)
- カメラ: bbox クランプ区間が空に落ちた率 (受入条件 8.)
- 置き去りと救済 (受入条件 9.)
- フレーム時間 (受入条件 10.)

⭐⭐⭐ **ペア比較でしか測れない。** オートプレイは非決定論なので、N=4 と N=5 を
**同じシナリオ・同じ順番**で交互に走らせ、**ペアの差**を見る(単独の平均値を比べない)。
`sweep_recruit_balance.js` の `--fixed-order` (`:51`) と同じ思想。

⚠⚠ **走らせている間、作業ツリーを触らないこと。** 別窓が保存すると混合ビルドになり、
ゲームの数値は全部捨てることになる(2026-08-23 に実際に踏んだ)。走行前に `git status` ではなく
**mtime** で静かなことを確かめる。

**この STEP の受入条件**: 7. / 8. / 9. / 10.

### STEP4 — 判定・報告・非退行

- 既存 golden の非退行 (受入条件 11.)
- 本依頼書へ「実装結果」節 (受入条件 12.)

---

## 受入条件

実装窓が自分で「終わった」と判定できること。装置は `node tools/probe_party_size.js`。

1. **既定が本番と 1 ビットも変わらない** — `?party` を付けない腕で、6 シナリオすべての
   formation 人数が #7 と一致する。既存 `node tools/verify_recruit_size.js` が **79/79 のまま緑**。
2. **負のコントロールを道具に内蔵** — `--negative` を付けると
   「シームが恒等でなくなった」ことを検出する assert が**赤くなる**。緑のままなら **exit 1**。
   ⭐ 「赤くなるはず」を人間の目視に委ねない。道具自身が判定して落ちること。
3. **母集団ガード** — 各走行が「本当に N 人で出発した」ことを、`departToScenario()` 後の
   `sessionStorage["dragonfighters.partyMembers"]` の実体の長さで確認する。
   ⚠⚠⚠ **届かなかった走行を成功に数えないこと。** 「違反 0 件」は、母集団が 0 でも 0 件になる
   (2026-08-23 に `?doors=0` の腕が対象ノードへ一度も到達せず、空振りを緑と読み違えた)。
   到達数を必ずログの先頭に出す。
4. **スポーン** — N=5 と N=6 で、6 シナリオ × 全ノードにわたり
   (a) 全員が互いに別タイル (b) `isTileWall(tx,ty)` が全員 false
   (c) **最終手段の「頭に重ねる」枝 (`index.html:31531`) に落ちた仲間が 0 人**。
   ⭐ (c) が 1 人でも出たノードは座標を全部ログに残す(そのノードが上限の真の制約)。
5. **隊列** — N=5 / N=6 で `orderFormation` の出力が front→mid→rear を守り、`[0]` が front zone。
   zone が 1 つも欠けていない (`buildParty` の (1) 各 zone 最低 1 人 `tavern.html:3293-3303` が効いている)。
6. **下部 HP ミニバーの枠** — 390x844 (compact) で `#hpMiniBar` のチップ実寸を
   `getBoundingClientRect()` で採り、**横スクロールに落ちない N の上限**を出す。
   ~~⭐ 計算上の予測 = `#hpMiniBar` は `gap:6px` / 左右 `padding` 6+6 / `.hpChip { flex:1 1 0; min-width:52px }`
   (`index.html:2088-2096`) なので、390px では
   **4 人 + 撤退 = 5 枚 → 70.8px / 5 人 + 撤退 = 6 枚 → 58.0px (収まる) / 6 人 + 撤退 = 7 枚 → 48.9px (溢れる)**。~~
   ⚠ **予測と実測が食い違ったら実測が正**。予測に合わせて assert を書き換えないこと。

   ⚠⚠ **【2026-08-23 実装時に訂正】起草時の予測値 (70.8 / 58.0 / 48.9px) は誤りだった。** 実測は
   **4 人 75.33px / 5 人 59.06px / 6 人 52px**(`getBoundingClientRect()`)。予測が外した理由は 2 つ:
   - `.hpChip.retreatChip` は `flex: 0 0 auto` の**非伸縮枠**なので、basis が `auto` = **内容幅 52.67px**。
     `min-width: 52px` は下限でしかなく、撤退チップを他と同じ「1 枚の等分枠」として数えたのが誤り。
   - `clientWidth` は左右 `padding` 6+6 を**含む**。
   → 正しい導出式は **`(client − padL − padR − 撤退実寸 − gap×N) / N`**。実測と 0.01px 一致した。
   ⭐ **結論(5 人は収まる / 6 人は横スクロールへ落ちる)は予測と同じ**。外れたのは中間の数値だけ。
7. **カメラ / 主人公が画面外のフレーム率**(本命) — 同一シナリオ・同一順序のペアで
   N=4 と N=5 を走らせ、desktop 1280x800 と compact 390x844 の**両方**で測る。
   ⭐ 比較の基準は `index.html:6343-6345` が残している既存の実測値
   (**compact 25.9%(廃坑) / 16.5%(竜の巣) / desktop 1280 = 0%**)。
   ⚠⚠ **【2026-08-23 実装時に訂正】compact の 25.9% / 16.5% は比較基準に使えない。**
   この 2 つは救済節 `[compact-hero-anchor]` (`index.html:6354-6368`) を**入れる前**の値で、
   そもそも「救済節を足す理由」として書かれたもの。現行コードで測った値とは同じ土俵に無い
   (実測では compact は N=4 / N=5 とも **0.00%**)。**直接比べられるのは desktop の 0% だけ**。
   ⛔ **desktop が 0% から有意に上がったら、それが「5 人は行けない」の根拠**。
8. **カメラ / クランプ区間が空に落ちた率** — `computeCameraTarget` の
   `loCx <= hiCx` / `loCy <= hiCy` が偽になった回数を N ごとに数える。
   ⭐ 7. が「症状」で 8. が「原因」。**両方採ること**(症状だけだと直し方が決まらない)。
   ⚠ 計測は `window.__camAnchorOff` と同じ思想の**新規の dev シーム**で採る。本番は常に無効。
9. **置き去りと救済** — `MAX_LAG`(480) 超で `heroAI` が待った回数と、
   ワープ救済 (`index.html:16934`) の発動回数を N ごとに数える。
10. **描画コスト** — `performance.now()` で 1 フレームの実測を N ごとに採る。
    ⚠⚠ **JS 時間と真のコストは順位が正反対になることがある**(`project_camera_perf` の実測)。
    数字は出すが、**これ単独を可否の根拠にしない**。
11. **非退行** — 既存 golden が緑のまま:
    ~~`verify_recruit_size` 79 / `driver_grid_p8` 55~~ / `driver_grid_p9` 52 / `driver_graph_p6` 244 /
    `driver_spawn_not_on_gate` 47 / `driver_encounter_mopup` 36。
    ~~⚠ `driver_grid_p8` の基準は **55/55**(`30f4869` で golden 更新漏れを回収済み)。~~
    ⚠⚠ **【2026-08-23 実装時に訂正】起草時に書いた基準値のうち 2 本が古い。**
    - `verify_recruit_size` は **79 → 82**。#8 (`11e4678`) が assert を 3 本足した。
    - `driver_grid_p8` は **55 → 56**。#11 (`f62f145`) の時点で既に 56/56 になっていた。
    - 残り 4 本(p9 52 / graph_p6 244 / spawn_not_on_gate 47 / encounter_mopup 36)は起草時のまま。
    → **STEP4 の実測 = 82 / 56 / 52 / 244 / 47 / 36 で 6 本とも緑**(下記「実装結果」)。
12. **報告** — 本依頼書へ「実装結果」節を足し、次を書く:
    - **5 人の可否 / 6 人の可否**(それぞれ ⭕ / ⛔ と、根拠になった受入条件の番号)
    - ⛔ の場合、**何を直せば行けるか**を箇条書き(見積りは不要。直す作業は別チケット)
    - 起草時の表(前提 4 点の判定)が実装後も正しかったか。**違っていたら本文を訂正する**

---

## 撤退スイッチ

⭐ **このチケットは「既定が撤退状態」**。`?party` を付けなければ本番と 1 ビットも変わらない
(受入条件 1. が機械的にそれを検査する)。

- `?party=N` … 調査シーム。**付けたときだけ**人数が変わる。N は 2〜8 の整数のみ。範囲外は無視 + `[DIAG]` 1 行。
- 計測用の dev シーム(受入条件 8. / 10. 用)は `window.__*` 側に置き、**本番は常に undefined**。
  URL からは有効化できないようにする(`index.html:6353` の `__camAnchorOff` と同じ作法)。

---

## やらないこと

- ⛔ **`recruitCountOf()` の `MAX = 3` を外すこと。** 本チケットは測るだけ。
  外すかどうかはユーザーが結論を読んでから決める。
- ⛔ **`index.html:11962` の `PARTY_SIZE = 4` を触ること。** 既存ドライバ 20 本以上の基準が黙って動く。
- ⛔ **カメラを直すこと。** 8. で原因まで特定するが、修正は別チケット。
- ⛔ **準備画面 UI の作り替え。** 実測でキャラタブは全 6 職固定と分かったので、そもそも要らない。
- ⛔ **`.partyComp` / `.partyMemberToggle` の死んだ CSS の掃除。** 別件。
  このチケットに混ぜると調査の差分が読めなくなる。
- ⛔ **難易度・XP の調整。** #8 recruit-balance-retune の担当。
  ⚠ ただし 5 人が行けるという結論になった場合、**#8 の測定は人数の前提が変わるのでやり直しになる**
  ことを実装結果節に明記すること。
- ⛔ **`?party=N` を本番の導線から呼べるようにすること。** 調査シームであって機能ではない。

---

## 参照

- 上限を守れと書いているコメント本体: `tavern.html:3266-3270`
- 測定台の原型: `tools/sweep_recruit_balance.js`(#8 で作った酒場経由の腕)
- 依存関係: **#8 とは独立**だが、⚠ **結論が「5 人は行ける」なら #8 の前提が動く**ので、
  #8 の本番走行より**先に**このチケットを終わらせるのが安全。
- 依頼書の作法: [README.md](README.md)

---

## 実装結果

**ステータス**: **完了**(2026-08-23)。dev-loop 4 項目で完走。
STEP1 `a1558ba`(調査シーム + 測定装置の骨組み)/ STEP2 `83ac18f`(受入条件 4./5./6.)/
STEP3 `01eac91`(受入条件 7./8./9./10.)/ STEP4 = 本コミット(受入条件 11./12.)。

⭐ **本番の挙動は 1 ビットも変えていない。** `?party` を付けない腕は完全に恒等で、
`recruitCountOf()` の `MAX = 3`(`tavern.html:3274`)は 1 文字も触っていない。
`grep -c psProbe index.html` = **0**(受入条件 8./10. の計測シームは `index.html` を無改修のまま
配信スナップショットへ実行時注入する方式にした)。

---

### 1. 結論 — サイズごとの可否

| サイズ | 判定 | 根拠にした受入条件 |
|---|---|---|
| **5 人**(主人公 1 + NPC 4) | ⭕ **engine 側は行ける** | 4. / 5. / 6. / 7. / 8. / 9. / 10. がすべて条件を満たした |
| **6 人**(主人公 1 + NPC 5) | ⛔ **今のままでは行けない** | **6. だけで落ちる**(390x844 で下部 HP ミニバーが横スクロールへ落ちる)。⚠ かつ 7.〜10. は **N=6 では測っていない** |

⚠⚠ **これは「上限を 3 → 4 へ上げてよい」という意思決定ではない。**
本チケットが答えたのは「engine が 5 人を壊さずに通せるか」だけ。
難易度・XP・テンポが 5 人で成立するかは**測っていない**(それは #8 の領分。§5 の申し送り参照)。

#### 5 人が ⭕ の内訳

- **スポーン (4.)** — 156 配置 / 仲間の着地 **702 人ぶん**で違反 **0**。
  「頭に重ねる」最終手段の枝 (`index.html:31531`) に落ちた仲間も **0 人**。
- **隊列 (5.)** — 24 標本すべてで front→mid→rear と zone の充足が成立。
- **UI 枠 (6.)** — 390x844 で **横スクロールに落ちない上限がちょうど 5 人**。
- **カメラ (7.)** — 依頼書が置いた不合格条件「desktop が 0% から有意に上がる」が**成立しなかった**
  (desktop は N=4 / N=5 とも **0.03%**、12 ペアすべて差 −0.01〜+0.01pt)。
- **置き去り (9.)** — 停滞(詰み)は 24 走行とも **0 件**。
- **描画 (10.)** — `renderWorld` の p50 が 4 群すべてで N=4 と同値。

#### 6 人が ⛔ の理由と、直せば行けるもの

⛔ 落ちているのは **受入条件 6. の 1 点だけ**。390x844 で
`#hpMiniBar` の `client 390 / scroll 413` = **23px はみ出す**。
仲間チップは `min-width: 52px` に張り付き(素の計算値は 48.22px)、これ以上は縮まない。

直し方の候補(⚠ **どれも別チケット**。本チケットでは 1 行も直していない):

- `.hpChip` の `min-width: 52px`(`index.html:2088-2096`)を compact のときだけ下げる。
  ⚠ ただし 48px まで下げても 4 桁の HP 表示が入るかは未確認。
- **撤退チップを縮める** — 実測 **52.67px** を占めているのは `flex: 0 0 auto` で
  内容幅がそのまま basis になっているから。アイコンのみにすれば ~20px 縮む。
- `#hpMiniBar` に `flex-wrap: wrap` を許して 2 段にする。⚠ 帯の高さが変わるので
  `cameraBottomHud()` を出所にしている計算(選択肢バー・カメラ下端)が全部連動する。
- 主人公チップを固定枠にし、NPC ぶんだけ内側の横スクロール容器へ入れる。

⚠⚠ **UI を直しても 6 人が ⭕ になるわけではない。** 7./8./9./10. は N=4 と N=5 のペアでしか
走らせていない。**6 人のカメラと置き去りは未測定**なので、UI を直したら測り直しが要る。

---

### 2. 受入条件ごとの実測値

#### 4. スポーン(N=5 / N=6 × 6 シナリオ)

| 検査 | 結果 |
|---|---|
| 腕 | 12 本 / **156 配置** / 仲間の着地 **702 人ぶん** |
| (a) 全員が互いに別タイル | 違反 **0** |
| (b) `isTileWall` が全員 false | 違反 **0** |
| (c) 「頭に重ねる」枝に落ちた仲間 | **0 人**(N=5 も N=6 も) |

⭐⭐⭐ **依頼書 §背景の表 #1 が見落としていた配置点がもう 1 本あった。**
`resolveAllyInitTile()`(起動時 1 回・代替候補 24 個)だけでなく、
**`placeNodeParty()`(`index.html:33230` 付近・`enterNode` からノード遷移のたび・候補 RING 16 個)**
が実プレイのほぼ全部を担っている。**両方を掃いた上で 0 件**なので判定「⛔ 成立しない」は変わらない
(本文の表を訂正済み)。

#### 5. 隊列

**24 標本すべて成立**(酒場が書いた `partyMembers` と index が実際に読んだ `formation` の
2 経路 × N=5 / N=6 × 6 シナリオ)。front→mid→rear の順序保持 / `[0]` が front zone / zone 欠落 **0**。

- N=6 の例: `front>front>front>mid>mid>rear`
- N=5 の例: `front>mid>mid>mid>rear`

#### 6. 下部 HP ミニバー(390x844 compact / `getBoundingClientRect()`)

| N | 頭 + 仲間チップの実寸 | 撤退チップ | client / scroll | 横スクロール |
|---|---|---|---|---|
| 4 | **75.33px** | 52.67px | 390 / 390 | なし |
| 5 | **59.06px** | 52.67px | 390 / 390 | なし |
| 6 | **52px**(`min-width` に張り付き。素の計算値 48.22px) | 52.67px | 390 / **413** | **★あり** |

⭐ **横スクロールに落ちない N の上限 = 5 人**(主人公 1 + NPC 4)。

⚠⚠ **起草時の予測値 (70.8 / 58.0 / 48.9px) は外れた**(→ 本文の受入条件 6. を訂正済み)。
外した理由は ① `.hpChip.retreatChip` が `flex: 0 0 auto` の**非伸縮枠**で basis が内容幅 52.67px
になること ② `clientWidth` が左右 padding 6+6 を**含む**こと の 2 点。
正しい導出式 **`(client − padL − padR − 撤退実寸 − gap×N) / N`** は実測と **0.01px 一致**した。
⭐ **結論(5 人は収まる / 6 人は溢れる)は予測と同じ。** 外れたのは中間の数値だけ。

#### 7. カメラ / 主人公が画面外のフレーム率(本命)

24 走行 / 12 ペア / **41,610 フレーム** / 廃坑 + 竜の巣 / 各 3 ペア / 1 走行 60 秒 / 所要 24 分 38 秒。

| | N=4 | N=5 |
|---|---|---|
| desktop 1280x800 | **0.03%** (3 / 9,784) | **0.03%** (3 / 9,282) |
| compact 390x844 | **0.00%** | **0.00%** |

- 12 ペアすべて差 **−0.01〜+0.01pt**。
- ⛔ **依頼書の不合格条件「desktop が 0% から有意に上がったら 5 人は行けない」は成立しなかった。**
- ⚠⚠ 依頼書が比較基準に置いた compact **25.9%(廃坑) / 16.5%(竜の巣)** は
  `[compact-hero-anchor]` を**入れる前**の値なので比較対象にできない(本文を訂正済み)。
  直接比べられるのは **desktop の 0%** だけ。

#### 8. カメラ / クランプ区間が空に落ちた率(= 7. の原因側)

- **`loCy <= hiCy` は全 24 走行で一度も偽にならなかった。** 偽になるのは X だけ。
- **desktop は全 4 群で 0.00%**(合計 **18,623 回**の clamp で 0 件)。N=5 でも横の余裕が
  **最小 +328px** 残る。
- **compact / 廃坑** も両腕 **0.00%**。
- **compact / 竜の巣 のみ** N=4 **22.40%** / N=5 **23.82%**。

⚠⚠⚠ **STEP3 の初回報告にあった「34.95% → 65.34% で倍増」は再測定で再現せず、取り下げた。**
走行ごとの emptyX 率は 前回 N=4 15.5 / 54.4 / 40.8% ・ N=5 64.6 / 61.2 / 70.2%、
今回 N=4 15.5 / 18.5 / 35.4% ・ N=5 25.5 / 22.6 / 23.2%。
6 ペア中 5 ペアで N=5 > N=4 なので**向きは N=5 が悪い側**だが、
**レンジが重なっており差の大きさは 6 ペアでは決まらない**。

⭐ 一方で**幾何は 2 走行で完全に一致した(決定論)**:

| 指標 | N=4 | N=5 |
|---|---|---|
| 隊列 bbox p50 | 372px | 372px |
| 隊列 bbox p95 | **480px** | **576px** |
| 横の余裕(最小) | −186〜−187.8px | **−282px** |

⭐ **正しい主語**: compact + 竜の巣は **4 人の時点で既に**区間が空へ落ちている
(bbox p50 372px > 閾値 294px)。5 人で悪化するのは**幾何の裾**であって、
「5 人で初めて壊れる」ではない。症状(受入条件 7.)が 0.00% のままなのは
`[compact-hero-anchor]` の 2 点ハードクランプが吸収しているから。

#### 9. 置き去りと救済

| | N=4 | N=5 |
|---|---|---|
| `MAX_LAG`(480) 超で `heroAI` が待った周期 | **0 回** | **8 回**(初回走行では 2 / 11) |
| ワープ救済 (`index.html:16934`) の発動 | **0 人** | **3 人**(初回走行では 0 / 2) |
| 停滞(詰み) | **0 件** | **0 件** |

⭐ 向きは N=5 が悪い側だが、**24 走行とも詰まなかった**。救済機構が設計どおり働いている。

#### 10. 描画コスト

- `renderWorld` の p50 は **4 群すべてで N=4 と N=5 が同値**、かつ 2 走行間でも同値
  (desktop/廃坑 0.6ms ・ desktop/竜 0.3ms ・ compact/廃坑 0.3ms ・ compact/竜 0.2ms)。
- フレーム間隔 p50 は全群 **29.6〜30.0ms** で不変(`setInterval(moveEnemies, 30)` に律速)。
- ⚠ 依頼書のとおり、**これ単独を可否の根拠にしていない**。

#### 11. 非退行(既存 golden)

| ドライバ | 依頼書の基準 | **実測** | |
|---|---|---|---|
| `verify_recruit_size` | ~~79~~ | **82 / 82** | ⚠ 基準が古い。#8 (`11e4678`) が assert を 3 本追加 |
| `driver_grid_p8` | ~~55~~ | **56 / 56** | ⚠ 基準が古い。#11 (`f62f145`) の時点で既に 56 |
| `driver_grid_p9` | 52 | **52 / 52** | |
| `driver_graph_p6` | 244 | **244 / 244** | |
| `driver_spawn_not_on_gate` | 47 | **47 / 47** | |
| `driver_encounter_mopup` | 36 | **36 / 36** | |

- **6 本とも FAIL 0。** 既知の間欠フレーク `(8e)` も 1 回目で PASS(再走不要だった)。
- 走行中に別窓が書いていないことを **mtime** で確認済み(`git status` ではなく mtime で見る)。
- ⭐ 本チケットの `tavern.html` への変更が `devPartySizeOverride` の**追加だけ**であることを
  `git diff a1558ba^ HEAD -- tavern.html` で確認した(`recruitCountOf` の clamp は無改修)。

#### 1./2./3.(STEP1 で確定済み)

`node tools/probe_party_size.js` = **本番 57 / 57 PASS** / `--negative` = **22 / 22 PASS**。
負のコントロールは道具に内蔵してあり、恒等が壊れたのを検出できなければ **exit 1** で落ちる。

---

### 3. 起草時の表(前提 4 点)は実装後も正しかったか

| # | 起草時の判定 | 実装後 | |
|---|---|---|---|
| 1 | スポーン枠が 4 人前提 → ⛔ 成立しない | ⭕ **判定は正しかった**(違反 0 件を 702 人ぶんで実測) | ⚠ ただし**根拠に挙げた関数が片方しか無かった**。`placeNodeParty()` を追記して訂正した |
| 2 | 隊列 `orderFormation` が 4 人前提 → ⛔ 成立しない | ⭕ **正しかった**(受入条件 5. で 24 標本すべて成立) | 訂正なし |
| 3 | 準備画面のキャラタブが 4 人前提 → ⛔ 成立しない | ⭕ **正しかった**(N=6 でも準備画面は変わらない) | 訂正なし。⚠ ただし**プレイ中の下部 HP ミニバーは人数に依存した**(受入条件 6.)。「UI は人数非依存」と一般化しないこと |
| 4 | カメラ・視界・描画 → ⭕ 要調査 | ⭕ **調査する価値はあった**が、**危ないと名指しした desktop は白だった** | 危険側は desktop ではなく **compact + 竜の巣**。しかもそれは **4 人の時点で既に**空区間へ落ちている |

⭐⭐⭐ **表そのものは 4 行とも判定が生き残った。** 崩れたのは「根拠の挙げ方」と
「どこが危ないかの見立て」であって、結論ではない。

---

### 4. 本文の訂正(5 箇所)

1. **前提表 #1 のセル** — 配置点は `resolveAllyInitTile()` だけではなく
   **`placeNodeParty()`(`index.html:33230` 付近・RING 16 個)** も担っている旨を追記。判定は不変。
2. **受入条件 6. の予測値** — 70.8 / 58.0 / 48.9px を打ち消し線にし、実測 **75.33 / 59.06 / 52px**
   と正しい導出式へ差し替え。結論は不変。
3. **受入条件 7. の比較基準** — compact 25.9% / 16.5% は `[compact-hero-anchor]` **導入前**の値で
   比較に使えない旨を追記。
4. **受入条件 11. の基準値** — `verify_recruit_size` **79 → 82** / `driver_grid_p8` **55 → 56**。
5. **測定台の注記** — **`?autoplay` はカメラ測定に使えない**(`focusCameraOn` が `__autoplay` で
   丸ごと早期 return する `index.html:6657`)。代わりに `RUN.auto` を使う。
   併せて **`currentPhase` は戦闘の目印にならない**(竜の巣では `encounterActive=true` のまま
   `currentPhase="explore"` が続く)。

⭐ 依頼書が既に指摘していたとおり **`driver_camera*.js` は実在しなかった**(訂正不要)。
測定装置は本チケットで新規に作った(`tools/probe_party_size.js`)。

---

### 5. 申し送り(次に触る人へ)

#### ⚠⚠ #8 recruit-balance-retune との関係

依頼書の「やらないこと」節は「5 人が行けるなら #8 の測定はやり直しになる」と予告していたが、
**#8 は本チケットより先に完了している**(`11e4678` / 120 走行で計 4 人・3 人・2 人を測り、
廃坑を `recruit: 3` へ)。したがって正しい言い方は:

> **#8 の 120 走行は「パーティ 4 人が上限」という前提の下で最適化された結果である。**
> 上限を 5 人へ上げるなら、#8 が出した数値(廃坑の `recruit: 3` / クリア率 / XP 中央値)は
> **前提が変わるので測り直しになる**。

⛔ **本チケットは「上限を上げよ」とは言っていない。** 上げるかどうかはユーザーの決定であり、
上げると決めたときに初めて #8 の再走行が必要になる。

#### ⚠⚠⚠ 監視事項 — この救済節を壊す改修は 5 人化とセットにできない

`[compact-hero-anchor]`(`index.html:6354-6368`)の **2 点ハードクランプが症状を吸収している**。
受入条件 8. の compact + 竜の巣は N=4 で既に 22.40% が空区間に落ちているのに、
受入条件 7. の症状が 0.00% で済んでいるのはこの節のおかげ。

⛔ **この救済節を外す / 弱める改修は、5 人化と同じリリースに乗せてはいけない。**
外すなら、受入条件 7. を N=4 / N=5 の両方で測り直してからにすること。

#### ⚠ 受入条件 8. の人数差は 6 ペアでは語れない

compact + 竜の巣の emptyX 率は N=4 と N=5 でレンジが重なる(§2 の 8. 参照)。
**語るなら「4 人の時点で既に区間が空」を主語にするのが安全。**
人数差を有意に言いたければペア数を増やす必要がある(**別チケット**)。

#### ⚠ 置き去りの向きは N=5 が悪い

受入条件 9. で `MAX_LAG` 超の待機が 0 回 → 8 回、ワープ救済が 0 人 → 3 人に増えた。
**詰みは 0 件**なので可否には効かないが、**体感テンポは落ちる可能性がある**。
5 人化するなら実機の体感で見ること(自動計測では拾えない)。

#### 撤退・道具

- **撤退スイッチ**: `?party=N`(2〜8 の整数のみ)。**付けなければ本番と恒等**。
  範囲外・非整数は無視して `[DIAG]` を 1 行出す(silent fail-open にしていない)。
- **装置**: `node tools/probe_party_size.js`(本番 57/57)/ `--negative`(22/22・緑なら exit 1)。
- ⛔ **`?party=N` は本番の導線からは呼べない。** 調査シームであって機能ではない。
