# #18 シナリオ2「盗賊団のアジト」のクリア率 0% の原因調査

- **起草**: 2026-08-23(計画窓) / **ステータス**: **完了 `faaaa61`**(2026-08-23 / 測定 84 走行・§12 参照)
- **触るファイル**: `tools/probe_s2_clear.js`(新規)、本依頼書、`実装依頼書/README.md`(§11 の行・承認後)
- ⛔ **触らないファイル**: `index.html` / `tavern.html` / `audio.js` / `js/*.js`
  — **本チケットは調査だけで、本番の振る舞いを 1 バイトも変えない**(ユーザー決定 2026-08-23)。
  §2-10 のとおり調査シームを本番へ置くと changelog ガードに書ける嘘が要るので、
  **測定は全部ツール側から本番のグローバルを読むだけで済む**ことを実測で確かめてある。

---

## 1. 目的

シナリオ2 のクリア率が **0% で床に張り付いて**いる。#8 は 80+40 走行の末に
「シナリオ2 は n1 で折れる。募集人数では動かない」と結論して宿題へ切り出し、
#16 は 8 ノードを 1 ノードへ畳んだが完走率は **0/3 のまま**だった。

⚠ **その 2 枚が置いていった前提は、今日の実測で 3 つとも崩れている**(§2-2)。
本チケットは「何を直せば動くのか」を**測って数値で出す**ところまでを担う。
⛔ **直しはしない。** 候補と効き目を出し、どれを採るかはユーザーが決める。

**ユーザー決定(2026-08-23)**:

| 問い | 決定 |
|---|---|
| チケットの形 | **調査のみ(#14 型)**。本番の振る舞いは変えず、測って候補を数値付きで出す |
| 測る範囲 | **シナリオ2 だけ**。沼 `lizard-swamp`(同じ ★★☆・#8 で未測定)は別チケットへ残す |
| 起案順 | #16 の宿題 1(n7 の間欠停止)より先に本件 |

⭐ 不採用にした案: 「調査 + 是正を 1 枚で」。**測る前に直す軸を決め打ちすることになる**ため。
今日の実測で最有力の軸(§2-3)は #8/#16 のどちらも想定していなかったものなので、
決め打ちは高い確率で外していた。

---

## 2. 着手前の実測(この窓が本番コードと実走行で確かめた事実)

⭐ **測定は 2 系統。**
①既存の測定台 `tools/sweep_recruit_balance.js`(#8 のもの・酒場経由)で **10 走行**、
②新設した下見 `probe_s2_fight.js`(scratchpad・**未コミット**)で **15 走行**。
合計 **25 走行**。すべて 2026-08-23 / HEAD = `d7326d5`(作業ツリー空)。

### 2-1. 現状のクリア率(既存の測定台・N=5 ペア = 10 走行)

    node tools/sweep_recruit_balance.js --scen bandits-forest --pairs 5 --workers 2 --port 9362 --max 300

| 腕 | 出発 | n | クリア | 全滅 | 打切 | 仲間生存(中央) | XP(中央) | 主人公HP(中央) |
|---|---|---|---|---|---|---|---|---|
| OLD | 計 4 人 | 5 | **0%** | 100% | **0%** | **3/3** | 1,190 | 0 |
| NEW | 計 3 人 | 5 | **0%** | 100% | **0%** | **2/2** | 830 | 0 |

- 到達ノード: 両腕とも `n7:5`(1 ノードなので当然)
- 母集団ガード OLD 5/5 / NEW 5/5・装置 assert 崩れ 0 件・pageerror 0 件
- ペア比較 5 組 → 両方クリア 0 / OLDのみ 0 / NEWのみ 0 / **両方だめ 5**

⭐ #8 の N=20 ペアと**同じ姿**(あちらも 0% / 0% / 両方だめ 20)。畳んでも数字は動いていない。

### 2-2. ⚠⚠⚠ 引き継ぎメモと #8 が置いていった前提のうち、**3 つが崩れている**

| 前提(出所) | 実測 | 結論 |
|---|---|---|
| 「シナリオ2 は **n1** で折れる」(#8 §9-3) | 既定の腕に **n1 は存在しない**。#16 (`7642eba`) が 1 ノード(`n7`)へ畳んだ。到達ノードは 10/10 とも `n7` | **失効**。#8 の 120 走行の数値は畳む前のもの |
| 「n7 の間欠停止が測定を汚染する(6 走行中 3 回)」(#16 §実装結果) | **この測定台では 25 走行中 0 件**。sweep の打切 0%、`debugReport` の `outcome:"aborted"` も 0 件 | **今の既定の腕では再現しない**。#16 の 3/6 は `?s2fold=0` 腕込み・index 直起動の別測定台。⭐ ただし 0 件は「存在しない」ではないので、測定台には停滞の数え口を必ず残す(§8) |
| 「敵を減らせば勝てるはず」(#16 が 26 → 11 体へ) | 11 体でも **0/25** | **人数の軸は既に空振り済み** |

### 2-3. ⭐⭐⭐ 敗北の実体 — **パーティは全滅していない。主人公 1 人が死んでいる**

**25 走行すべてで `isPartyWiped()` は一度も真になっていない。**
`sweep` の「仲間生存(中央)」が OLD **3/3** / NEW **2/2** = *全員生存*。
下見の 12 走行(§2-5)でも **12/12 で仲間が 1 人以上生存**、うち **7 走行は仲間が完全に無傷**。

コードの根拠(実測で行を確認済み):

    index.html:17949   if (heroIsHead) return false;       // 頭=主人公の死 → GO
    index.html:17867   function isPartyWiped() {
    index.html:17868     return hp <= 0 && allies.length > 0 && allies.every(a => !a.alive);

`onHeadDowned()` は **頭が主人公なら委譲せず即 `gameOver`**。主人公が後衛(ally 側)でも
`if (ally.isHero) { gameOver = true; }` が 4 箇所(`:24522` / `:27128` / `:29648` / `:31518`)。
⇒ **どちらの隊列でも「主人公 1 人の死 = 即敗北」**。`isPartyWiped()` は事実上使われない。

さらに **被弾が主人公に集中している**。Lv1 の 3 走行では、仲間の HP が
**最初から最後まで 1 も減らない**(下見ログの生の列):

    START lv=1 heroHp=30/30 allies=26/18 (elf/mage) 敵=10/10
    ...
    61s C | 1 | 8 | 30 | 26/18 | 41,16 | n7 | 盗賊重装兵
    65s C | 2 | 8 | 30 | 26/18 | 42,16 | n7 | 盗賊重装兵,盗賊重装兵
    66s C | 2 | 8 | 22 | 26/18 | 42,16 | n7 | 盗賊重装兵,盗賊重装兵
    69s C | 2 | 8 | 14 | 26/18 | 42,16 | n7 | 盗賊重装兵,盗賊重装兵
    69s . | 0 | 8 | ★0 | 26/18 | 42,16 | n7 |        ← 主人公だけ 30 → 0、仲間は 26/18 のまま

構造上の理由(コードで確認・**推測ではない**):
`pickEnemyTarget()`(`index.html:24735`)は候補を距離で選び、
`zonePullFor()`(`:24724`)が **front を 0.75 倍**して「前ほど狙われやすい」バイアスを掛ける。
頭は隊列の先頭に居るので常に最短。⇒ 前衛を主人公が務める編成では、
**主人公が全被弾を独占したうえで、その 1 人の死が敗北条件**になる。

### 2-4. 勝利条件との非対称 — **敵を 1 体残らず倒さないと勝てない**

    index.html:17746   if (enemies.every(e => !e.alive || e.passiveNpc) && objectiveDone) {
    index.html:17747     dungeonCleared = true;

⭐ **ボスを倒しただけでは終わらない。** 実測(Lv2 の 1 走行):

    117s C | 4 | 3 | 15 | 42/26 | 53,17 | 盗賊魔術師†,盗賊頭「スカー」,盗賊†,盗賊†
    122s C | 4 | 2 |  3 | 42/26 | 53,17 | 盗賊魔術師†,盗賊頭「スカー」★†,盗賊†,盗賊†   ← スカー撃破
    130s . | 0 | 2 |  0 | 42/26 | 56,13 |                                        ← 残敵 2 で主人公が落ちる

**スカーを倒した後、残り 2 体を掃討しきれずに主人公が死んで敗北。仲間は 42/26 の満タン。**
畳んだ n7 は敵 **10 体 + ボス**(+ スカーの召喚 2 体で最大 13)。
⇒ 「主人公 1 人の死で負ける」側と「13 体を全滅させないと勝てない」側の**両方が厳しい**。

### 2-5. 入場レベルを上げても動かない(推奨 Lv 4 でも 0/3)

⚠ 測定台は `localStorage` を purge するので入場は **Lv1**。しかし本番のシナリオ2 は

    tavern.html:2207   recommendedLevel: 4,
    tavern.html:2219   locked: true,
    tavern.html:2220   unlockAfter: "goblin-mine",

= **廃坑クリアが前提・推奨 Lv 4**。廃坑 1 周の XP 中央は #8 実測で **1,890**
(`XP_THRESHOLDS = [0,1000,3000,...]` `index.html:11760` → **Lv2**)。
つまり #8/#16 の 0% は「推奨より 3 レベル低いパーティ」の数字だった。

そこで `dragonfighters.xp` を焼いて 4 段階で測り直した(各 3 走行):

| 入場 Lv | 焼いた XP | 主人公 maxHP | 決着 | 同時交戦の最大 | 終了時の残敵 | 仲間 |
|---|---|---|---|---|---|---|
| 1 | 0 | 30 | **WIPE 3/3** | 7 / 3 / 2 | 3 / 8 / 8 | **3 走行とも無傷** |
| 2 | 1,890(= 廃坑 1 周) | 40 | **WIPE 3/3** | 6 / 7 / 4 | 4 / 3 / 2 | 満タン 1・被弾 1・死亡 1 |
| 3 | 3,000 | 50 | **WIPE 3/3** | 9 / 8 / 4 | 5 / 5 / 1 | 満タン 2・被弾 1 |
| **4(推奨)** | 6,000 | 60 | **WIPE 3/3** | 8 / 6 / 11 | 6 / 3 / 7 | 満タン 1・被弾 2 |

⭐⭐⭐ **主人公の HP を 30 → 60(2 倍)にしても 0/12。** レベルは軸として弱い。
⭐ 仲間 NPC の Lv は `assignCompanionLevels()`(`tavern.html:5345`)が推奨 Lv 帯から抽選するが
**`cap = Math.min(heroLevel, 10)`** で主人公 Lv に頭打ちになる ⇒ パーティは常に一様な Lv。
(「仲間だけ Lv4 で来る」ということは起きない。実測で全員同 Lv を確認)

⚠ **この 12 走行が再現しているのは XP だけ**で、装備と金貨は再現していない
(廃坑クリアの報酬 = 金貨 50 + 装備)。**実プレイより弱い側に寄っている**ので、
STEP2 では「廃坑を実際に 1 周させてから続けて出発する」腕も必要(§5)。

### 2-6. ⭐⭐ 廃坑との対照 — **同じ Lv1・同じ総数でも廃坑は 2/3 クリアする**

    node <probe> --runs 3 --scen goblin-mine        # 入場 Lv1・purge のみ

| | シナリオ2(畳んだ n7) | 廃坑(#P8 で 2 部屋) |
|---|---|---|
| 決着(Lv1・3 走行) | **WIPE 3/3** | **CLEAR 2 / WIPE 1** |
| **入場時に場に居る敵** | **10 体**(`敵=10/10`) | **2 体**(`敵=2/2`) |
| 走行全体の敵の総数 | 11(+ 召喚 2) | 10〜11 |
| 同時交戦の最大 | 7 / 3 / 2 | 4 / 4 / 7 |
| 終了時の主人公 | 0 / 0 / 0 | **14/40** / **8/40** / 0 |
| 仲間の被弾 | **無し**(3/3 走行) | **有り**(32/25/18 of 34/28/22 など) |

⭐⭐⭐ **総数はほぼ同じ。違うのは「一度に場に居る数」。**
廃坑はノードを進むたびに 2 体ずつ湧くので、パーティは**戦闘の切れ目ごとに回復**を貰える
(#13 の掃討 → 休憩)。畳んだシナリオ2 は 10 体が最初から全部同じ部屋に立っている。
⚠ ただし **畳む前のシナリオ2 も 0%** だった(#8 の 20 ペア)ので、
**「畳んだこと」が原因だと即断してはいけない**。この対照が示すのは
「場に居る敵の数」が**候補の 1 つとして測る価値がある**ということまで。

### 2-7. 交戦のまとまり — 門の 3 体と焚き火の 4 体 + ボスは**同じ戦闘に畳まれる**

交戦の判定は「**敵ごとの `engagePx` の内側にプレイヤーが入ったか**」(`index.html:19260`)。
増援は +`REINFORCE_MARGIN_PX`(160px、`:19236`)。射程表は `index.html:18290`:

| range | tiles | engagePx | = タイル(96px/タイル) |
|---|---|---|---|
| melee | 1 | 400 | 4.17 |
| bow | 12 | **1152** | **12.0** |
| spellSingle | 10 | **960** | **10.0** |

畳んだ n7 のスポーン(`index.html` の `buildBanditsForestRun` 内・全部 global 座標):

| 群 | 座標 | 種別 |
|---|---|---|
| 橋の見張り | (33,15) bandit / (34,16) banditArcher | melee / bow |
| 門の門番 | (50,15)(50,16) banditHeavy / (51,17) banditArcher | melee×2 / bow |
| 焚き火 | (55,12) banditHeavy / (56,11)(56,14) banditArcher / (55,13) banditMage | melee / bow×2 / 術 |
| ボス | (57,12) scar | melee |

⚠ **門の群と焚き火の群は 5.7 タイルしか離れていない**。弓の engagePx は 12 タイルなので、
門の射手を起こす位置に立った時点で焚き火の射手 2 人と魔術師も同時に条件を満たす。
⭐ #11 のコード注釈が確かめているのは「**橋の見張りとは 17 タイル離れているので別の戦闘**」
までで、**門と焚き火のあいだは確かめていない**。実測の同時交戦最大は **11**(Lv4 run3)。

### 2-8. 敵の質の比較(廃坑との差)

| | HP | AC | 命中 | ダメージ | 備考 |
|---|---|---|---|---|---|
| goblin | 8 | 14 | +2 | 1d6 | 廃坑 |
| goblinBrute | 16 | 13 | +4 | 2d6+2 | 廃坑の前衛 |
| bandit | 10 | 14 | +3 | 1d6+1 | |
| **banditHeavy** | **22** | **16** | **+5** | **2d6+3** | **n7 に 3 体** |
| banditArcher | 8 | 13 | +4 | 1d8+1 | crit 19-20 ×3・**3 体** |
| banditMage | 6 | 12 | +2 | 2d6 | fireBolt / magicMissile / scorchingRay |
| **scar** | **60** | **17** | **+7** | **2d6+4** | crit 19-20 ×3・HP50% で AC+4・**部下 2 体召喚** |

`banditHeavy` は `goblinBrute` に対し HP +37% / AC +3 / 命中 +1 / ダメージ +1。それが **3 体**。

### 2-9. 停滞(#16 の宿題 1)は、この測定台では 0 件

25 走行すべてで sweep の「打切」= **0%**、`debugReport` の `outcome` に `aborted` は **0 件**、
`violations` に `stall` / `run-timeout` も **0 件**。
⭐ 本番の診断ウォッチドッグは `?autoplay` が付いていれば**既に動いている**
(`index.html:35273` 付近。有効化 = `?autoplay` / `?autodebug` / `?diag=1`)。
決着の語彙も既にある: `clear` / `defeat` / `aborted`(90 秒無進行 = `STUCK_ABORT_MS`、`:35636`)。
⇒ **「全滅」と「停滞」を別々に数えるための新しいシームは要らない。** `localStorage` を読むだけ。

### 2-10. changelog の要否

`scripts/hooks/check_changelog.py:24` の `GAME_LOGIC = ("index.html", "tavern.html", "audio.js")`。
本チケットが触るのは `tools/` と `実装依頼書/` だけ ⇒ **鳴らない**。
⭐⭐⭐ しかも §2-9 のとおり **調査シームを本番へ置く必要が無い**ことを実測で確かめてある。
(置くと「プレイヤーに見える変化が無いのに index.html を触る」= CLAUDE.md が名指しで禁じた設計になる)

---

## 3. 変更範囲

| ファイル | 変更 |
|---|---|
| `tools/probe_s2_clear.js` | **新規**。測定台(§8) |
| `実装依頼書/2026-08-23_scenario2-clear-rate.md` | 本書。§12 に結果を書き戻す |
| `実装依頼書/README.md` | §11 の行を追加(**承認後**) |

⛔ `index.html` / `tavern.html` / `audio.js` / `js/*.js` は**開かない**(§2-10 で不要を確認済み)。
⛔ ゲームの数値・データ・座標は **1 つも変えない**。

---

## 4. STEP1 — 測定台 `tools/probe_s2_clear.js`(新規)

`tools/sweep_recruit_balance.js` を土台にする(酒場 → `departToScenario()` の本番導線。
⛔ index 直起動にすると人数が本番と変わる = #8 の実測)。**足すのは観測項目だけ**。

必ず出す列(§2 で「これが効く」と分かったもの):

1. **決着の 4 分類** — `clear` / `defeat`(全滅) / **`stall`** / `timeout`
   ⭐ `stall` は `localStorage["dragonfighters.debugReport"]` の
   `runs[].outcome === "aborted"` と `violations["stall"|"run-timeout"]` から採る(§2-9)。
   ⛔ 「上限秒数を超えた」だけで停滞と数えない(遅いだけの走行と混ざる)。
2. **主人公が死んだ瞬間に生きていた仲間の数** — `0` なら本当の全滅、`≧1` なら**主人公落ち**。
   ⭐ これが §2-3 の主張を毎走行で機械検査する列になる。
3. **主人公の被ダメージ / パーティ総被ダメージ** の比(0.5〜1 秒ポーリングの差分の総和で近似)。
4. **同時交戦の最大数**(`encounterEnemyIndices.length` のピーク)と、その時の敵の名前。
5. **入場 Lv / 主人公 maxHP / 仲間の Lv**(母集団の記録)。
6. 終了時の残敵数と、**ボスを倒したか**。

⚠ 裸の識別子で読む(`window.<名>` では読めない = classic script 直下の `let`)。
⚠ ポーリングを短くしすぎない(150ms の `evaluate` は測定対象そのものを遅くする = P9 の教訓)。
⚠ `path.resolve` 必須。ROOT をスラッシュのまま持つと `path.join` の返す区切りと
`startsWith` が一致せず **全ファイル 404**(本窓の下見で実際に踏んだ)。

## 5. STEP2 — 母集団の腕(入場状態)

| 腕 | 作り方 | 何が分かるか |
|---|---|---|
| A: purge のみ | 既存どおり | Lv1。#8/#16 と比較可能な基準線 |
| B: XP 焼き | `dragonfighters.xp = 1890 / 3000 / 6000` | Lv2 / 3 / 4。§2-5 を N を増やして確認 |
| C: **廃坑を実際に 1 周** | 廃坑をクリアさせてから続けて出発 | **装備と金貨も本物**。B との差が「装備の寄与」 |

⭐ C が本命。B は装備を再現していないので**実プレイより弱い側に寄っている**(§2-5)。
⚠ C は 1 走行が 2 倍以上かかる。まず A/B を N=10 ずつ、C は N=5 で足りる。
⚠ C は廃坑が **2/3 でしかクリアしない**(§2-6)ので、**「廃坑をクリアできた走行だけ」を
母集団にする**こと。失敗した走行を混ぜると入場状態が腕の中で割れる。

## 6. STEP3 — 軸の腕(既存の撤退スイッチだけで作る)

⭐⭐⭐ **本番を書き換えずに作れる腕を優先する。**

| 腕 | スイッチ | 測りたい軸 |
|---|---|---|
| `?recruit=0` | `tavern.html:3266` | 人数(計 4 人)。#8 の再確認 |
| `?dndrange=0` | `index.html:4581` | **交戦のまとまり**。弓の engagePx が 1152 → 576 に戻るので門と焚き火が割れるはず(§2-7) |
| `?mopup=0` | `index.html:19240` | #13 の掃討合流。休憩の回数が戻る = 回復量が増える |
| `?s2fold=0` | `index.html:3870` | 8 ノード版。**「場に居る敵の数」の軸**(§2-6) |

⚠ `?dndrange=0` は**敵だけでなく味方の射程も戻す**。片側だけの実験ではない、と結果に明記すること。
⚠ 4 本を掛け合わせない。**1 本ずつ既定と比べる**(交互作用を見るには走行数が足りない)。

## 7. STEP4 — 報告

§12 に、STEP2/3 の表と**候補の一覧**を数値付きで書く。⛔ 直さない。
候補として少なくとも次を、効き目の実測値とともに並べること(⭐ 順位は付けてよいが決めない):

| # | 候補 | 触る場所(予定) | 今日の材料 |
|---|---|---|---|
| 1 | **敗北条件** — 主人公が倒れても仲間が生きていれば継続(気絶扱い) | `onHeadDowned` / 4 箇所の `ally.isHero` | 25/25 走行で仲間が生存(§2-3) |
| 2 | **被弾の配分** — 前衛バイアス / 隊列 / 主人公を壁にしない | `zonePullFor` | Lv1 で仲間の被弾ゼロ(§2-3) |
| 3 | **交戦のまとまり** — 門と焚き火を別の戦闘へ | スポーン座標 or engagePx | 同時交戦 最大 11(§2-7) |
| 4 | **勝利条件** — ボス撃破で終了(全滅要求をやめる) | `checkDungeonClear` | ボス撃破後に残敵 2 で敗北(§2-4) |
| 5 | **敵の質・数** — `banditHeavy` 3 体 / `scar` の召喚 | シナリオ2 のデータ | §2-8 の比較表 |
| 6 | **入場レベル / 装備** — 廃坑の周回誘導・推奨 Lv の提示 | 酒場 | Lv4 でも 0/3(§2-5) |

⚠ **候補 1・2・4 は 6 シナリオ全部に効く**(シナリオ2 専用ではない)。
効き目を出すときは「廃坑のクリア率が上がりすぎないか」も**同じ表に併記**すること。

---

## 8. 受入条件 — `tools/probe_s2_clear.js`

### §0 装置(先に母集団を確かめる)

- **(0a)** 酒場側 `isRecruitOn()` / `recruitCountOf(sc)` が腕どおり(#8 の装置 assert 1・2 と同じ)
- **(0b)** 着地 URL が `/index.html` を含み、`scenarioId === "bandits-forest"`
- **(0c)** **「何走行を集計できたか」を必ず 1 行出す。** 0 件でも「差が無かった」に見える
- **(0d)** ⭐⭐⭐ **停滞の検出器が空振りしていないことの母集団ガード** —
  `debugReport` が **1 走行でも読めている**こと(`runs.length + (current?1:0) ≧ 1`)。
  ⚠ これが無いと「`aborted` 0 件」が「診断が動いていない」と区別できない(§2-9 は 0 件だった)
- **(0e)** 入場 Lv が腕の期待値どおり(XP 焼きが効いているか)

### §1 決着の分類

- **(1a)** 各走行の決着が `clear` / `defeat` / `stall` / `timeout` のちょうど 1 つに落ちる
- **(1b)** ⭐ **2 経路で突き合わせる**: ページ側の `gameOver` / `dungeonCleared` と、
  `debugReport.runs[].outcome` が**一致する**。⛔ 片方の写経にしない
- **(1c)** `stall` と数えた走行は `violations` に `stall` か `run-timeout` を**必ず持つ**

### §2 主人公落ちの計数

- **(2a)** 敗北した走行ごとに「生存していた仲間の数」を出す
- **(2b)** `isPartyWiped()` を**本番の関数で呼んで**記録する(ドライバ側で式を写経しない)
- **(2c)** 主人公の被ダメージ比が `0 ≦ r ≦ 1` に収まる(近似の破綻検知)

### §3 恒等(非退行)

- **(3a)** 本番ファイルの差分が **0 バイト**
  (`git diff --stat -- index.html tavern.html audio.js js/` が空)
- **(3b)** 既存 golden(**2026-08-23 時点の記録**・⚠ 走らせて違ったら期待値を書き換える前に理由を突き止める):
  `driver_grid_s2` **111** / `driver_graph_p6` **245** / `driver_spawn_not_on_gate` **51** /
  `driver_encounter_mopup` **36** / `verify_recruit_size` **82** / `driver_bgm_town` **17**

### ⛔ 測らないこと

- **難易度の「良し悪し」**。本チケットは数値を出すだけで、どれを直すかは決めない
- **沼 `lizard-swamp`**(ユーザー決定でスコープ外)
- **n7 の間欠停止の原因**(#16 の宿題 1・別チケット)。本チケットは**数えるだけ**

### 負のコントロール(`--negative` で道具に内蔵する。赤くならなければ exit 1)

| 変異 | 注入する欠陥 | 赤くなるべき節 |
|---|---|---|
| `norecruitarm` | OLD 腕から `&recruit=0` を外す | (0a) |
| `nodiag` | 配信スナップショットから `?autoplay` を落として診断を眠らせる | **(0d)** ⭐ §2-9 の罠(0 件と未起動の見分け)を機械化したもの |
| `wipeblind` | 「仲間の生存数」を常に 0 と報告させる | (2a)/(2b) |
| `stallblind` | `outcome:"aborted"` を `defeat` に丸める | (1b)/(1c) |
| `xpseedoff` | XP 焼きを黙って無効化する | **(0e)** |

⭐ `nodiag` は §2-9 で実際に踏みかけた罠の再現。**「0 件」を出す検出器は母集団ガードと対**。
⚠ 変異は**配信スナップショットをメモリ上で差し替えて**作る。本番ファイルを書き換えない。

### 実行例

    node tools/probe_s2_clear.js --arm base --pairs 10
    node tools/probe_s2_clear.js --arm xp:1890 --pairs 10
    node tools/probe_s2_clear.js --arm qs:dndrange=0 --pairs 10
    node tools/probe_s2_clear.js --negative

---

## 9. 実機/実感の確認

⛔ **本チケットには無い。** 本番の振る舞いを変えないので、実機で見て変わるものが何も無い。
⭐ 実感が要るのは**次のチケット**(§7 の候補から採る 1 本)から。
⚠ そのとき http 起動が必須(`file://` では音が鳴らない)。

---

## 10. changelog

**不要**(§2-10)。`tools/` と `実装依頼書/` しか触らないので `check_changelog.py` は鳴らない。
⛔ 鳴らせるために本番ファイルを触らないこと。

---

## 11. やらないこと

- ⛔ **ゲームの数値・データ・座標の変更**(調査のみ。ユーザー決定 2026-08-23)
- ⛔ **沼 `lizard-swamp` の測定**(同じ ★★☆ だが別チケット)
- ⛔ **n7 の間欠停止の原因究明**(#16 の宿題 1・別チケット。本チケットは**数える**だけ)
- ⛔ **`index.html` へ調査用の計測シームを置くこと**(§2-9 で不要を実測済み)
- ⛔ **`実装依頼書/README.md` への行追加**(承認後に足す)。用意してある行:

    | 18 | [2026-08-23_scenario2-clear-rate.md](2026-08-23_scenario2-clear-rate.md) | **承認済** | 0% | シナリオ2 のクリア率 0% の**調査**(本番は 1 バイトも変えない)。⭐⭐⭐ 着手前実測 25 走行で **「全滅」ではなく「主人公 1 人の死」**だと判明(仲間生存 中央 3/3・Lv1 では仲間の被弾ゼロ)。⭐ **推奨 Lv 4 まで上げても 0/12**。⭐ 廃坑は同じ Lv1 で **2/3 クリア**(入場時に場に居る敵 **2 体** vs シナリオ2 **10 体**)。⚠ #8 の「n1 で折れる」は #16 の畳みで**失効**。⚠ #16 の「間欠停止」は本測定台では **25 走行 0 件** |

---

## 12. 実装結果

**完了 (2026-08-23) — `faaaa61` (STEP1 の道具) / 測定 84 走行。⛔ 本番は 1 バイトも変えていない。**

### 12-1. STEP1 — `tools/probe_s2_clear.js` (新規 924 行)

装置 assert **12 本**・負のコントロール **6 本**。`sweep_recruit_balance.js` を土台にし、
足したのは観測項目だけ (酒場 → `departToScenario()` の本番導線は不変)。

**負のコントロールは 6/6 すべて期待どおり赤くなった** (`--negative`):

| 変異 | 赤くなった節 | 実際に出た値 |
|---|---|---|
| `norecruitarm` | `0a_recruitArm` | `recruitOn:true` (期待 false) |
| `nodiag` | `0d_diagAlive` | `0` (診断が 1 走行も記録していない) |
| `xpseedoff` | `0e_entryLevel` | `1` (期待 Lv2) |
| `stallblind` | `1b_outcomeXcheck` | `page:"running"` vs `report:"defeat"` |
| `stallviolblind` | `1c_stallHasViolation` | `outcome:"stall"` なのに `viol:[]` |
| `wipeblind` | `2b_wipedIdentity` | `wiped:false` なのに `alliesAlive:0` |

⭐⭐⭐ **§8 の `stallblind` は書かれたままでは検査力ゼロだった。**
「`aborted` を `defeat` に丸める」変異は、**丸める対象の `aborted` が存在しない**と何も起きない。
§2-9 の実測どおりシナリオ2 は自然には停滞しない (本測定でも 84 走行 **stall 0 件**) ので、
配信スナップショットで `STUCK_ABORT_MS` を 90000 → **1ms** に縮めて**停滞を人工的に起こしてから**
目隠しする形へ変えた。さらに (1b) と (1c) は別の目隠しでないと両方赤くならないので 2 本に割った。
⇒ 依頼書の 5 本 → **6 本**。

**受入条件の結果**

- **(3a) 本番の差分 0 バイト** ✅ — 道具が**毎回起動時に** `git diff HEAD --stat -- index.html
  tavern.html audio.js js/` を引き、空でなければ測らずに `exit 2`。⭐ 依頼書の `git diff` より
  厳しく **staged も拾う** (`HEAD` を付けた)。84 走行すべてで「空 ✓」。
- **(3b) 既存 golden** ✅ **6/6 すべて記録どおり**:
  `driver_grid_s2` **111/111** / `driver_graph_p6` **245/245** / `driver_spawn_not_on_gate` **51/51** /
  `driver_encounter_mopup` **36/36** / `verify_recruit_size` **82/82** / `driver_bgm_town` **17/17**
- **(0c) 母集団ガード** ✅ 全腕で「集計できた走行 n/N」を出力。装置 assert 崩れ **0 件** /
  pageerror **0 件** / stall **0 件** / timeout **0 件**。

### 12-2. ⚠⚠ 実装中に崩れた依頼書の前提 2 件

| 依頼書の前提 | 実測 | 影響 |
|---|---|---|
| §6「`?dndrange=0` / `?mopup=0` / `?s2fold=0` は既存の撤退スイッチなので**そのまま腕に使える**」 | **`departToScenario()` は `autoplay` と `evade` しか index へ引き継がない** (`tavern.html:5413-5421`)。3 つとも index 側で `location.search` を読む IIFE なので、**酒場の URL に付けても届かない** | 道具側で `evaluateOnNewDocument` + `history.replaceState` を使い、**着弾前に** `location.search` を書き換える。届いたかは装置 assert **0f** が本番の `RANGE_LEGACY` / `MOPUP_OFF` / `S2_FOLD_OFF` を裸の識別子で読んで検算する |
| §4-6「終了時の残敵数と、**ボスを倒したか**」を敵インスタンスから読める | **`isBoss` はインスタンスでなく `def` に載っている** (`index.html:15671` の `enemy.def && enemy.def.isBoss` が本番の読み口) | `e.isBoss` で読むと**全走行「ボス 0 体」**になり、§2-4 の「ボス撃破後に掃討しきれず負ける」が丸ごと見えなくなる。スモーク 1 走行で実際に踏んで修正 |

### 12-3. STEP2/STEP3 — 測定結果 (9 腕 / 85 走行・集計 83 + スモーク 1 = **84**)

すべて 2026-08-23 / HEAD = `faaaa61` (本番の差分ゼロ) / `--workers 3` / autoplay=15。
⚠ 腕は掛け合わせず**1 本ずつ既定と比べた** (§6 のとおり)。

| # | 腕 | n | **クリア** | 主人公落ち | 真の全滅 | 死亡時の仲間生存(中) | 入場Lv | 主人公maxHP | **被弾比(中)** | 同時交戦 中/最大 | 残敵(中) | ボス撃破 | 到達ノード | XP(中) | 秒(中) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| A | `base` | 10 | **0%** | 10/10 | **0** | 2/2 | 1 | 30 | 0.80 | 8 / **12** | 6 | 0/10 | n7×10 | 1190 | 113.5 |
| B1 | `xp:1890` (Lv2) | 10 | **10%** | 9/9 | **0** | 2/2 | 2 | 40 | 0.80 | 6 / 11 | 4.5 | 1/10 | n7 clear×1 | 1100 | 93.5 |
| B2 | `xp:3000` (Lv3) | 10 | **0%** | 10/10 | **0** | 1.5/2 | 3 | 50 | 0.70 | 8 / 10 | 4 | 0/10 | n7×10 | 1350 | 118 |
| B3 | `xp:6000` (Lv4=推奨) | 10 | **10%** | 9/9 | **0** | 2/2 | 4 | 60 | 0.70 | 7 / 11 | 3 | 1/10 | n7 clear×1 | 1515 | 123.5 |
| C | `mine` (廃坑1周・装備込) | **3**/5 | **0%** | 3/3 | **0** | 1/2 | 2 | 40 | 0.60 | 9 / 9 | 4 | 0/3 | n7×3 | 1440 | 123 |
| D1 | `qs:recruit=0` (計4人) | 10 | **0%** | 10/10 | **0** | 2.5/3 | 1 | 30 | **0.50** | 8.5 / **12** | 4.5 | 0/10 | n7×10 | 1290 | 127 |
| D2 | `qs:dndrange=0` | 10 | **0%** | 10/10 | **0** | 1.5/2 | 1 | 30 | 0.60 | 6.5 / 8 | **3** | 0/10 | n7×10 | **1715** | 126 |
| D3 | `qs:mopup=0` | 10 | **0%** | 10/10 | **0** | 2/2 | 1 | 30 | **0.90** | **4** / 9 | 5 | 0/10 | n7×10 | 1100 | 93 |
| D4 | `qs:s2fold=0` (8ノード版) | 10 | **0%** | 10/10 | **0** | 2/2 | 1 | 30 | 0.60 | 5 / 8 | 4 | 0/10 | **n1×8** / n7×2 | **275** | **23.5** |

**合計 84 走行: クリア 2 (2.4%) / 敗北 82。主人公落ち 82/82 = 100%。真の全滅 (`isPartyWiped()`) 0/82 = 0%。**
被弾比の全体分布: 中央 **0.702** / 最小 0.385 / 最大 1.000。⭐ **19/84 走行が比 1.000**
= その走行では**仲間が 1 ダメージも受けず主人公だけが全被弾を負った**。

### 12-4. ⭐⭐⭐ 測って初めて分かったこと (依頼書の想定と違ったもの)

**① 「畳んだこと」は原因ではない。8 ノード版のほうが**もっと**悪い。**
§2-6 は「畳んだせいで 10 体が同室に立っている」を候補として挙げ、⚠ 付きで「即断するな」と書いていた。
実測は逆だった。`?s2fold=0` は **10/10 が n1 で折れ**、所要 **23.5 秒**・XP **275**・入場時に場に居る敵 **0**。
畳んだ既定版は n7 まで進んで 113.5 秒・XP **1190**。
⇒ **#8 の「n1 で折れる」は畳む前の姿として今も正確に再現する**し、**#16 の畳みは改善だった**。
⛔ 「場に居る敵の数を減らす」方向 (§7 候補 5) は、この腕で見るかぎり**逆効果**。

**② 推奨 Lv4 は 0% ではない。ただし 10% で頭打ち。**
§2-5 の「Lv4 でも 0/12」は N=3 の観測だった。N=10 で **1/10**。⭐ Lv2 でも **1/10** 出ている
(Lv3 は 0/10)。**主人公 maxHP を 30 → 60 (2 倍) にしても 10% を超えない**という §2-5 の結論
自体は変わらないが、「まったく動かない」ではなく「**わずかに動くが壁は破れない**」が正確。

**③ §2-4 の「ボス撃破後に残敵を掃討しきれず敗北」は 84 走行で 1 件も再現しなかった。**
ボス撃破は **2 件だけで、2 件ともクリア走行**。⇒ 現状のシナリオ2 は
「掃討で詰む」より**前に**、スカーへ到達する前後で主人公が落ちている。
⚠ §7 候補 4 (勝利条件をボス撃破で終了へ) の**効き目は、今回の母集団では測れていない**
(効く場面が発生していない)。

**④ 装備を本物にしても動かない (腕 C)。**
廃坑を実際に 1 周させた 3 走行 (XP + 金貨 + 装備すべて本物・入場 Lv2) も **0/3**。
XP 焼きだけの `xp:1890` (1/10) と有意な差なし。⇒ **§2-5 の「B は実プレイより弱い側に寄っている」
という留保は、結果を変えるほどではなかった。**
⭐ 同じ走行で測れた**廃坑の Lv1 クリア率 = 3/5 (60%)** (§2-6 の 2/3 と整合)。

**⑤ 人数を増やすと被弾は分散するが、クリア率は動かない。**
`qs:recruit=0` (計 4 人) で被弾比が **0.80 → 0.50** へ下がり、死亡時の仲間生存も 2/2 → 2.5/3 へ増える。
⭐ **「主人公が壁になっている」ことの直接の裏付け**だが、それでも **0/10**。
⇒ #8 の「募集人数では動かない」は本測定でも成立。

**⑥ 同時交戦の最大は 12 (§2-7 の推定 11 を上回る)。**
内訳 = 盗賊重装兵×3 + 盗賊アーチャー×3 + 盗賊魔術師 + スカー + 盗賊×4。
⇒ **門の群・焚き火の群・ボス・スカーの召喚が 1 つの戦闘に畳まれている**ことの直接証拠。
`?dndrange=0` で最大 12 → **8** に割れ、残敵が 6 → **3**・XP が 1190 → **1715** まで改善する
(= **9 腕で一番遠くまで進む**) が、クリア率は **0%** のまま。
⚠ この腕は**敵だけでなく味方の射程も旧仕様へ戻す**ので、片側だけの実験ではない。

**⑦ 停滞 (#16 の宿題 1) は本測定でも 0 件。**
84 走行すべてで `outcome:"aborted"` **0 件** / `violations` の `stall`・`run-timeout` **0 件** /
打切 **0%**。⭐ ただしこの 0 件は **assert 0d (診断が生きている) が全走行で緑**だったうえでの
0 件であり、`nodiag` 変異を入れると 0d が実際に赤くなることも確認済み。
⇒ **「検出器が眠っていた」可能性は潰してある。**

### 12-5. STEP4 — 候補の一覧 (⭐ 順位は付けたが**決めていない**。採否はユーザー判断)

| # | 候補 | 触る場所 (予定) | **本測定で出た効き目** | 6 シナリオへの波及 |
|---|---|---|---|---|
| **1** | **敗北条件** — 主人公が倒れても仲間が生きていれば継続 (気絶扱い) | `onHeadDowned` (`index.html:17949`) / `ally.isHero` 4 箇所 (`:24522` `:27128` `:29648` `:31518`) | ⭐⭐⭐ **82/82 の敗北すべてで仲間が生きていた**。`isPartyWiped()` は **0/82**。9 腕どれでも例外なし。**現状の敗北条件が壁そのもの** | ⚠ 全 6 シナリオに効く。廃坑の 60% が上がりすぎないかを同時に測ること |
| **2** | **被弾の配分** — 前衛バイアス / 隊列 / 主人公を壁にしない | `zonePullFor` (`index.html:24724`) / `pickEnemyTarget` (`:24735`) | ⭐⭐⭐ 被弾比 中央 **0.702**、**19/84 走行で 1.000** (仲間の被弾ゼロ)。人数を 4 人へ増やすと 0.50 まで下がる = **主人公が壁になっている**ことの直接証拠 | ⚠ 全 6 シナリオに効く |
| **3** | **交戦のまとまり** — 門と焚き火を別の戦闘へ | シナリオ2 のスポーン座標 or `engagePx` (`index.html:18290`) | ⭐⭐ 同時交戦 **最大 12**。`?dndrange=0` で 8 へ割れると残敵 6→**3**・XP 1190→**1715** (9 腕で最遠) だが **クリア率は 0% のまま** | シナリオ2 限定にできる |
| **4** | **勝利条件** — ボス撃破で終了 (全滅要求をやめる) | `checkDungeonClear` (`index.html:17746`) | ⚠ **今回の母集団では効き目を測れていない** (ボス撃破かつ敗北 = **0/84**)。⛔ §2-4 の 1 走行は再現しなかった | ⚠ 全 6 シナリオに効く |
| **5** | **敵の質・数** — `banditHeavy` 3 体 / `scar` の召喚 | シナリオ2 のデータ | ⛔ **逆効果の証拠が出た**。`?s2fold=0` (1 部屋の敵を散らす) は **n1 で 23.5 秒で全滅**・XP 275 | シナリオ2 限定 |
| **6** | **入場レベル / 装備** — 廃坑の周回誘導・推奨 Lv の提示 | 酒場 | ⭐ Lv1→Lv4 (maxHP 30→60) で **0% → 10%**。装備を本物にしても (腕 C) **0/3**。**壁は破れない** | 全シナリオ |

⭐⭐⭐ **数値がいちばん強く指しているのは候補 1 + 2 の組。**
「主人公 1 人の死 = 即敗北」(候補 1) と「その主人公が全被弾を独占する」(候補 2) は
**同じ現象の表と裏**で、9 腕どの軸を動かしても例外が 1 件も出なかった。
⛔ ただし本チケットは**どれを採るかを決めない**。次のチケットで 1 本選び、
そのとき **廃坑のクリア率 (本測定で Lv1 = 3/5) が上がりすぎないか**を同じ表へ併記すること。

### 12-6. 未着手のまま残したもの

- ⛔ **沼 `lizard-swamp` の測定** (ユーザー決定でスコープ外・別チケット)
- ⛔ **n7 の間欠停止の原因究明** (#16 の宿題 1・別チケット)。⭐ 本測定では **84 走行 0 件**で、
  検出器が生きていることも確認済みなので、**そもそも再現しない可能性が高い**
- ⛔ **是正そのもの** (調査チケットなので直さない)

### 12-7. 道具の使い方 (次のチケット用)

    node tools/probe_s2_clear.js --arm base            --runs 10 --workers 3
    node tools/probe_s2_clear.js --arm xp:1890         --runs 10 --workers 3
    node tools/probe_s2_clear.js --arm qs:dndrange=0   --runs 10 --workers 3
    node tools/probe_s2_clear.js --arm mine            --runs 5  --workers 3
    node tools/probe_s2_clear.js --negative

⚠ 是正チケットで本番を変えると **(3a) が `exit 2` で止める**。是正後の測定用に
`--allow-dirty` 相当のスイッチは**わざと用意していない**。比較したいときは
**変更前の commit を checkout して測る**か、`productionDiff()` の呼び出しを
是正チケットの側で**明示的に**外すこと (⭐ 黙って外さない)。
