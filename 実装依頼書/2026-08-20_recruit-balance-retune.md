# 募集人数の変更に伴う 難易度 / XP の再調整(測ってから決める)

**ステータス**: 実装中(2026-08-23 本番 80 走行 完了 → **§9**。調整は廃坑の `recruit: 3` のみ = ユーザー決定)
⚠ **2026-08-23 の着手前実測で本文を全面改訂した。** 旧本文の測定計画は本番のコードでは成立しない。
**依存**: ✅ #7 `2026-08-20_quest-recruit-size.md` 完了 `459c34a` / ✅ #11 `2026-08-22_bandit-hideout-map-s2.md` 完了 `f62f145`
**撤退スイッチ**: なし(値の調整。撤退は `?recruit=0` で #7 ごと戻す)

---

## 0. 着手前実測 (2026-08-23) — 旧本文のどこが崩れたか

⭐⭐⭐ 承認が出る前に「依頼書が主張する真因」を本番の関数で 1 回測った(#4 / #10 / #12 と同じ作法)。
道具は使い捨てのプローブ 2 本(`probe_partysize.js` / `probe_rig_tavern.js`、scratchpad)。

| # | 旧本文の主張 | 実測 | 判定 |
|---|---|---|---|
| 1 | `?recruit=0`(旧) と既定(新) を**既存ドライバで**交互に回して対比較する | 既存ドライバは `index.html` を直起動する → 両腕とも**計 4 人** | ⛔ **空振り** |
| 2 | 廃坑 3→1 / 森 3→2 / 沼 3→2 / 砦・神殿・竜巣 3 | `recruitCountOf()` = ★カウント → `clamp(1,3)`。表どおり | ✅ 正 |
| 3 | XP が頭割りされるか要確認 | **されない**(§0-4) | ✅ 判明 |
| 4 | 変更箇所 `tavern.html:2085` | 実際の `difficulty:` 行は 2107 / 2130 / 2150 / 2170 / 2190 / 2210 | ⚠ 行番号が腐り |
| 5 | `tools/sweep_recruit_balance.js` を新規作成 | 未実在 = 新規で正しい | ✅ 正 |
| 6 | (旧本文に無い) 4 試行中 3 回全滅は #7 の人数減が原因 | **旧仕様の計 4 人での結果だった**(§0-3) | ⛔ **前提が誤り** |

### 0-1 なぜ `?recruit=0` が空振りするのか

人数を決める `isRecruitOn()` / `recruitCountOf()` は **`tavern.html` にしかない**。
`index.html` は `sessionStorage["dragonfighters.partyMembers"]` を読むだけで、
無ければ `orderFormation(buildParty(heroOld))` = `PARTY_SIZE`(定数 4)へ**無言でフォールバック**する。

既存のヘッドレスドライバは全部 `index.html` を直起動する:

- `tools/auto_debug_run.js` — `index.html?autodebug=N&autoplay=...`
- `tools/probe_n4_stall.js` — `index.html?autoplay=...&diag=1`

→ URL に `recruit=0` を足しても `index.html` の誰も読まない。実測:

```
A: index直起動 (既存ドライバ)      allies=3 (計4人)  ss=null
B: index直起動 + ?recruit=0        allies=3 (計4人)  ss=null   ← A と同じ
```

⚠ `tools/verify_recruit_size.js`(#7 の検証・79/79)は **`tavern.html` だけ**を駆動し、
`index.html` への遷移を**横取りで abort** している。つまり
**人数を減らした状態でダンジョンを 1 回もプレイしていない。**

### 0-2 正しい測定台 = 酒場を本番どおり通して遷移させる(実測で成立)

`departToScenario()` は `tavern.html` の URL から `autoplay` を読んで
`index.html?autoplay=N` へ引き継ぐ。だから **酒場側に `?recruit=0` を付ければ腕が本当に割れる**。

```
tavern.html?autoplay=15&recruit=0   → prepScenario=sc; regeneratePartyMembers(); departToScenario()
                                    → index.html?autoplay=15  (遷移は横取りしない)
```

実測(シナリオ2・各腕 1 走行):

```
OLD (?recruit=0)  酒場: recruitOn=false  recruitCountOf=2 → index側 allies=3 (計4人) 仲間Lv=[1,1,1]
NEW (既定)        酒場: recruitOn=true   recruitCountOf=2 → index側 allies=2 (計3人) 仲間Lv=[1,1]
```

⭐⭐⭐ **この経路でなければならない理由**: `sessionStorage` へ手作りの `partyMembers` を
注入する案でも人数は作れる。しかし仲間の **Lv は `assignCompanionLevels()`、装備は `allyEquip`** が
決めており、手作りでは本番と別物になる。**人数だけ合った偽の編成で難易度を測ることになる。**

⚠⚠ しかも手作り注入は**無言で失敗する**。実測で `classKey:"wizard"`(本番の `PARTY_ZONES` に無い)を
1 つ混ぜたら、index 側の `pm.every(m => PARTY_ZONES[m.classKey])` が false になり
**formation ごと捨てられて 4 人に戻った**のに、人数だけ見ると「旧仕様どおり」に見えて緑になった。

### 0-3 前回窓の「4 試行中 3 回全滅」は #7 のせいではない

あの 4 試行は `tools/probe_n4_stall.js`(= `index.html` 直起動)で採った。
`partyMembers` を仕込んでいないので **全部が計 4 人 = 旧仕様の編成**だった。

⭐⭐⭐ つまり **シナリオ2 は #7 以前から 4 人でも全滅する**。#7 はそこへ上乗せするだけ。
「C(#7) が持ち込んだ問題」という旧本文の枠組みは、少なくともシナリオ2 については誤り。
**既存の難易度と #7 の上乗せを分けて測る**(§3-3 の腕はそのために 2 本ある)。

### 0-4 XP は頭割りされない(旧本文「先に確かめること」への答え)

`gainXP(amount, source)` は主人公の `currentTotalXp` / `earnedXpThisRun` に足すだけ。
パーティ人数で割る処理はどこにも無い。呼び口は 4 箇所だけ:

| 場所 | 内容 |
|---|---|
| `index.html` の敵撃破処理 | `gainXP(enemy.def.xp \|\| 0, nm)` |
| 罠解除 ×2 | `gainXP(spec.disarmXP, "罠解除")` |
| クリア報酬 | `gainXP(clearXp, "クリア報酬")` |

⭐ **人数は XP 量に一切影響しない。** クリアできれば同額、できなければゼロ。
→ **XP の調整は人数問題と切り離してよい**(残るのは §2 の宿題 2 = 廃坑の既存宿題だけ)。

---

## 1. 目的

依頼書 #7 でパーティ人数がクエストごとに変わった。**その結果どうなったかを実測し、値を決める。**

⚠ **この依頼書のゴールは「調整すること」ではなく「測ってから決めること」。**
測った結果「調整不要」も正式な結論として認める。

---

## 2. この依頼書が回収する宿題は 3 つ(混ぜない)

| # | 宿題 | 出所 |
|---|---|---|
| 1 | #7 で NPC が減ったぶん(廃坑 3→1、森・沼 3→2) | 本依頼書 |
| 2 | 廃坑の既存宿題 — 卓上グリッド P9 で寄り道 4 か所と宝箱を足したぶんの未調整 | #7 と無関係 |
| 3 | #11 でシナリオ2 の n7 を大部屋化し **敵を 5 体 → 9〜10 体**に増やしたぶん | `実装依頼書/README.md` |

⚠ 3 つを 1 つの数字で語らない。**どの宿題に効いた調整なのかを毎回書く。**

---

## 3. 測り方(全面改訂)

### 3-1 測定台 — 新規 `tools/sweep_recruit_balance.js`

⛔ **既存の `auto_debug_run.js` / `probe_n4_stall.js` を流用しない**(§0-1 の理由で腕が割れない)。

1 走行の手順:

1. `evaluateOnNewDocument` で `dragonfighters.*` / `df.*` を purge し、
   `dragonfighters.prologueSeen = "1"` を焼く(前口上は測定対象外)
2. `tavern.html?autoplay=<N>` (+ OLD 腕だけ `&recruit=0`) を開く
3. **本番の関数だけ**を呼ぶ(写経しない):
   `prepScenario = scenarios.find(s => s.id === <id>)` → `regeneratePartyMembers()` → `departToScenario()`
4. **遷移は横取りしない。** `index.html?autoplay=<N>` へ着地するのを
   `typeof mapData !== 'undefined' && typeof heroAI === 'function'` で待つ
5. 1 秒間隔でポーリングして走行を観測(⭐ 150ms の evaluate は測定対象そのものを遅くする = P9 の教訓)

### 3-2 装置 assert(これが無いと全部緑になる)

1 走行ごとに必ず記録し、1 つでも崩れた走行は**集計から外して件数を報告する**:

| 装置 assert | 崩れると何が起きるか |
|---|---|
| 酒場側の `isRecruitOn()` が腕どおり(OLD=false / NEW=true) | 腕が割れていないのに数字が出る |
| 酒場側の `recruitCountOf(sc)` が期待値 | ★の読み違いに気づけない |
| 着地 URL が `/index.html` を含む | 遷移していないのに「走った」ことになる |
| index 側の `allies.length + 1` が期待人数 | フォールバックした 4 人を新仕様として数える |
| index 側の `scenarioId` が対象シナリオ | 別のシナリオを測る |

⚠ **母集団ガード**: 「両腕とも N 走行を集計できたか」を最後に 1 行出す。
0 件でも「差が無かった」に見えるので、**件数を出さない集計は禁止**。

### 3-3 腕(2 本)

| 腕 | URL | 意味 |
|---|---|---|
| OLD | `tavern.html?autoplay=N&recruit=0` | #7 以前(NPC 3 = 計 4 人)。**§2 の宿題 2・3 だけが乗った状態** |
| NEW | `tavern.html?autoplay=N` | #7 以後(★の数)。**宿題 1 が上乗せされた状態** |

⭐ 2 本を**交互に**回す(同一ビルド・同一マシン負荷で対にする)。
⭐ **OLD 腕そのものが「既存の難易度」の測定値**になる。ここが既に悪ければ #7 とは別の話。

### 3-4 指標

⚠ **所要「秒数」で測らない**(マシン負荷とポーリング間隔で揺れる)。

| 指標 | 取り方(裸の識別子で読む) |
|---|---|
| クリア率 | `dungeonCleared` が true で終わった走行 / 全走行 |
| 全滅率 | `gameOver` が true で終わった走行 / 全走行 |
| 到達ノード | 終了時の `currentNodeId`(**どこで折れたか**が本体。シナリオ2 は n1 / n4 で割れる) |
| 仲間の生存 | 終了時の `allies.filter(a => a.hp > 0).length` / `allies.length` |
| 獲得 XP(中央値) | `earnedXpThisRun` |
| 主人公 HP | 裸の `hp`(⚠ `playerHp` という識別子は**存在しない**。実測で `null` を踏んだ) |

### 3-5 規模(ユーザー決定 2026-08-23)

**シナリオ2 `bandits-forest` と 廃坑 `goblin-mine` の 2 本に絞る。各 N = 20 ペア。**

⭐ 残り 4 本(沼・砦・神殿・竜巣)を測らない理由:

- `lizard-swamp` は ★2 で 3→2 だが、シナリオ2 と同じ変化量。まず 2 本で効き目を見てから判断する
- `orc-fort` / `undead-temple` / `dragon-lair` は **#7 で人数が変わらない**(★3 以上 → clamp で 3 のまま)
  → **宿題 1 の影響が原理的にゼロ**。測っても差は出ない
- 闇市の生成クエストは `difficulty` を持たず fallback 3 で**これも人数不変**

⚠ 1 走行の実測レンジは **17〜206 秒**(全滅は速い)。2 本 × 20 ペア × 2 腕 = 80 走行。
並列ページを使ってよいが、**ポートは腕ごとに別**にする(⚠ 同時実行のポート衝突は偽の赤を生む)。

### 3-6 スイープ中の禁止事項

- ⚠⚠ **スイープ中に `tavern.html` / `index.html` を編集しない。** 前半と後半で別ビルドの混合物になる
- ⚠⚠ background で回す場合 **`TaskStop` では止まらない**(bash ループと node が生き残る)。
  実測して殺すこと。出力は**毎回新しいファイル名**に書く
- ⚠ 別窓が **#12 港町マップ**を並行進行中。`tools/make_grid_map.py` / `assets/town_phlan.jpg` に触らない。
  `git add .` 禁止 → **ファイル単位で add** し `git show :<path>` でステージを読んでから commit

### 3-7 読み取りに使う裸の識別子(実測済)

⭐⭐⭐ `window.<名前>` では読めない(classic script 直下の `let` は window に載らない)。
`page.evaluate` の中で**裸の識別子**として読む: `allies` / `partyComposition` / `scenarioId` /
`currentNodeId` / `gameOver` / `dungeonCleared` / `hp` / `earnedXpThisRun` / `PARTY_SIZE`。
酒場側は `scenarios` / `prepScenario` / `regeneratePartyMembers` / `departToScenario` /
`isRecruitOn` / `recruitCountOf`。

### 3-8 測定台の実装 (2026-08-23) — できたこと・実測で分かったこと

**`tools/sweep_recruit_balance.js` を新設。§3-1〜3-7 をそのまま実装した。**
本番 80 走行は **#13 が着地してから**回すこと(§3-6 の理由)。

```bash
node tools/sweep_recruit_balance.js --scen bandits-forest --pairs 2      # スモーク
node tools/sweep_recruit_balance.js --scen all --pairs 20 --workers 2    # 本番 (80 走行)
node tools/sweep_recruit_balance.js --scen goblin-mine --pairs 1 --negative
```

✅ **腕は割れた**(スモーク実測): シナリオ2 OLD=計4人 / NEW=計3人、廃坑 OLD=計4人 / NEW=計2人。
✅ **装置 assert 5 本は空回りしていない**。`--negative`(OLD から `&recruit=0` を外す)で
   assert 1 `isRecruitOn(got=true want=false)` と 4 `partySize(got=2 want=4)` が**赤くなる**ことを実測。
   赤くならなかったら exit 1 で落ちる = 負のコントロールが道具に内蔵されている。

#### ⚠⚠⚠ 本番走行の前に知っておくこと(全部 N=2 スモークの実測)

1. **`allies` は走行中に増える。** 廃坑の囚われの従者 `joinServantAlly()`(`index.html`、
   `sce1CaptiveState` の救出イベント)と闇市の召喚 `executeSummon()` が `allies.push` する。
   → **開始 3 人が終了 4 人になる**。⭐⭐⭐ 「仲間の生存」の分母は**終了時**から採ること。
   開始の分母と終了の分子を混ぜると **`4/3`** という有り得ない数が出る(実際に出した)。
   → **人数差の一部は救出で自然に埋まる**。★1 の廃坑で NEW=計2人 でも、従者を助ければ計3人。
     「★1 が最も過酷になる」という #7 の懸念は、この合流ぶんを差し引いてから判断すること。
2. **全滅走行でも仲間は生きている。** ゲームオーバー判定は**主人公の死**だけを見るので、
   `生存=3/3` かつ `HP=0` で `wipe` になる。→ **「仲間の生存」は難易度の指標として鈍い**。
   効くのは**到達ノードと XP**。
3. **1 走行の上限は 300 秒では足りない。** 廃坑 OLD(計4人)が 241 秒でもまだ n1 を戦っていた。
   既定を **420 秒**にした。⚠ 打切は「クリアでも全滅でもない」ので、集計表に**打切列を必ず出す**
   (無いと打切が「クリアしなかった」に混ざる)。
4. **到達ノードは思ったより手前で割れる。** シナリオ2 は n1(入口の次)での全滅が多く、
   n7 まで行けたのは OLD の 1/2 だけだった。廃坑は 2 ノードなので `n1(clear)` / `n1` の 2 値。

#### 期待値表はドライバ側が独立に持つ

`SCEN_TABLE`(`goblin-mine` = ★1/NPC1、`bandits-forest` = ★2/NPC2)は
`recruitCountOf()` の clamp を**写経していない**。ページから読んだ `sc.difficulty` の★の数と
突き合わせるので、仕様を変えたら赤くなる。⚠ 表に無いシナリオを `--scen` に渡すと
**わざと exit 2 で止まる**(表を足さずに測ると「★の読み違い」に気づけないため)。

---

## 4. 調整の順序(この順を守ること)

### 1. まず測る(調整しない)

§3 のペア比較を 2 本回し、**表にしてユーザーへ報告する**。
**ここで一度止まる。** 何をどう動かすかはユーザーの判断を仰ぐ。

### 2. 敵の「質と配置」で調整する(数ではない)

⚠ **このプロジェクトでは「敵を増やす / 減らす」がバランスに効かないことが実測で分かっている**
(`±1 体の護衛調整は N=60 でもノイズ以下` — シナリオ3 の実測)。
効くのは**敵の種類・配置・ボスのロジック**。

調整の候補(効き目の大きい順):

1. `recruit:` の個別上書き — **最も安全。** 廃坑だけ `recruit: 3` にすれば元に戻る
2. 敵の構成(エリート個体の有無・遠距離役の数)
3. 湧き位置・寄り道スポットの配置
4. ボスのロジック(HP ではなく行動)

⚠ シナリオ2 は **n1 と n4 で折れている**(実測)。ボス部屋 n7 の調整では届かない可能性が高い。
**どのノードで折れたか**を見てから触る場所を決める。

### 3. XP は最後

§0-4 で「頭割りされない」ことが確定したので、**人数を理由に XP を動かす根拠は無い**。
動かすとしたら §2 の宿題 2(廃坑に寄り道 4 か所と宝箱が増えたぶん)だけ。

XP テーブルは D&D 3.5 準拠(累積 XP = `500 × Lv × (Lv-1)`、Lv10 = 45,000、
6 シナリオ + 最終 2 周で Lv10 到達想定)。**ここを動かすと全シナリオの設計前提が動く。**
実物は `index.html` の `XP_THRESHOLDS = [0, 1000, 3000, 6000, 10000, 15000, 21000, 28000, 36000, 45000]`。

---

## 5. 変更範囲

⚠ **行番号は書かない**(#2 / #4 / #10 / #12 で全部ズレた)。識別子で grep すること。

| ファイル | アンカー | 変更 |
|---|---|---|
| `tavern.html` のシナリオ表 | `difficulty: "★` を grep(6 件) | 該当シナリオへ `recruit:` の**値を入れる**(#7 で作った器を使う) |
| `js/df-mapdef.js` / `tavern.html` の敵スロット | — | §4-2 の結果に応じて。**§4-1 の報告後、ユーザー判断を得てから** |
| `index.html` の `XP_THRESHOLDS` | `XP_THRESHOLDS` | §4-3 の結果に応じて。**原則触らない** |
| 新規 `tools/sweep_recruit_balance.js` | — | §3 の測定台 |

### 触らないと決めたファイル

- `js/save-slots.js` / `title.html` — #5 / #6 と無関係
- `audio.js`
- 敵スプライト・アセット類
- `index.html` の `PARTY_SIZE` — フォールバック専用(#7 と同じ理由)
- `tools/make_grid_map.py` / `assets/town_phlan.jpg` — **別窓 (#12) が編集中**

---

## 6. 受入条件

1. **測定結果の表がユーザーに報告されている**(シナリオ2 + 廃坑、各 N ≥ 20 ペア、§3-4 の 6 指標)
2. **装置 assert の集計行が出ている**(両腕とも何走行が有効だったか。無効走行の件数と理由)
3. 各シナリオについて「調整した / 調整不要と判断した」のどちらかが**理由付きで**記録されている
4. 調整したシナリオについて、**調整後にもう一度ペア比較を回して**、狙った方向に動いたことを示している
5. `goblin-mine` の全滅率が、**OLD 腕(NPC 3 人)と同等以下**になっている
   初見が最初に触る場所なので、ここだけは「同等以下」を条件にする
6. **§2 の 3 つの宿題それぞれについて 1 行の結論がある**(「宿題 3 は n7 に到達した走行が N 件しか無く測れなかった」も可)
7. 既存 golden ドライバ群が全部緑(または赤の理由が 1 本ずつ書かれている)
8. ⚠ `tools/verify_recruit_size.js`(79/79)が緑のまま。
   **もし `recruit:` に値を入れて静的検査 (S9) が赤くなったら、期待値を緩めず測定点を移す**
   (#7 の実装時点で「#8 が値を入れると (S9) は必ず赤」と分かっている)

---

## 7. やらないこと(スコープ外)

- パーティを 5 人以上にする(engine 未検証。別チケット)
- 新しい敵・新しいマップ・新しいギミックの追加
- 難易度選択(イージー/ノーマル)の新設
- `?recruit=0` の撤去(#7 の撤退スイッチは残す)
- 沼・砦・神殿・竜巣・生成クエストのスイープ(§3-5 の理由)
- iOS 実機での体感確認(これはユーザーの担当。数値が揃った後)

---

## 8. リスクと対策

| リスク | 対策 |
|---|---|
| 腕が割れていないのに数字が出る | §3-2 の装置 assert 5 本。1 つでも崩れた走行は集計から外して件数を報告 |
| 差がノイズに埋もれて何も言えない | N ≥ 20 ペア。それでも見えなければ「**影響しなかった**」を結論として書く。無理に動かさない |
| 既存の難易度と #7 の上乗せを混同する | OLD 腕そのものが「既存の難易度」の測定値。§2 の 3 宿題を分けて書く |
| 調整が過剰になり全部やさしくなる | §4-1 で止まってユーザー判断を仰ぐ。**測る前に触らない** |
| スイープ中の編集で混合物ができる | スイープ開始前に作業ツリーを固定。走行中は一切編集しない |
| background スイープが残留して次の測定を汚す | 出力は毎回新ファイル。終了後に**プロセスを実測して殺す** |
| 別窓 (#12) の作業を巻き込む | ファイル単位 add + `git show :<path>` でステージを読んでから commit |

---

## 二重ファイル同期

値の変更のみ。`tavern.html` のシナリオ表と、必要なら敵スロット定義(`js/df-mapdef.js`)。
**`index.html` の `PARTY_SIZE` は触らない**(#7 と同じ理由でフォールバック専用)。

---

## 9. 本番 80 走行の結果 (2026-08-23) — §4-1「まず測る」の産物

```
node tools/sweep_recruit_balance.js --scen all --pairs 20 --workers 2
```

09:07:39 → 10:13:35 (66 分・`--workers 2`)。
生データ: `%TEMP%\df_sweep_recruit_bandits-forest+goblin-mine_20260823011335.json`

### 9-0 装置の健全性(受入条件 2)

| 項目 | 実測 |
|---|---|
| 母集団ガード | シナリオ2 **OLD 20/20 / NEW 20/20**、廃坑 **OLD 20/20 / NEW 20/20** |
| 装置 assert 崩れ | **0 件**(全 80 走行が 5 本すべて通過) |
| pageerror | **0 件** |
| 負のコントロール | 本番の**前に**実施し、狙いどおり赤を確認:<br>`⛔ pair1 OLD: 1_isRecruitOn(got=true want=false) , 4_partySize(got=2 want=4)`<br>`[negative] ✓ 装置 assert は空回りしていない` |

### 9-1 シナリオ2 `bandits-forest`(★2)

| 腕 | 出発 | クリア | 全滅 | 打切 | XP(中央) | 仲間生存(中央) | 主人公HP(中央) | 途中合流 |
|---|---|---|---|---|---|---|---|---|
| OLD | 計 4 人 | **0%** | 100% | 0% | 600 | 3/3 | 0 | 0/20 |
| NEW | 計 3 人 | **0%** | 100% | 0% | 250 | 2/2 | 0 | 0/20 |

- 到達ノード OLD: `n1:14  n4:2  n7:4`
- 到達ノード NEW: `n1:17  n4:1  n7:2`
- ペア比較 20 組 → 両方クリア 0 / OLDのみ 0 / NEWのみ 0 / **両方だめ 20**

### 9-2 廃坑 `goblin-mine`(★1)

| 腕 | 出発 | クリア | 全滅 | 打切 | XP(中央) | 仲間生存(中央) | 主人公HP(中央) | 途中合流 |
|---|---|---|---|---|---|---|---|---|
| OLD | 計 4 人 | **55%** | 45% | 0% | 2,090 | 4/4 | 5.5 | 16/20 |
| NEW | 計 2 人 | **15%** | 85% | 0% | 580 | 1/1 | 0 | 8/20 |

- 到達ノード OLD: `n1:9  n1(clear):11`
- 到達ノード NEW: `n1:17  n1(clear):3`
- ペア比較 20 組 → 両方クリア 2 / **OLDのみ 9** / NEWのみ 1 / 両方だめ 8

### 9-3 読み取り(受入条件 3・6 の材料)

⭐⭐⭐ **廃坑は募集人数が一方向に効いている。** クリア率 55% → 15%、XP 中央 2,090 → 580。
ペア比較でも OLDのみ 9 対 NEWのみ 1。README の警告「7 だけ入れると ★1 の廃坑が
最初で最も過酷になる」が数値で裏付けられた。→ **宿題 1 の結論**。

⚠⚠⚠ **シナリオ2 はクリア率が両腕とも 0% で、この土俵では募集人数の差が測れない。**
ペア比較 20 組がすべて「両方だめ」= **床に張り付いている**。
**これは「差が無かった」ではない**(§8 のリスク表の 2 行目とは別の事象。あちらは
「差がノイズに埋もれた」だが、こちらは**指標そのものが飽和している**)。
差が見えるのは XP 中央(600 → 250)と n7 到達(4 → 2)だけ。

⭐⭐ **シナリオ2 は n1 で折れている**(OLD 14/20・NEW 17/20)。
§4-2 が予告した「n1 と n4 で折れている。ボス部屋 n7 の調整では届かない」と一致。
**募集人数を何人にしてもクリア率 0% は動かない**見込み。→ 宿題 3 は
**募集人数の題材ではない**ので #8 から切り出す(ユーザー決定 2026-08-23)。

⭐ **#13 の掃討合流は廃坑で実際に働いている**(途中合流 OLD 16/20・NEW 8/20)。
シナリオ2 は 0/20 — n1 で早々に全滅するため合流の機会自体が来ていない。

### 9-4 ユーザー決定 (2026-08-23)

| 問い | 決定 |
|---|---|
| 廃坑 ★1 の募集人数 | **`recruit: 3` へ個別上書き**(§4-2 の候補 1「最も安全」)。`tavern.html:2173` の `id: "goblin-mine"` 定義に 1 行 |
| シナリオ2 のクリア率 0% | **別チケットへ分離**。#8 は廃坑の結論だけで閉じる |

### 9-5 スイープ中に踏んだこと(次に同じ測定をする人へ)

⭐⭐⭐ **混合ビルド検知器は mtime で鳴るが、鳴っただけでは走行を捨てる理由にならない。**
走行中に別窓が `tavern.html` を 2 回保存したが、**どちらも続行可**だった:

1. 1 回目 = `changelogList` の `<li>` 入れ替え **1 行だけ**。
   `recruit` / `buildParty` / `PARTY_SIZE` / `departTo` / `partyMembers` / `regenerateParty`
   のいずれにも一致行ゼロ → 測定経路に届かない
2. 2 回目 = #14 の調査シーム `devPartySizeOverride()` を **`partySize` の計算に割り込む位置**へ挿入。
   grep では「触れている」と出るので、**分岐を読んで恒等性を確認した**:
   `?party` 未指定 → `raw === null` → 即 `return n`。sweep の URL に `party` は無いので素通り

→ ⭐⭐⭐ **効くのは「変更が測定経路に届くか」で、それは mtime ではなく diff でしか分からない。**
検知器は「見るべき瞬間」を教えるだけで、可否は判定しない。
⚠ ただし `index.html` が動いたときは話が別(測定経路の本体)。検知器は
index = 即停止 / tavern = 報告して継続 と非対称に組むのが正しい。

---

## 10. 調整と、その後の再測定 (2026-08-23)

### 10-1 入れた変更

| ファイル | 変更 |
|---|---|
| `tavern.html` | `goblin-mine` 定義に **`recruit: 3`** を 1 行(§9-4 のユーザー決定)。受け皿は #7 が作った `recruitCountOf()` の `sc.recruit` 枝。呼び口は `partySize = 1 + recruitCountOf(prepScenario)` |
| `tavern.html` | 更新情報 1 行(changelog フックの要求)|
| `tools/verify_recruit_size.js` | 12 箇所。**期待値の緩和はゼロ**(内訳は §10-3)|
| `tools/sweep_recruit_balance.js` | `SCEN_TABLE['goblin-mine'].newNpc` を 1 → **3**(仕様変更の反映)|

### 10-2 調整後 20 ペア (40 走行) の結果 — 受入条件 4

10:44:11 → 11:54:10。装置 assert 崩れ **0 件** / 母集団ガード **OLD 20/20・NEW 20/20**。
生データ: `%TEMP%\df_sweep_recruit_goblin-mine_20260823025410.json`

| 腕 | 出発 | クリア | 全滅 | XP(中央) | 主人公HP(中央) | 途中合流 |
|---|---|---|---|---|---|---|
| OLD (`?recruit=0`) | 計 4 人 | 60% | 40% | 2,240 | 4.5 | 18/20 |
| NEW (既定 = `recruit: 3`) | 計 4 人 | 40% | 60% | 1,890 | 0 | 17/20 |

ペア比較 20 組 → 両方クリア 5 / OLDのみ 7 / NEWのみ 3 / 両方だめ 5

**狙った方向へ動いた(受入条件 4 の本体):**

| | クリア率 | XP 中央 |
|---|---|---|
| 調整**前**の NEW (計 2 人) | 15% | 580 |
| 調整**後**の NEW (計 4 人) | **40%** | **1,890** |

### 10-3 ⭐⭐⭐ 調整後は「ペア比較」では効果を測れない(次に触る人へ)

**受入条件 4 の「調整後にもう一度ペア比較を回す」は、字義どおりには実施できない。**
理由は本番のコードにある:

```js
let partySize = PARTY_SIZE;                                                       // OLD: 4
if (isRecruitOn() && prepScenario) partySize = 1 + recruitCountOf(prepScenario);  // NEW: 1 + 3 = 4
selection.partyMembers = orderFormation(buildParty(heroKey, partySize));
```

`recruit: 3` を入れた結果、**両腕とも `buildParty(heroKey, 4)` を呼ぶ = 完全に同一のコード**。
ペア比較は「同じものを 2 回測る」ことになり、**差はすべて乱数**になる。

→ ⭐ **効果は「調整前の NEW」と「調整後の NEW」の比較でしか読めない**(上の表)。

観測された 60% 対 40% が乱数である根拠(数値を言い訳にしないための実測):

- ペア比較の不一致 10 組が 7 対 3 → 二項検定 **p ≈ 0.34**(有意でない)
- ⭐ **同一条件の再現でも 5 ポイント動く**: 80 走行の OLD 腕 55% と、今回の OLD 腕 60%。
  この 2 つは `?recruit=0` どうしで**まったく同じ条件**。これが N=20 のノイズ幅の実測値

### 10-4 受入条件 5 の判定(数値と構造が食い違う件)

条件: 「`goblin-mine` の全滅率が、OLD 腕(NPC 3 人)と同等以下」

- **数値上**: NEW 60% > OLD 40% → **未達**
- **構造上**: 両腕は同一の `buildParty(heroKey, 4)` を通る = **同等であることがコードで保証されている**

⚠ この 2 つは矛盾ではない。**条件が想定していた「OLD と NEW が別条件である世界」が、調整によって消えた**のが実態。
`recruit: 3` は OLD 腕そのものへ戻す調整なので、「同等以下」は定義上満たされ、
観測値の差は §10-3 の乱数幅に収まる。**N を増やしても収束先は同じ**なので追加走行はしていない。

### 10-5 §2 の宿題 3 件の結論(受入条件 6)

| # | 宿題 | 結論 |
|---|---|---|
| 1 | #7 で NPC が減ったぶん | **廃坑のみ調整した**(`recruit: 3`)。クリア率 15% → 40%、XP 中央 580 → 1,890。⚠ **森 (bandits-forest) は調整していない** — §9-1 のとおりクリア率が両腕とも 0% で、募集人数では動かないと実測されたため(#8 から切り出して別チケットへ)。⛔ **沼 (lizard-swamp) は測っていない** — §3-5 の規模決定で対象を 2 本に絞ったとき、★2 で人数が 3→2 に減っているにもかかわらず母集団から漏れた。**未回収の宿題として残る** |
| 2 | 廃坑の既存宿題(P9 の寄り道 4 か所と宝箱) | **追加調整は不要と判断**。OLD 腕(= 寄り道込みの現状)が 80 走行で 55%、40 走行で 60% のクリア率を出しており、難易度として破綻していない。`recruit: 3` で NEW を OLD へ戻したので、この宿題は「OLD と同じ土俵に戻す」ことで同時に回収された |
| 3 | #11 の n7 大部屋化(敵 5 → 9〜10 体) | **測れなかった。** n7 に到達した走行が OLD 4/20・NEW 2/20 しかなく、大半が n1 で折れる(§9-3)。⭐ **ボス部屋の難易度を語るには、まず n1 を通れるようにする必要がある** → シナリオ2 の別チケットへ引き継ぐ |

### 10-6 副作用 — 「応じたのは、ただ一人 ――」が本番で出なくなった

`recruit: 3` により 6 シナリオの NPC 数は `3 / 2 / 2 / 3 / 3 / 3` = **最小 2**。
`#pmSub` の 1 人ぶんの文面は `recruitCountOf()` の clamp 下限としてコード上は生きているが、
**実プレイの導線からは到達しない**。

⚠ これは廃坑を `recruit: 2`(計 3 人)にしても同じ。**NPC1 が出るのは ★1 のシナリオを素で使うときだけ**。
→ 文面と分岐は**残した**(将来 ★1 の新シナリオが増えれば自動的に復活する)。
腐らせないために (E1) を `recruit: 0` のランタイム注入経由へ移し、(Ez5) が注入の成立を検査する。
**消すかどうかはゲーム設計の判断なので、実装側では決めていない。**
