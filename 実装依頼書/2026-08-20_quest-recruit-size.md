# クエスト難易度に連動した「募集 → 増援」の人数

**ステータス**: **承認済** (2026-08-22。着手前実測で行番号 10 件を訂正 — 下記「着手前実測」節)
**依存**: なし(依頼書 A / B とは独立。並行して進めてよい)
**この後に必ず**: `2026-08-20_recruit-balance-retune.md`(D)。**本依頼書はバランスを決めない**
**撤退スイッチ**: `?recruit=0`

---

## 目的

いまは出発ボタンを押すと NPC が 3 人**勝手に**ついてくる。プレイヤーは一度も「頼む」と言っていない。

これを「**依頼を受けて酒場で募集をかけ、応じた者が同行する**」に変える。
★1 の小仕事には人が集まらず、★3 の大仕事には集まる。

---

## 着手前実測 (2026-08-22 / 承認直後)

⭐ **行番号は 10 件中 10 件ズレていた**(#6 title-screen が入ったぶん全体が下へ動いた)。
本文の行番号は**この実測値へ置換済み**。以降も行番号は当てにせず**識別子で grep** すること。

**主張の中身はすべて正しかった** — 依頼書の根幹 3 点を本番コードで確認した:

1. ✅ **`index.html` の `buildParty` はフォールバック専用**。
   `index.html:31373` が sessionStorage の `partyMembers` を読み、
   それが無い/壊れている時**だけ** `31387` で `orderFormation(buildParty(heroOld))` を通る。
   → **「`index.html` を 1 行も触らない」は成立する**
2. ✅ **`difficulty` を持つのは固定 6 シナリオだけ** (`tavern.html` 2100 / 2123 / 2143 / 2163 / 2183 / 2203)
3. ✅ **生成クエストは持たない**。`buildPlazaSynthetic` (`tavern.html:5996`) の return は
   `{ id:"generated-quest", place, title, source, clientName, clientPortrait, enemies, target, recommendedLevel, __generated }`。
   → フォールバック + `[DIAG]` が必要。**DIAG に出す id は `generated-quest`**

### ⚠ 依頼書に書かれていなかった 2 点 (起草後に変わった / 見落とされていた)

- **`regeneratePartyMembers()` の呼び出しは 6 箇所**(依頼書は 1 箇所前提の書き方だった)。
  `tavern.html` の **4101** (`openPrep`) / **4476** (`selectHero`) /
  4494・5218・5274・5736 (いずれも「空なら作る」の遅延初期化)。
  ⭐ `openPrep(sc)` は **4084 で `prepScenario = sc` を代入してから** 4101 で呼ぶので、
  「`regeneratePartyMembers` が `prepScenario` を読む」設計はそのまま成立する。
  ⚠ 遅延初期化の 4 箇所は **人数が変わっても再抽選しない**(既に `partyMembers` があれば素通り)。
  クエストを選び直したら人数が変わるので、**`openPrep` が毎回作り直す**この経路が唯一の正。
- ⚠ **`selectHero()` は #6 (title-screen) で酒場から封印された**。
  通常経路では非主人公タイルが `.locked-out` になり、**`?herolock=0` の時だけ**到達できる。
  → 「主人公切替 → 再編成」経路の `PARTY_SIZE` フォールバックは**残すが、主経路ではない**。

---

## 背景・現状

### いまのパーティ編成

| 場所 | 内容 |
|---|---|
| [tavern.html:3123](../tavern.html#L3123) | `const PARTY_SIZE = 4;` — 主人公1 + NPC3 |
| [tavern.html:3177](../tavern.html#L3177) | `buildParty(heroClassKey)` — zone をばらけさせつつ `PARTY_SIZE` まで詰める |
| [tavern.html:4485](../tavern.html#L4485) 付近 | `regeneratePartyMembers()` — `orderFormation(buildParty(heroKey))` |
| [tavern.html:4523](../tavern.html#L4523) | 「仲間を引き直す」ボタン(出発前に何度でも再抽選可) |
| [tavern.html:1827](../tavern.html#L1827) | `#partyMatchOverlay` — 「共に挑む仲間が集う……」のマッチング演出 |
| [tavern.html:5202](../tavern.html#L5202) | `departToScenario()` が `sessionStorage["dragonfighters.partyMembers"]` に書いて index へ渡す |

### 朗報: `index.html` を触らなくてよい

`index.html` にも `PARTY_SIZE = 4`([index.html:11860](../index.html#L11860))と `buildParty`([11906](../index.html#L11906))の**写しがある**。
しかし [index.html:31373](../index.html#L31373) を読むと、実プレイのパーティは
**sessionStorage の `partyMembers` を読んで組んでおり**、`buildParty` が使われるのは
[31387](../index.html#L31387) の**「それが無い時のフォールバック」だけ**。

→ **人数を変えるのに `index.html` を触る必要はない。二重同期コストはほぼゼロ。**
(直起動・デバッグ時のフォールバックが 4 人のままなのは、むしろ望ましい)

### クエスト定義の難易度

`difficulty` を持つのは**固定 6 シナリオだけ**:

| id | difficulty | ★の数 |
|---|---|---|
| `goblin-mine`(廃坑) | `★☆☆` | 1 |
| `bandits-forest`(森) | `★★☆` | 2 |
| `lizard-swamp`(沼地) | `★★☆` | 2 |
| `orc-fort`(砦) | `★★★` | 3 |
| `undead-temple`(神殿) | `★★★` | 3 |
| `dragon-lair`(竜の巣) | `★★★★` | **4** |

⚠ **生成クエスト(闇市ポドルプラザ)は `difficulty` を持たない。**
[tavern.html:6049-6054](../tavern.html#L6049-L6054) の `buildPlazaSynthetic()` が返すのは
`recommendedLevel: q.level` までで、`difficulty` フィールドは無い。**フォールバックが必要。**

---

## 設計

### 募集人数の決め方

```
recruitCountOf(sc):
  1. sc.recruit が数値      → その値を使う            (クエスト個別の上書き)
  2. sc.difficulty が文字列 → "★" の数を数える        (固定6シナリオ)
  3. どちらも無い           → 既定 3 を返す + [DIAG] を1行出す   (生成クエスト)
  最後に clamp(n, 1, 3)
```

#### なぜ上限 3 なのか(勝手に外さないこと)

`dragon-lair` は ★4 なので素直に数えると NPC 4 人 = **パーティ 5 人**になる。
だが **5 人編成は一度も通したことがない**:

- `index.html` の味方スポーンタイル割り当て(`allyInitTakenTiles` 付近、[31351](../index.html#L31351))
- 隊列順 `orderFormation` と「頭 = 最も狙われる位置」の前提
- 出発準備画面のキャラタブ / 装備 UI の枠
- カメラ・視界・描画コスト

これらが 4 人前提で組まれている。**上限を 4 人パーティに固定することで、この依頼書では engine を一切触らない。**
5 人以上を試したくなったら**別の調査チケット**を切ること。

#### なぜフォールバックで黙らないのか

生成クエストが `difficulty` を持たないのは既知。ここで黙って 3 を返すと、
**将来 `difficulty` を持つ生成クエストが増えた時に誰も気づかない**。
`[DIAG] recruit: fallback used (no difficulty) id=generated-quest -> 3` を必ず 1 行出す。

### 触る場所

| 場所 | 変更 |
|---|---|
| [tavern.html:3177](../tavern.html#L3177) `buildParty(heroClassKey)` | 第2引数 `partySize = PARTY_SIZE` を足す。`PARTY_SIZE` は**既定値としてのみ残す**(定数は消さない) |
| [tavern.html:4485](../tavern.html#L4485) `regeneratePartyMembers()` | `prepScenario` から `recruitCountOf()` を引いて `buildParty(heroKey, 1 + n)` を呼ぶ。`prepScenario` が無い時は `PARTY_SIZE` |
| [tavern.html:2095](../tavern.html#L2095) 以降のシナリオ表 | **今は何も足さない。** `recruit:` は依頼書 D で値を決めてから入れる(器だけ先に作る) |
| [tavern.html:4523](../tavern.html#L4523) 「仲間を引き直す」 | ラベルを **「募集をかけ直す」** に変更 |
| 出発準備画面(`#partyPreview` の上) | **「この依頼に応じた冒険者: N 人」** を 1 行出す |
| [tavern.html:1831](../tavern.html#L1831) `#pmSub` | 人数で文言を出し分ける(下記) |

#### `#pmSub` の文言(任意だが推奨)

| 人数 | 文言 |
|---|---|
| 1 | 「応じたのは、ただ一人 ――」 |
| 2 | 「二人が、卓についた ――」 |
| 3 | 「共に挑む仲間が集う ――」(現行文言) |

### 触らないと決めたファイル

- **[index.html](../index.html) — 1 行も触らない。** `PARTY_SIZE`(11860)も `buildParty`(11906)も**フォールバック専用**
- `js/save-slots.js` / `title.html`(依頼書 A / B の担当)
- 敵の配置・数・強さ・XP テーブル — **依頼書 D の担当**

---

## 実装ステップ(順序)⚠ 2 段構えを守ること

### STEP 1: 配線だけ(既定 OFF)

`recruitCountOf()` と `buildParty(hero, size)` を実装するが、**既定を OFF にする**
(`?recruit=1` で初めて有効、無指定は従来通り 4 人)。

この段で **既存の golden ドライバ群が全部緑のまま**であることを確認する。
挙動が 1 ミリも変わっていないことを証明してから機能を入れる。

### STEP 2: 既定 ON へ切り替え

既定を ON(`?recruit=0` が撤退)に変える。**ここで既存 golden の多くが赤くなるはず。**

⚠⚠ **赤を機械的に更新しないこと。** 1 本ずつ、
「パーティ人数が変わったせいで期待値が変わった」のか「実装が壊れた」のかを**目視で判定**してから更新する。
判定できない赤が 1 本でも残ったら、そこで止めてユーザーに報告する。

### STEP 3: UI(ラベル・人数表示・`#pmSub`)

### STEP 4: 受入条件のドライバを通す

---

## 受入条件

新規ドライバ `tools/verify_recruit_size.js`。冒頭で必ず `require('./_pptr_profile')`。

### 機能

1. 固定 6 シナリオそれぞれで、出発時の `sessionStorage["dragonfighters.partyMembers"]` の長さが
   **`1 + 期待値`** になる:

   | id | 期待 NPC 数 | パーティ計 |
   |---|---|---|
   | `goblin-mine` | 1 | 2 |
   | `bandits-forest` | 2 | 3 |
   | `lizard-swamp` | 2 | 3 |
   | `orc-fort` | 3 | 4 |
   | `undead-temple` | 3 | 4 |
   | `dragon-lair` | **3**(★4 だが clamp) | 4 |

2. **どのクエストでもパーティが 4 人を超えない**
3. 生成クエスト(闇市)は NPC 3 人。かつ `[DIAG] recruit: fallback used` のログが**実際に出ている**
4. シナリオ定義に `recruit: 3` を注入すると、`goblin-mine` でも NPC 3 人になる(個別上書きが効く)
5. 隊列順が front → mid → rear のまま(`orderFormation` を壊していない)
6. 「募集をかけ直す」を押すと**顔ぶれが変わり、人数は変わらない**

### 装置 assert(検出器が空振りしていない証明)

7. `?recruit=0` を付けると **1. の全 6 件が 4 人になる**(= 従来動作)。
   「`?recruit=0` で緑」ではなく「**スイッチを外すと期待値が変わる**」ことを測る

### 既存の非退行

8. STEP 1 完了時点(既定 OFF)で既存 golden が**全部緑**
9. STEP 2 完了時点で赤くなった golden について、**1 本ずつ理由を書いた一覧**を残す
   ⚠ 「人数が変わったから」で片付けず、そのドライバが何を測っていたのかを 1 行で書く

---

## やらないこと(スコープ外)

- **バランス調整をしない。** 敵の数・質・配置・XP は 1 つも触らない(依頼書 D)
- シナリオ表に `recruit:` の具体値を**入れない**(器だけ作る。値は D で測って決める)
- パーティを 5 人以上にしない(engine 未検証。別チケット)
- `index.html` を触らない
- 「誰が応募するか」をプレイヤーが選べるようにしない(いまも選べない。抽選のまま)
- 応募者の立ち絵・専用演出の新規作成(既存 `#partyMatchOverlay` を流用)

---

## リスクと対策

| リスク | 対策 |
|---|---|
| ★1 の廃坑が NPC 1 人になり、**最初のクエストが一番難しくなる** | ⚠ **これは起きる。** 本依頼書は器だけ作り、値は D で実測して決める。D をやるまで出荷しない |
| 既存 golden が大量に赤くなる | STEP 1 の既定 OFF で「挙動不変」を先に証明 → STEP 2 で 1 本ずつ理由を確認して更新 |
| 生成クエストが黙って 3 人になる | `[DIAG]` を必ず 1 行出す。受入条件 3. でログの実在を測る |
| ★4 で 5 人パーティになり engine が壊れる | `clamp(n, 1, 3)`。受入条件 2. で「4 人を超えない」を全クエストで測る |
| `regeneratePartyMembers()` が `prepScenario` 無しで呼ばれる | `PARTY_SIZE` にフォールバック。`selectHero` 経由の呼び出しがこれに当たる |

---

## 二重ファイル同期

**なし。`tavern.html` だけを触る。**
`index.html` の `PARTY_SIZE` / `buildParty` は**フォールバック専用**なので据え置く
(この非対称は意図的。上記「背景・現状」に理由を書いてある)。

---

## 実装結果

**完了**: 2026-08-23 / **進め方**: dev-loop「1 項目 = 1 サブエージェント」で 4 分割
**検証**: `tools/verify_recruit_size.js` … **79/79 passed**(同じツリーで 3 回・全て exit 0)

### ⛔⛔ この依頼書だけで出荷してはいけない

**★1 の廃坑(`goblin-mine`)は NPC 1 人 = 主人公と 2 人きり**になった(実測)。
つまり **ゲームで最初に受ける依頼が、いちばん過酷**である。
これは本文「リスクと対策」の 1 行目が予告したとおりで、**本依頼書は器だけを作る**という
設計判断そのものの帰結。値を決めるのは別依頼書の仕事である。

- 値の実測調整 = **[`2026-08-20_recruit-balance-retune.md`](2026-08-20_recruit-balance-retune.md)(#8・未承認)** の担当
- **#7 → #8 は必須順。#8 を飛ばして出荷しない**
- 器は完成しているので、#8 は **シナリオ表に `recruit: <数値>` を 1 行足すだけ**で人数を動かせる
  (その経路が実際に効くことは受入条件 4 =`(G)` がランタイム注入で実測済み)
- ⛔ 上限は `clamp(n, 1, 3)`。`recruit: 9` と書いても NPC 3 で止まる(`(G2)` で実測)。
  5 人パーティは engine 未検証なので **#8 でも上限を外さないこと**(外すなら別チケット)

### コミット

| 項目 | commit | 内容 |
|---|---|---|
| (承認) | `dd02168` | 依頼書を承認済へ + 「着手前実測」節(行番号 10 件中 10 件を訂正) |
| 1/4 | `f2a581e` | `recruitCountOf()` / `isRecruitOn()` / `buildParty(hero, size)` の配線を **既定 OFF**(`?recruit=1` でのみ有効)で入れ、新規ドライバ `tools/verify_recruit_size.js` を起こす(STEP1)。33/33 |
| 2/4 | `bfb39e7` | 既定 ON へ反転(`?recruit=0` が撤退)+ 既存 golden の triage(STEP2)。**期待表は 1 文字も変えず測定点(URL)だけ張り替えた**。33/33 |
| 3/4 | `204dc92` | UI:「📣 募集をかけ直す」/「この依頼に応じた冒険者: N 人」/ `#pmSub` の人数出し分け(STEP3)。56/56 |
| 4/4 | `a87ce12` | 受入条件 3 / 4 / 5 を測る (F)(G)(H) をドライバへ追加(STEP4)。56/56 → **79/79** |
| 4/4 | `(本コミット)` | 本「実装結果」節を依頼書へ追記 |

### 受入条件 1.〜9. の証拠(実測値つき)

すべて `tools/verify_recruit_size.js`(URL 無指定 = 実プレイヤーが踏む経路で測る)。

| 受入条件 | 測っている assert | 実測値 |
|---|---|---|
| 1. 6 シナリオの出発人数 = 1 + 期待値 | `(A)` + 装置 `(Az0)`〜`(Az4)` `(S3)` `(Sz)` | `goblin-mine` 計2/NPC1・`bandits-forest` 計3/NPC2・`lizard-swamp` 計3/NPC2・`orc-fort` 計4/NPC3・`undead-temple` 計4/NPC3・`dragon-lair` 計4/NPC3。実クリック導線でも一致 `(B)` `(B2)` |
| 2. 4 人を超えない | `(A2)` `(C2)` `(F4)` `(A4)` | 全クエストで最大 4 人。★4 の `dragon-lair` も clamp で NPC3 |
| 3. 生成クエスト(闇市)= NPC3 + `[DIAG]` の実在 | **`(F)` `(F2)` `(F3)`** + 装置 `(Fz0)`〜`(Fz4)` | 計4人/NPC3/主人公1。DIAG は **出発の再抽選でちょうど 1 行**、文面は `[DIAG] recruit: fallback used (no difficulty) id=generated-quest -> 3`。⭐ 件数の絶対値ではなく **観測直前の `consoleLines` 件数との差分**で測る(「作るだけ=0 行 / 出発=1 行 / 経路B=1 行」の 3 区間に割って出所まで確定させた) |
| 4. `recruit: 3` の個別上書き | **`(G)` `(G2)` `(G3)`** + 装置 `(Gz0)`〜`(Gz4)` | 注入前 NPC1 → `recruit:3` で NPC3 → `recruit:9` でも NPC3(clamp 上限)→ `recruit:0` でも NPC1(clamp 下限)→ 撤去で NPC1 へ復帰。経路A(実際に書かれた配列長)と経路B(`recruitCountOf`)が 5 点とも一致 `[1,3,3,1,1]` |
| 5. 隊列順 front → mid → rear | **`(H)` `(H2)` `(H3)`** + 装置 `(Hz1)` `(Hz2)` `(Hz3)` | 既定 ON の 6 件・`?recruit=0` の 6 件・生成クエスト 1 件で崩れ 0。⭐ **zone が 2 種類以上ある行が 6/6 件実在**(全員 front なら自明にソート済み = 何も証明しないため)。`orderFormation()` 直呼びの 2 経路でも `R1>F1>M1>F2>M2 → F1>F2>M1>M2>R1` と安定ソートを確認 |
| 6. 「募集をかけ直す」で顔ぶれが変わり人数は変わらない | `(D6a)` `(D6b)` + 装置 `(Dz1)`〜`(Dz7)` | ★1 で 10 回押して人数の種類 `[1]` / 顔ぶれ 9 種、★3 で 人数 `[3]` / 顔ぶれ 11 種(11 サンプル中) |
| 7.【装置】`?recruit=0` で 1. の全 6 件が 4 人 | `(C)` + `(Cz2)`〜`(Cz6)` | 6 件とも計 4 人。⭐「`?recruit=0` で緑」ではなく **同じ `judgePartySizes()` に募集 ON の期待表を通すと落ちる**`(Cz3)`・**(A) では true を返していた**`(Cz4)`・**スイッチの有無で人数が変わる id が 3 件**`(Cz6)` の 3 本で「飾りでない」ことを測る |
| 8. STEP1(既定 OFF)で既存 golden 全緑 | 項目 1 で実測(`f2a581e`) | golden 8 本すべて基準どおり緑 |
| 9. STEP2 で赤くなった golden の理由一覧 | 項目 2 で実測(`bfb39e7`) | **赤 0 本**。⭐ ただし「緑のまま空振り」が 1 本見つかった → 下記「逸脱」⑦ |

### 負のコントロール(新規 assert が空振りでない証明)

⚠ **本体リポジトリでは変異させていない。** 別窓(計画窓)が `tavern.html` / `index.html` を
未コミットで編集中だったため、**隔離 worktree(`204dc92` を checkout)の中だけ**で変異させ、
毎回 md5 で baseline 一致を確認して戻した(5 回とも `MATCH`)。

| 変異 | 赤くなった assert | 結果 |
|---|---|---|
| `recruitCountOf` の `console.log` を消す | `(F2)` `(F3)` | 77/79 |
| DIAG を無条件で出す(if の外へ) | `(Fz2)` `(F2)` `(F3)` | 76/79 |
| `buildPlazaSynthetic` の戻り値に `difficulty: "★★"` を足す | `(Fz1)` `(F)` `(F2)` `(F3)` | 75/79 |
| `sc.recruit` の個別上書き分岐を殺す | `(G)` `(G2)` `(Gz4)` | 76/79 |
| `orderFormation` の `rank` を逆順にする | `(H)` `(H2)` `(H3)` `(Hz3)` | 75/79 |

⭐ 3 つ目の変異は「**将来 `difficulty` を持つ生成クエストが増えた日**」そのものの再現。
本文「なぜフォールバックで黙らないのか」が守りたかったものが、機械で守られていることの証拠になる
(`(Fz1)` が前提の崩壊を検出し、同時に `(F)` の人数が 3 → 2 へ動く)。

### 既存の非退行(本ドライバの外・同じ隔離 worktree で実測)

| ドライバ | 実測 | 基準 |
|---|---|---|
| `tools/verify_recruit_size.js` | **79/79**(3 回とも exit 0) | 56/56 → 本項目で 79/79 へ ✅ |
| `tools/verify_title_screen.js` | 83/83 | 83/83 ✅ |
| `tools/verify_save_slots.js` | 30/30 | 30/30 ✅ |
| `tools/driver_dev_gate.js` | 52/52 | 52/52 ✅ |
| `tools/driver_dev_gate2.js` | 62/62 | 62/62 ✅ |
| `tools/driver_depart_menu_clean.js` | 41/41 | 41/41 ✅ |
| `tools/driver_equip_compact_ios.js` | 31/31 | 31/31 ✅(項目 2 で ★1 → ★3 へ張り替え済) |
| `tools/driver_skillcheck_roster.js` | 12/12 | 12/12 ✅ |
| `tools/driver_grid_p8.js` | 55/55(FAIL 0) | 55/55 ✅(既知フレークの `(8e)` も出ず) |

⚠⚠ **本体リポジトリではなく隔離 worktree(`204dc92` を checkout + 項目 4 のドライバ)で測った。**
測定時点で別窓(計画窓)が `index.html`(+136 行)/ `tavern.html` / `js/df-mapdef.js` /
`tools/driver_grid_p8.js`(+30 行)/ `tools/driver_graph_p6.js` を**未コミットで編集中**だった。
本体で回すと **別窓の作業途中を測る**ことになり、赤が出ても自分の変更が原因か切り分けられない。
⭐ 項目 4 が触ったのは `tools/verify_recruit_size.js` と本 `.md` だけなので、他の 8 本へ
影響し得る経路は原理的に無い(それでも 8 本すべて実測した)。

### 依頼書からの逸脱

1. **【文言】`#pmSub` の 3 人ぶんを「共に挑む仲間が集う……」→「共に挑む仲間が集う ――」へ変えた。**
   本文の表は 3 人の欄に「(現行文言)」と注記していたが、**現行は三点リーダ**だった。
   1 人 / 2 人の新規文言が全部ダッシュ止めなので、3 人だけ三点リーダだと演出が揃わない。表の字面に合わせた。
2. **【文言】ボタンの絵文字を 🎲 → 📣 へ変えた。** 依頼書はラベル文言(「募集をかけ直す」)しか
   指定していないが、🎲(くじ)は「もう仲間を引くのではなく酒場に募集をかける」という
   本依頼書の主題と食い違う。
3. **【依頼書に無い】準備画面の見出し注記を「(酒場で居合わせた冒険者・自動編成 …)」→
   「(募集に応じた者が同行 / 先頭=前衛が盾)」へ。** 同じ理由(ボタンだけ言い換えると画面内で矛盾する)。
   `≤480px` の CSS コメントも同じ言い回しへ揃えた。
4. **【依頼書に無い】オンボーディングのナレ本文に「依頼が重いほど多くの者が名乗りを上げよう」を追加。**
   人数が依頼ごとに変わることを、プレイヤーがどこかで一度は聞ける必要があるため。
5. **【依頼書に無い】0 人 / 4 人以上の `#pmSub` フォールバック文言を足した。**
   表は 1 / 2 / 3 しか定義していない。黙って空文字になるのを避け、
   0 人 =「名乗りを上げる者は、なかった ――」、4 人以上 = 3 人の文言へ寄せる、とした(`(E5)` が測る)。
6. **【依頼書の実測が不足していた】`regeneratePartyMembers()` の呼び口は 6 箇所ではなく 7 箇所。**
   「着手前実測」節は 6 箇所と書いていたが、**「仲間を引き直す」ボタンのハンドラ(旧 `4528`)が抜けていた**。
   7 箇所のうち **人数変更に追随するのは `openPrep` 経路だけ**で、
   `renderPartyPreview` / `departToScenario` / 他 2 箇所は `if (!partyMembers.length)` の遅延初期化なので
   人数が変わっても再抽選しない。**クエストを選び直したときに作り直すのは `openPrep` が唯一の正**
   (この非対称は意図的。コードにコメントで残してある)。
7. **【依頼書に無い・重要】`driver_equip_compact_ios.js` を ★1 → ★3 へ張り替えた。**
   既定 ON にした瞬間、このドライバは `goblin-mine`(★1)を開くので **計 2 人の準備画面**を測る
   ようになっていた。赤にはならないが「いちばん枠が要る状態」を一度も見なくなる = **緑のまま空振り**。
   既定を `orc-fort`(★3・計 4 人)へ変え、母集団ガード `(0)` を追加(31/31)。
   負のコントロール `--scenario goblin-mine` で `(0)` だけが落ちることを実測。
   ⚠ 同時に、事前の想定「キャラタブが人数直撃」は **誤り**と判明 —
   `renderCharTabs()` は `PARTY_SLOTS`(全 6 職)を回す職業別プリセットなので人数に依存しない。
8. **【測り方の変更】受入条件 4 をソース編集で測らなかった。**
   `(S9)` が「シナリオ表に `recruit:` の具体値を入れていない」(= 値は #8 の担当)を静的検査しているため、
   テスト用に `recruit: 3` を書き足すと `(S9)` が赤くなる。
   → **ランタイムで `scenarios` のオブジェクトへ注入**し、測り終えたら `delete` で戻す方式にした。
   副産物として **注入前 1 → 注入後 3 → 撤去後 1 の往復**まで測れるようになり、
   「元から 3 だった」との区別がついた。
9. **【追加】`clamp` の上限・下限を上書き経路でも測った(`(G2)` `(G3)`)。**
   依頼書の受入条件 4 は「3 を注入すると 3 になる」までだが、器は #8 が数値を入れる場所なので、
   `recruit: 9` でも 5 人パーティにならないことを機械で止めておく価値が高い。
10. **【依頼書どおり未実施】**シナリオ表への `recruit:` の具体値・バランス調整(敵/XP)・
    5 人以上のパーティ・`index.html` の改修は、すべて「やらないこと」のまま。
    `(S8)` が `index.html` の `PARTY_SIZE` と 1 引数の `buildParty` が無傷であることを毎回検査している。

### 撤退スイッチ `?recruit=0`

| 付けた時 | 起きること |
|---|---|
| `tavern.html?recruit=0` | `isRecruitOn()` が `false` を返し、`regeneratePartyMembers()` が `PARTY_SIZE`(4)で編成する = **本依頼書の前の挙動そのもの**。6 シナリオ全部が計 4 人 / NPC3 |
| 無指定(既定) | ★の数 → NPC 1〜3 人 |

測っているのは `(C)`(受入条件 7 の本体)と装置 4 本:
`(Cz2)`「`?recruit=0` で `isRecruitOn()` が `false`」/
`(Cz3)`「**同じ判定関数**に募集 ON の期待表を通すと落ちる(恒真でない)」/
`(Cz4)`「その判定関数は (A) では `true` を返していた(共有が空振りでない)」/
`(Cz6)`「スイッチの有無で **実際に人数が変わる id が 3 件以上**ある」。
⭐ 「`?recruit=0` で緑」は何も証明しない(何も起きなくても一致する)ので、
**期待表を共有した上で「外すと期待値が変わる」**を数値で測る形にしてある。
また `(H2)` により、撤退経路でも隊列順が壊れていないことを併せて押さえている。

### ユーザーの目視・実機判断が要るもの(ヘッドレスでは代替できない)

⚠ **どれも「機械では緑だが、遊んで良いかは別」**の項目。

1. **★1 の廃坑を実際に遊んで「主人公 + NPC 1 人」が成立するか**(← 最重要。#8 の入力になる)
2. 出発準備画面に出る 1 行: **「この依頼に応じた冒険者: 1 人」**(★1)/
   **「この依頼に応じた冒険者: 3 人」**(★3)の見え方・置き場所
3. ボタンのラベル **「📣 募集をかけ直す」**(旧「🎲 仲間を引き直す」)が、
   絵文字込みで iPhone 幅(390px)でも折り返さずに収まるか
4. マッチング演出 `#partyMatchOverlay` のサブ文言:
   1 人 = **「応じたのは、ただ一人 ――」** / 2 人 = **「二人が、卓についた ――」** /
   3 人 = **「共に挑む仲間が集う ――」**。**人数ごとに演出の尺が合っているか**(1 人だと短い)
5. 準備画面の見出し注記 **「パーティ (募集に応じた者が同行 / 先頭=前衛が盾)」**
6. オンボーディングのナレ本文に足した一文
   **「依頼が重いほど多くの者が名乗りを上げよう。」** が語りとして自然か
7. 2 人パーティ(★1)で **装備画面・キャラタブの見た目が寂しくならないか**(枠が 2 つになる)
8. 闇市ポドルプラザの生成クエストが **常に NPC 3 人**でよいか
   (難易度は生成時のレベルで動くのに、人数は 3 固定。#8 の検討材料)

