# 実装依頼書

**用途**: 別窓(実装窓)が稼働している間、この窓で立てた実装計画を置く場所。
実装窓はここの `.md` を 1 枚読めば作業に入れる、という粒度で書く。

## 一覧(新しい窓はここを最初に読む)

上から順に着手する。ステータスが「完了」のものは読まなくてよい。

| # | 依頼書 | ステータス | 進行度 | 一行 |
|---|---|---|---|---|
| 1 | [2026-08-20_mine-wall-clipping.md](完了/2026-08-20_mine-wall-clipping.md) | **完了 `7c59c44`** | 100% | 廃坑の外周 1 タイルを塞ぎ、ゴブリン戦車を 176px + footprint 判定へ |
| 2 | [2026-08-20_wallbox-hardcoded-72px.md](完了/2026-08-20_wallbox-hardcoded-72px.md) | **完了 `f8c3bba`** | 100% | 壁の当たり判定の箱を実測比率へ。⚠ 箱の中心はスプライト中心から ±28px に制限(絵の中心をそのまま追うと下隣のタイルへ出る) |
| 3 | [2026-08-20_paint-mask-subtile.md](完了/2026-08-20_paint-mask-subtile.md) | **見送り `5cfe1e1`** | 100%(調査) | 1 枚絵マスクの内側の半マスズレ。STEP1(道具・実測・目視)まで実施し、**マスク編集は見送り**。⚠ STEP1 の道具は #1 で既に作られていた。⭐ ノード部屋の内側に岩盤は 0 マス = 赤は全部マスク由来 |
| 4 | [2026-08-20_bgm-mine-swap.md](完了/2026-08-20_bgm-mine-swap.md) | **完了 `4090a99`** | 100% | 廃坑の BGM を 3 曲へ(入口 d1 / 坑内 haikou / グリクス戦 boss01) |
| 5 | [2026-08-20_save-slots.md](完了/2026-08-20_save-slots.md) | **完了 `dde8457`** | 100% | セーブスロット 3 つ。**キー名は 1 文字も変えない**(ライブ + アーカイブ方式)。UI なし。STEP1-7 完了(`89f58a9`/`87ffca8`/`b92444e`/`dde8457`)。検証 `tools/verify_save_slots.js` 30/30。⭐ **3 スロット満杯で 78,326 bytes = 5MB の 1.49%** = quota は問題にならない。⭐⭐⭐ 受入条件 7 は「?slots=0 で緑」ではなく **同じ assert 本体を両ページへ当てて「外すと赤」**で測る。⚠ 実機(iOS Safari)で `pagehide`/`visibilitychange` が発火するかは未確認 |
| 6 | [2026-08-20_title-screen.md](完了/2026-08-20_title-screen.md) | **完了 `9f8b1bd`** | 100% | `title.html` 新設。新規/続き → 名乗り(主人公選択)→ 酒場。以後クラス変更不可。dev-loop 4 項目で完走(`a242ab1` STEP1 / `720006b` STEP2-3 / `30e5134` STEP4-6 / `954c41c` STEP7-8 / `9f8b1bd` 実装結果節)。検証 `tools/verify_title_screen.js` **83/83**・既存 golden 7 本非退行。撤退=`?title=0` / `?herolock=0`。⭐ 着手前の実測で **行番号が 8 件中 8 件ズレていた**(`d4ee24d` で訂正して依頼書へ書き戻し) |
| 7 | [2026-08-20_quest-recruit-size.md](2026-08-20_quest-recruit-size.md) | **完了** `459c34a`(dev-loop 4 項目・検証 79/79) | 100% | 「募集 → 増援」。★の数で同行 NPC が 1〜3 人に変わる。**器だけ作り値は入れない** |
| 8 | [2026-08-20_recruit-balance-retune.md](2026-08-20_recruit-balance-retune.md) | **完了 `11e4678`**(120 走行・golden 8 本緑) | 100% | 7 の後に難易度/XP を**ペア比較で実測してから**調整。廃坑の既存宿題も同時に回収。⚠ **2026-08-23 の着手前実測で本文を全面改訂**(旧本文の測定計画は成立しなかった)。⛔⛔ **`?recruit=0` は `index.html` では効かない** — `isRecruitOn()`/`recruitCountOf()` は `tavern.html` にしかなく、既存ドライバ(`auto_debug_run.js`/`probe_n4_stall.js`)は index 直起動なので**両腕とも計 4 人**になる。⭐⭐⭐ 正しい測定台 = **酒場を本番どおり通して `departToScenario()` に遷移させる**(`autoplay` は index へ引き継がれる。実測で OLD=計4人 / NEW=計3人 に割れた)。⛔ **前回窓の「4 試行中 3 回全滅」は #7 のせいではない** — 全部 index 直起動 = 旧仕様の計 4 人だった。✅ **XP は頭割りされない**(`gainXP` は主人公の `currentTotalXp` 一本。呼び口 4 箇所)→ 人数を理由に XP を触る根拠は無い。⭐ 規模はユーザー決定で **シナリオ2 + 廃坑 の 2 本 × N=20 ペア**に限定(★3 以上の 4 本は #7 で人数が変わらず差が原理的にゼロ) |
| 9 | [2026-08-22_boss-latch-during-combat.md](完了/2026-08-22_boss-latch-during-combat.md) | **完了 `24dbb60`** | 100% | 戦闘中も「玉座へ詰めたか」を見るラッチ点を `runEncounter` の手番ループへ 1 本。⚠ 負のコントロールは実プレイではなく **`tools/probe_boss_latch.js`**(`--index` で修正前の index.html を配れる)。⭐⭐⭐ **「戦闘中だったか」は「同じ戦闘だったか」ではない**(`encounterStartedAt` で見る) |
| 10 | [2026-08-22_mine-s4-guard-fog.md](完了/2026-08-22_mine-s4-guard-fog.md) | **完了 `5a0e3c3`** | 100% | 廃坑 s4 へ実質行けない。⚠ **本文の「真因」は実測で訂正済**(霧ではなく**索敵 12.5 タイル AND 視線**)。採用は **案 1 修正版 = 護衛を (51,20)/(51,22) へ**。末尾の「決定」節が唯一の正 |
| 11 | [2026-08-22_bandit-hideout-map-s2.md](2026-08-22_bandit-hideout-map-s2.md) | **完了 `f62f145`**(新規 driver 93/93・既存 p6 244/244・p8 56/56) | 100% | codex1 の `盗賊団のアジト.png` をシナリオ2 の**ボスノード n7 を 52x26 へ大部屋化**して貼る(ユーザー決定=案A + `outdoor:true`)。⭐ **n0〜n6 は 1 バイトも触らない** = 分岐/罠/宝箱/残影の獣が全部生き残る。⭐ 実測済: **52x30 マス**・周期 30.5150x31.1400・位相 21.50/16.20・**1 マス 48px** で焼く(0.87MB / `--check` 両軸 OK)。⚠⚠⚠ **絵は MAP_H=28 より 2 行高い**→上下 2 行(樹冠のみ)を捨てて 52x26・`rect [1,10,26,61]`。⚠⚠⚠ **`outdoor:true` × 敵の居る屋外部屋は前例が無い**(`index.html:31977` のコメントが「屋外ノードに敵を置く日には必ず効く」と名指しで警告)→ STEP1 で実測 |
| 12 | [2026-08-22_town-map-phlan.md](2026-08-22_town-map-phlan.md) | **完了 `227785b`**(検証 85/85・負のコントロール 5 本・既存 15 本非退行) | 100% | codex1 の `harbor-town-rebuilding-player-v1.png` を街 `town.html` にして、**キャラ選択の直後に酒場ではなく街に立つ**。タイルクリックで A* 経路歩き + 看板クリックで施設へ。ユーザー決定 = **案B(新規ページ)** + 銀の鹿亭 (09-10,00-02) + 3 施設(酒場/武器屋/闇市)。⭐ 実測済: **23x15 マス**・周期 63.945x64.410・位相 33.40/7.25・**1 マス 64px** で焼く(1472x960 / 0.53MB / 倍率 **1.00x = 水増しゼロ** / `--check` 両軸 OK)。⚠⚠⚠ **案B は起草者の推奨(案A)ではない** — 武器屋と闇市が `tavern.html` の中にあるため「入口が 2 種類になる」問題が残る → **クエリを足さず `sessionStorage["dragonfighters.enterVia"]` で解く**(#6 が名指しで禁じた事項に触れないことを受入条件 7 で機械的に検査)。⚠⚠ **南橋は絵が半マスずれている**(row 9/10 にまたがる)→ 歩けるのは **row 10 だけ**。✅ **#6 は完了済 `9f8b1bd` なので着手可**。⚠⚠⚠ `index.html` の帰還先は **2 箇所ではなく 4 箇所**(`:34717` リザルト / `:34774` 撤退 / `:13213` エンディング / `:13212` autoplay)。⚠⚠ `verify_title_screen.js` の受入条件 **2. と 8. は必ず赤くなる** → **期待値を書き換えず測定点を移す**(依頼書 §10 に張り替え方の表あり) |
| 13 | [2026-08-23_encounter-mopup-before-rest.md](2026-08-23_encounter-mopup-before-rest.md) | **完了 `3cc3eaf`**(新規 driver 36/36) | 100% | ⚠⚠ **`index.html` の変更は別窓の #12 コミット `3cc3eaf` に紛れて入っている**(相手のファイル単位 add が、同居していた本チケットの 5 hunk ごと取り込んだ。未 push で気づいたが別窓が稼働中のため切り直さずそのままにした)。実測: 休憩の回数が **8 回 → 3 回**、全休憩で `detectReinforcements()` = **0**、休憩明けの再戦闘まで 0.2〜3.4 秒 → **43.5 / 55.8 秒**。非退行 6 本緑 (p8 56 / p9 52 / graph_p6 244 / bgm_mine 37 / grid_s2 93 / spawn_not_on_gate 47)。⚠ 難易度は上がった (回復 3 種の回数が減る) → 数値は #8 へ。撤退=`?mopup=0` / `?mopupsight=0`。<br>【起草時の記録】**手前の 1 体を倒しただけで休憩フェーズに入る**。⭐ 着手前に実測済 (`tools/probe_rest_premature.js` を新設): 休憩 13 回中 **1 回は本番の `detectReinforcements()` が 2 体を返しているのに休憩へ落ちた**(隣のマスにホブゴブリンとゴブリンブルート)。真因 = ①勝利判定が `encounterEnemyIndices` しか見ない ②増援を見る点がラウンド先頭の 1 点だけ ③視界はパーティ全員なのに**視線はリーダーだけ**。⚠⚠ **直すと難易度は上がる** (回復 3 種がエンカ終了ごとなので、分割していたぶん得していた) → 数値は #8 へ合流。撤退=`?mopup=0` / `?mopupsight=0` |
| 14 | [2026-08-23_party-size-5-feasibility.md](2026-08-23_party-size-5-feasibility.md) | **完了**(調査チケット・dev-loop 4 項目 `a1558ba` / `83ac18f` / `01eac91` + STEP4) | 100% | ⭕ **結論: 5 人(主人公1+NPC4)は engine 側は行ける。6 人は ⛔。** 6 人が落ちるのは**下部 HP ミニバーの 1 点だけ**(390x844 で `client 390 / scroll 413` = 23px はみ出す。横スクロールに落ちない上限がちょうど 5 人。⚠ かつ 6 人のカメラは未測定)。⭐⭐ **本命だった desktop のカメラは白だった** — 主人公が画面外のフレーム率は N=4 / N=5 とも **0.03%**(24 走行 / 12 ペア / **41,610 フレーム**、12 ペアすべて差 ±0.01pt)で、依頼書の不合格条件「desktop が 0% から有意に上がる」は**成立しなかった**。危ない側は desktop ではなく **compact + 竜の巣**で、しかも **4 人の時点で既に**クランプ区間が空(N=4 22.40% / N=5 23.82%)。症状が 0.00% で済んでいるのは `[compact-hero-anchor]` の 2 点ハードクランプが吸収しているから → ⛔ **この救済節を壊す改修は 5 人化とセットにできない**。⭐ スポーンは **702 人ぶん**で違反 0(⚠⚠ **依頼書が見落としていた 2 本目の配置点 `placeNodeParty()`(ノード遷移のたび・RING 16 個)も掃いた上で**)/ 隊列 24 標本すべて成立 / 停滞 **0 件**。⚠⚠ **本文を 5 箇所訂正**(① 前提表 #1 に `placeNodeParty()` を追記 ② 受入条件 6 の予測値 70.8/58.0/48.9px は誤りで実測 **75.33/59.06/52px**(撤退チップが `flex:0 0 auto` の非伸縮 52.67px)③ compact 25.9%/16.5% は `[compact-hero-anchor]` **導入前**の値で比較不可 ④ 非退行の基準が古い **79→82 / 55→56** ⑤ **`?autoplay` はカメラ測定に使えない**=`focusCameraOn` が丸ごと止まる → `RUN.auto` を使う)。⚠⚠ **#8 の 120 走行は「4 人が上限」という前提の下で最適化された結果** = 上限を上げるなら測り直しになる。⛔ **本チケットは上限を上げていない**(`MAX = 3` は無改修 / `?party=N` は調査シームで本番導線から呼べない / `grep -c psProbe index.html` = 0)。装置 `tools/probe_party_size.js` 本番 **57/57** ・ `--negative` **22/22**。非退行 6 本緑(recruit_size **82** / p8 **56** / p9 52 / graph_p6 244 / spawn_not_on_gate 47 / encounter_mopup 36)。<br>【起草時の記録】`recruitCountOf()` の clamp 上限 3 (= 4 人) を外せるかの**調査チケット**。本番の挙動は変えず、測って可否を出すだけ。⭐⭐⭐ **着手前実測で、コメント (`tavern.html:3266-3270`) が挙げる 4 つの理由のうち 3 つが既に成立していなかった** — スポーン枠は固定 4 枠でなく**代替候補 24 個**(`resolveAllyInitTile` `index.html:31518`)、隊列は zone 係数で人数非依存(`index.html:24662`)、準備画面のキャラタブは構成員でなく**全 6 職固定**(`renderCharTabs` `tavern.html:4784`。旧 `.partyComp` は死んだ CSS)。⭕ **本物のリスクはカメラだけ** = `computeCameraTarget` の隊列 bbox がクランプ区間を空にする枝(`index.html:6333`)。compact は 4 人で既に空だが人数非依存の `[compact-hero-anchor]` が救っており、**危ないのは desktop(今 0%)**。⚠⚠⚠ **コメントが言う「既存のカメラ回帰 (driver_camera)」は実在しない**(`tools/` に 0 本・`__camAnchorOff` を使う道具も 0 件)→ 測定装置は新規に作る。⭐ 測定台は #8 の `sweep_recruit_balance.js`(酒場経由)を流用。⛔ index 直起動の測定台を作ると人数指定が効かず全部 4 人になる |
| 15 | [2026-08-23_heromark-and-town-signplates.md](2026-08-23_heromark-and-town-signplates.md) | **完了 `372321c`**(新規 driver 46/46・`--negative` 4/4) | 100% | ①自キャラの頭上に ▽ 三角マーカー(街 + ダンジョンの両方) ②街の看板を丸アイコンから**羊皮紙の立て札**(施設名 + 説明 1 行)へ。ユーザー決定 = **両画面に ▽** + **札はテキストだけ(アイコン廃止)**。非退行: verify_town_map **85/85** / graph_p6 **244/244** / grid_p8 **56/0** / doors_p8 **15/15**。撤退=`?heromark=0` / `?signplate=0`(街とダンジョンで**独立に**読む)。<br>⚠⚠⚠ **依頼書 §4 A-2 の `placeUnscaledUi(…, -(hm-w), -46)` は dx が誤りだった** — `left = SX(wx + size/2) + (dx − size/2)` なので `dx = −半幅` だと**スプライト半分ぶん左へずれる**。正は `dx = size/2 − 半幅`(敵の装備バッジ `index.html:14606` と同じ規則)。⚠⚠ **`dy = −46` では名前ラベル(−28)と 1px しか離れない**(bob が +4px 下げる)→ **−50**(実測 gap 5.0px)。⭐⭐ **説明文は紙のまだら模様に食われて読めなかった** → `opacity: 0.85` を**外し**(斑な紙では透かした瞬間に死ぬ)、`--sp-ink-soft` を `#4a3413` へ、**クリームの薄掛け `--sp-wash: 0.30` を 1 層**追加。⭐ **`max-width: 210px` では 16 文字の説明(実測 208 layout px)が入らない** → 280px / padding 左右 16px。⭐ **desktop 1440x900 の実効 zoom は 0.8667** = 説明を依頼書どおり 12px にすると実効 10.40px で受入条件 10px に**余裕 0.4px しか無い** → 13px(実効 11.27px)。⚠⚠ **廃坑は camZ ≈ 0.346** = ワールド px と画面 px を取り違えると装置の閾値を誤る(実際に (Z2d) が一度赤くなった。実装の欠陥ではない)。⚠⚠ **負のコントロールのアンカーは「どのファイルの話か曖昧でない行」を選ぶ** — `platehide` の初版 `transform: translate(-50%, -50%);` は **index.html にも在り**、(Z3) の装置 assert が実行前に止めた。⚠ **iOS 実機の目視は未了**。<br>【起草時の記録】⭐⭐⭐ 着手前実測: **▽ を ally ごとに持たせると壊れる** — NPC 頭が死ぬと主人公が頭へ昇格し (`tryPromoteNewHead` `index.html:17776`)、その時 ally 側の DOM が `remove()` される → **▽ は 1 枚だけ作り毎フレーム主人公を引き直す**。⭐ 96px セルの**頭の天辺は箱の上端 + 32px**(6 職の row3 不透明上端を実測 = 32/33/33/34/32/32)。⭐ 紙は既存 `assets/parchment_plaza.jpg`(平均輝度 149.8 = セピア中間調)を**暗幕なし**で流用 → 新規アセット不要。⚠⚠ `verify_town_map.js` が看板を 4 点で縛る(id / 枚数 3 / **中心の elementFromPoint** / 変異アンカー 5 本の文字列)→ 85/85 非退行が受入条件。⚠ `index.html` を触るので **changelog 必須** |
| 16 | [2026-08-23_bandits-forest-single-room.md](2026-08-23_bandits-forest-single-room.md) | **完了**(非退行 12 本緑・新規 assert 18 本) | 100% | シナリオ2 を **8 ノード → 1 ノード**へ畳み、codex1 の卓上バトルマップ 1 枚 (52x26) だけで完結させる。ユーザー発端 = 「プレイしても出てこない」「手前の部屋とか古いの使ってない?」。⭐⭐⭐ 着手前実測: **1 ノード / kind:"boss" のグラフは本番の `lintRun` を error 0 / warning 0 で通る**(新規 `tools/probe_s2_fold.js --lint`)。⛔⛔⛔ **ただし 1 ノードでは kind は "boss" 一択**("search"/"loot" は `graph-no-boss` で error)→ `KIND_SPAWNS_TRAPS={search:1}` / `KIND_SPAWNS_ROOM_CHESTS={loot:1}` (`js/df-mapdef.js:2082-2083`) に引っかかり、**何も足さずに畳むと罠 8 個と宝箱 8 個が無言でゼロ**になる(廃坑 P8 が実際に払った代償)→ 2 関数に**第 3 引数 `extraKinds`**(既定は 1 ビット不変)。⭐ **呼び口は `applyNodeKindExclusions` (`index.html:4502`) ただ 1 箇所**・判定の出所も 2 関数だけ = index 側で kind を再判定しない。⭐ 実測 (`--kinds`) 敵 26 / 罠 8 / 玄室宝箱 8 / 檻 2 → 畳むと敵 10。⭐⭐⭐ **「出てこない」の正体 = 今は n7 に一度も到達していない**(実プレイ 3/3 敗北 R6/R8/R9)→ 畳めば 1 部屋目がその絵になり構造ごと解消。✅ **シナリオ2 にノードイベントは 1 本も無い**(`registerNodeEvent` の 3 本は全部シナリオ1)。⚠⚠⚠ **残影の獣の (36,13) は大部屋では北東の岩場の中**(#11 が名指しした起点タイル)→ 必ず移す・檻と同一タイル。⚠ **id は `n7` のまま変えない**(driver_grid_s2 93 と driver_graph_p6 244 の変異アンカーが 0 件ヒットで exit3 になる)。ユーザー決定 = 湧き水 rest は**消してよい** / ノード遷移 0 回で**いったんよい** / 罠 8・宝箱 8 は**総数そのまま**。撤退=`?s2fold=0` |
| 17 | [2026-08-23_town-tavern-bgm.md](2026-08-23_town-tavern-bgm.md) | **完了 `a030c04`**(新規 driver 17/17・`--negative` 15/15・非退行 37/85/85) | 100% | 街(`town.html`)と酒場(`tavern.html`)を合成トラック `tavern` から専用 mp3 へ。ユーザー決定 = **街 `village08.mp3` / 酒場 `酒場.mp3` の別曲**・出所は **5 曲とも魔王魂**。⚠⚠⚠ **`BGM_FILES.tavern` という ID は使えない**(`playBgm` が `TRACKS` より先に `BGM_FILES` を見るので、呼び口を直さないまま黙って mp3 へ逸れ、`renderBgmOffline("tavern")` だけが合成を指す二重定義になる)→ ID は `town` / `tavern_room`。⭐ ラウドネス実測済 village08 −14.6 → volume 0.54 / 酒場 −12.6 → 0.43(基準 −19.9 は `dungeon_normal` −15.5 × 0.60 で再現確認)。⭐⭐ **画面のクレジット行は `credit:` から生成されていない**(`audio.js:837` と `index.html:13404` は直書き)→ 5 曲とも魔王魂なので**画面側は変更不要**、`audio.js:331-333` の空 `credit` 3 件も同時回収。撤退=`?townbgm=0` |
| 18 | [2026-08-23_scenario2-clear-rate.md](2026-08-23_scenario2-clear-rate.md) | **完了 `faaaa61`**(新規 `tools/probe_s2_clear.js` 924行・装置assert 12本・`--negative` 6/6・**測定 84 走行**・非退行 golden 6/6) | 100% | シナリオ2 のクリア率 0% の**調査**(本番は 1 バイトも変えていない = 道具が起動時に `git diff HEAD` を引いて dirty なら測らない)。⭐⭐⭐ **9 腕 84 走行で敗北 82 件すべてが「主人公 1 人の死」**、`isPartyWiped()` は **0/82** — Lv・人数・射程・掃討・畳みのどれを動かしても例外ゼロ。被弾比 主人公/パーティ 中央 **0.702**、**19/84 走行で 1.000**(仲間の被弾ゼロ)。⭐⭐⭐ **`?s2fold=0`(8ノード版)はもっと悪い** — 10/10 が **n1** で **23.5 秒**・XP **275** で折れる(既定は n7 まで進んで 113.5 秒・XP 1190)⇒ **#16 の畳みは改善だった / 「場に居る敵を減らす」は逆効果**。⭐ **推奨 Lv4 でも 10%(1/10)** が上限(§2-5 の「0/12」は N=3 の観測だった)。⭐ **廃坑を実際に 1 周して装備込みでも 0/3**。⭐ **同時交戦 最大 12**(門+焚き火+ボス+召喚が 1 戦闘へ)、`?dndrange=0` で 8 へ割れて残敵 6→3・XP 1190→1715 と**最遠まで進む**が **0%**。⚠ §2-4 の「ボス撃破後に敗北」は **84 走行で 0 件**(ボス撃破 2 件は 2 件ともクリア)= 候補4 の効き目は未測定。⚠ 停滞は **84 走行 0 件**(`nodiag` 変異で検出器が生きていることも確認済)。⛔ 直していない — 候補 6 つを効き目の実測値付きで §12-5 に並べて終了。⭐ 次の一手の材料 = **候補1(敗北条件)と候補2(被弾配分)は同じ現象の表裏**。<br>【起草時の実測 25 走行】⭐⭐⭐ 着手前実測 **25 走行**で **「全滅」ではなく「主人公 1 人の死」**だと判明 — `isPartyWiped()` が **25/25 で偽**、仲間生存の中央値は **3/3**、Lv1 の 3 走行では**仲間の HP が 1 も減らない**。敗北は `onHeadDowned` の `if (heroIsHead) return false;` (`index.html:17949`) と、主人公が後衛のときの `ally.isHero` 4 箇所(`:24522`/`:27128`/`:29648`/`:31518`)。⭐ 一方で勝利は「**敵を 1 体残らず**全滅 + ボス撃破」(`:17746`)= **スカーを倒した後に残敵 2 で主人公が落ちて敗北**した走行が実在。⭐ **推奨 Lv 4(主人公 HP 60)まで上げても 0/12**(`dragonfighters.xp` を焼いて Lv1/2/3/4 を各 3 走行)。⭐⭐ 廃坑は同じ Lv1 で **2/3 クリア** — 敵の総数はほぼ同じで、違うのは**入場時に場に居る数**(廃坑 **2 体** vs シナリオ2 **10 体**)。⚠ #8 の「**n1** で折れる」は #16 の畳みで**失効**(既定に n1 が無い)。⚠ #16 の「間欠停止が測定を汚染」は本測定台では **25 走行 0 件**(打切 0%・`aborted` 0 件)。⭐ 停滞は本番の診断ウォッチドッグが既に `clear`/`defeat`/`aborted` で持っている → **本番に計測シームは不要 = changelog も鳴らない**。⛔ 直しはしない — 候補 6 つを数値付きで並べて終わる |
| 19 | [2026-08-23_action-priority.md](2026-08-23_action-priority.md) | **完了 2f33dfd** | 100% | 出発の準備画面で「全般/雑魚/ボス/道中」ごとの技の優先度をプルダウン指定。⭐ 主人公は既存の重み抽選(`pickLeaderAction`)に倍率 1 行、仲間は if 連鎖の先頭へ先出し 1 段(ゲート 20 本は上げるだけ)。⚠⚠⚠ 倍率は `LEADER_W_MAX` クランプの**後**に掛ける／道中バフは戦闘開始の `resetPlayerBuffs()` で**主人公だけ**剥がれる／1戦1回スキルは戦闘開始で使用記録が消えるので道中に許すと 2 回撃てる。撤退=`?actionpri=0` |
| 20 | [2026-08-25_title-bgm-opening.md](2026-08-25_title-bgm-opening.md) | **完了 `5531ffb`**(新規 driver 16/16・`--negative` 14/14) | 100% | タイトル画面(記録選択 + 名乗り)へ専用 BGM `opening.mp3` を敷く。⭐ #6 が「別チケット」として明示的に残していた宿題で、見送り理由(遷移先と同じ曲が頭出しへ戻る)は街が別曲なので消えている。⚠⚠⚠ **`unlock()` は「モジュール内部の」`playBgm` を呼ぶ**(`audio.js:119`)ので `GameAudio.playBgm` を包んだスパイでは **pendingBgm からの再生が永久に見えない** → 呼び口は `town.html` と同じ **ロード + 最初の pointerdown の 2 本**、検証は `__bgmFileState()` と 2 経路。`silent` 変異が (0a)(2a) だけを赤くし **(2b) は緑のまま**という形でこの罠を機械証明した。⚠⚠ 既存 golden `verify_title_screen.js` の**受入条件 10 が「BGM 0 回」を assert していた**ので反転(段数は減らさず `=== 1` + ID 一致へ)。⚠⚠⚠ その golden は **83/83 ではなく 85/85**(レシピ集の記録が古い)。⚠⚠ **`driver_bgm_town` の (0b) が `__bgmFiles()` の件数 9 を直書きしていた**ので曲を足した瞬間に赤 → 9→10 へ更新(退行ではない)。⚠ **変異アンカーは部分文字列一致**なので、ロード時の呼び口(インデント 4)は pointerdown 内の行(インデント 6)に含まれてしまい 2 箇所ヒットした → 末尾コメントまで含めて一意にした。⭐ 実測 opening **−10.3 LUFS → `volume 0.33`**(実効 −19.9 = 基準どおり)/ 出所 = 魔王魂(ID3 タグ無し・ユーザー確認)→ 画面のクレジット行は変更不要。⛔ 鳴り出しは **最初のタップから**(ブラウザの自動再生ポリシー)。⛔ 依頼書 §8 (4b) の「既存 9 件の volume も固定」は **#17 の『耳で下げてよい』決定を壊す**ので id/src/credit だけに縮めた。撤退=`?titlebgm=0`(無音へ) |
| 21 | [2026-08-25_world-map-entry.md](2026-08-25_world-map-entry.md) | **完了 `95fcd35`**(新規 driver 55/55・`--negative` 40/40) | 100% | キャラ選択の後とダンジョン帰還の両方へ、1 枚絵のワールドマップ `world.html` を挟み、主人公の駒を街道の上で歩かせる。ユーザー決定 = **駒を歩かせる / 帰還も挟む / 素材はこのまま + 拠点はコードで重ねる / 導線は点線 / 6 シナリオの地にも札(計 7 枚・入れるのは港町だけ) / BGM は `fierd.mp3`(魔王魂)**。⭐⭐⭐ **通行マスクを作らない** — 街道の描き幅は 1 マス 64px に対し ~10px しかなく量子化すると絵と食い違う → **ノード + エッジのルートグラフ**を持ち、そのエッジを画面の点線として描く(線とデータは同一配列 = 罠 C)。ユーザー追加指示「歩ける導線はルート線で分かるように」がこの方式そのもの。⚠⚠⚠ **間に 1 ページ挟むと一回性キーが 3 本壊れる** — `exitVia`(town が消費)/`enterVia`(酒場が消費)/**`lastResult`(酒場 `:4121` が消費 = 消すとリザルト画面が黙って出なくなる)**→ world は 3 本とも**消さない**。⚠⚠ **`?town=0` の判定より前に world へ飛ばすと撤退路が死ぬ** → 順序は `?town=0` → `?world=0` → `world.html`。⭐ 帰還地は既存 `sessionStorage["dragonfighters.currentScenario"]` で分かる(`removeItem` は全文 grep で 0 件)= index 側に新しい書き込み不要。⭐⭐ **水マスクは完全に分離する**(海 100.0% / 湖 72.1% / 陸 0.0%)ので「線が水を渡らない」は機械 assert にできるが、⚠⚠ **道マスクは東半分(岩山・溶岩)で使えない**(岩肌の暖色が同じ色域)→ 道の有無は目視。⭐ 素材は 1536x1024 = 24x16 マスにちょうど乗る(焼き直し不要)/ `codex1\` 直下の同名ファイルと sha256 一致。⭐ 素材には**港町 + 3 集落 + 街道網が既に描かれている**(港町 = タイル (5,8)-(6,9)、桟橋 2 本)が、⚠⚠⚠ **砦も神殿も坑口も描かれていない**ので 6 枚の札は**描かれた集落を避けて地形で置く**(全候補 water 0.0% / 最小間隔 2.24 タイル)。⭐ 札の文言の唯一の正は **`tavern.html` の `place:`**(`:2221` 他)で、`world.html` からは 6,800 行の別ページを読めないため**意図的に重複**する → (7a) が **tavern.html の実体と機械照合**してドリフトを止める。⭐ 点線は**明色 + 暗い縁取り 1 色**でよい(全区間の直下 luma mean 72.2〜116.7 / max 146.6 を実測)。⚠ `fierd.mp3` −13.5 LUFS → **volume 0.48**・ID は **`world`**(`field`/`town` にすると #17 の罠)・⛔ volume は assert しない。⚠ **`driver_bgm_town` の (0b) が件数直書き 10 なので曲を足すと必ず赤**(→11、退行ではない)。⚠ `verify_title_screen.js` は遷移が 1 段増えるので**必ず赤くなる** — 期待値を緩めず #12 と同じ「測定点を通り抜けた後へ移す」で回収。撤退=`?world=0`(`?town=0` とは独立) |
| 22 | [2026-08-25_town-world-exit.md](2026-08-25_town-world-exit.md) | **完了 `3dda3f5`**(新規 driver 23/23・`--negative` 4/4・PENDING 0) | 100% | 街から地方全景へ「出る」導線。港町フランの北の街道口 **(6,0)** に 4 枚目の立て札「町の外へ」(`sign` は (5,2))。⭐⭐⭐ **`exitVia="town"` を 1 行書けば `js/world-map.js:170` の fail-safe が `phlan` を返す**ので **`js/world-map.js` も `world.html` も 1 文字も触らずに成立した**(`verify_world_map.js` が**無変更**で 55/55・`--negative` 40/40)。⭐ `noexitvia` 変異が「書かないと駒が `pier` に立つ」= ⚓ アンカーの『新キー不要』という誤りを機械的に再現。⚠⚠⚠ **起草の主張が 3 件崩れた** — ① §2-5 の「札は `max-width:280px` に張り付く / (6,1) だと 4px 交差」は**偽**(実測は全部 client 210px = stage 240px。交差するのは中心間 **3.75 タイル未満**だけで (6,1) = 4 タイル差では 12px の隙間が残る)→ 負のコントロール `signcrowd` を **(7,1) = 3 タイル差**へ変更して (1b) だけが赤くなることを機械証明(本番の (5,2) は据え置き)。② **札の幅は一律ではない**(`shop` だけ 165px = 文字数で決まる)。③ §2-3 の「件数直書きは 16 箇所」は過小で **実際は 17 箇所** — 数え落としは **(8c) の `=== 2`**(`gate` は `requiresPlazaUnlock` を持たないので**闇市の解禁前から出る**)→ ⭐⭐⭐ **件数直書きの grep は「増える側」だけでなく「減る側 / 別の母集団の数」も掛ける**。⭐⭐ **出口タイルの到達は `pagehide` で「消える直前」を同期で焼き付けて遷移先から読む**(着いた rAF の中で `location.href` が走るのでポーリングでは原理的に間に合わない)。⭐⭐ **「不変」は番兵を置いてから測る**(`lastResult` の null 同士の一致は証拠にならない)。⚠⚠ ドライバの purge は `__drvSeeded` で **1 タブ 1 回**なので (5b) は**同じタブで `page.goto` を 2 回**踏む(新しいタブだと空振り)。⭐⭐ 撤退の受入は「OFF で緑」でなく **同じ conjunction を ON/OFF の両方へ当てて崩れる**((5c))。既存 golden **8 本すべて非退行**(85/85・46/46・55/55・86/86・30/30・17/17・16/16・37/37 = **期待値の書き換え 0 件**)。撤退 = `?world=0`(#21 と共有) |
| 23 | [2026-08-25_quest-walk-to-site.md](2026-08-25_quest-walk-to-site.md) | **完了 `e7f3ea1`**(新規 driver 25/25・`--negative` 46/46・PENDING 0・dev-loop 4 項目 `85b6de3`/`f0f185c`/`d588584`/`e7f3ea1`) | 100% | 受注した依頼の地まで**地方全景を歩いて向かう** + **未解放の拠点を地図から隠す**。⚠⚠⚠ **未解放ノードを「歩けなく」すると開始直後に廃坑へ到達できず詰む**(起草時に実グラフで実測 — `cleared=[]` で未解放 5 拠点を消すと phlan から届くのは `phlan,pier,cross_n` だけ。街道網は**環状**なので **1 つずつ消す単体テストでは永久に緑**)→ **隠すのは見た目だけ**にし `EDGES`/`findPath`/当たり判定は 1 バイトも触らない。⚠⚠ **地図を 1 段挟むと `autoplay`/`evade` が確実に落ちる**(遷移先にクエリを足さない確定作法のため。`probe_s2_clear` / `sweep_recruit_balance` の 2 本が死ぬ)→ dev URL では素通し。⚠ **`exitVia` を書かずに飛ぶと駒は港町でなく桟橋に立つ**(`world.html:476` が空を `"title"` と読む)。⭐ 既存 `verify_world_map.js`(**2026-08-25 実測 55/55**)は (7b-dom)/(7d)/(7e) の母集団が壊れるが、**装置へ `cleared`=6 本を仕込めば 55/55 のまま**(⛔ assert の文面も本数も減らさない)。⭐ ユーザー決定 3 件 = 出発は地方全景へ直行 / **生成クエストは据え置き**(`id` は 2 経路とも `"generated-quest"`)/ 入場は確認をはさむ。撤退 = `?questwalk=0`。**【実装で判明】** ⚠⚠⚠ **`tavern.html` は `js/world-map.js` を読み込んでいなかった**(§5-2 は見える前提で書かれていた。1 行足して解決 — 無いと `viaWorld` が永久 false =「何も起きないのに (1c)(4a)(4c)(4d)(4e) が全部緑」)。⚠⚠ **§2-3 の「影響なし」表は誤りで `verify_recruit_size.js` が実際に壊れた**(遷移の横取りが `/index.html` だけ → 酒場のタブが本当に `world.html` へ行き `Execution context was destroyed`。正規表現を `/(index\|world)\.html/` へ広げて 82/82 復帰)。⭐ **「着地先を見ない」は「遷移しても平気」ではない**。⚠ 母集団が壊れたのは 3 本でなく **2 本**((7e) は札でなく**ノード**を押すので減らない)。⚠ `verify_title_screen` の golden は 83/83 でなく **86/86**。⚠⚠ 変異 `eatquery` は条件①だけ潰しても (4d) が緑のまま(`target += params` で条件②が代わりに止めていた)= **dev URL を素通しさせているのは①②の 2 つ**。⭐ 確認ダイアログは `onclick` でなく `bindAsk()` で 1 回だけ登録(`click` + `touchend` 併用)。**残 = §9 の実機体感 4 件**(歩く距離の体感 / 未解放の点の見た目 / compact の文字サイズ / iOS のタップ) |
| 24 | [2026-08-26_codex-map-request-skill.md](2026-08-26_codex-map-request-skill.md) | **完了 `ddf1857`** | 100% | Codex へ MAP を発注して DF のタイル格子へ乗せる作法を `.claude/skills/codex-map-request/` へ。新 driver `tools/verify_codex_map_skill.js` **17/17 PENDING 0 / --negative 21/21 空振り 0**。⚠⚠⚠ **スキル 4 枚は git に入らない**(`.gitignore:2` が `.claude/` を丸ごと除外。既存の impl-request / dev-meeting も同じ)→ commit に入るのは `tools/` の 2 ファイルだけ。⭐⭐⭐ **消えた測定器は「復元」できた** — `line_response`/`comb_score` が生存しており、`phlan-harbor` の 6 数値を**完全一致**で再現。⚠⚠⚠ **罠 D(新規): 中心無しの広域探索は 3 方式とも倍音を拾う**(素朴 argmax / 反櫛の差 / 離散フーリエ。廃坑入口で 91.40 = 2x45.70 が本物に勝ち、港町は基本波より 32px 成分が強い)→ `--fit` は**探索中心を必須**にした。⚠⚠ **§4 のコード片と (1b) が矛盾していた**(tile を中心にすると周期 45.70 は原理的に測れない)。⭐⭐ **(3a) の基準は「控えた SHA」でなく `assets/` の現物**(焼き直しがバイト一致すると実測)。詳細は §12 |
| 25 | [2026-08-26_stag-tavern-dnd-map.md](2026-08-26_stag-tavern-dnd-map.md) | **完了 `037726a`**(新規 driver `verify_tavern_map` 42/42・`--negative` 66/66・PENDING 0・dev-loop 4 項目 `8036ce5`/`638b479`/`4aaea2b`/`d80e7f7`/`037726a`) | 100% | 銀の鹿亭を 1 枚絵から**歩ける D&D マップ**へ(港町フラン方式)。卓 3 つ = シナリオ1〜3。⚠⚠⚠ **`#tableArea .table` を押す golden がちょうど 3 本**(`verify_recruit_size:333` / `verify_quest_walk:636` / `driver_depart_menu_clean:147`)あり、歩きを 1 段挟むと #23 と同じ壊れ方をする → セレクタを `#questTable_goblin-mine` へ。⭐⭐⭐ **`goToTable` には town.html の `goToFacility` に無い「同じ卓へ歩行中の再クリックを無視する」ガードが要る**(ドライバは 420ms ごとに押し続け、`walkPath` は先頭で `stopWalk()` して `t0` を打ち直す = `MS_PER_TILE 340` に対して 420ms でぎりぎり進むのは**偶然であって設計ではない**)→ 変異 `reclick` が機械証明。⚠⚠ `@media (max-width:560px)` の 2 列スクロールグリッド(`position:relative !important`)はマップと原理的に両立しない → **削除せず** `body:not(.tavernMapOn)` で括る。⚠⚠ **シナリオ4〜6 は「奥の間へ」の暫定扉へ逃がす**(消すと進行が詰む。新しい建物は作らず既存 `renderTables()` を流用し、#26 で扉ごと撤去)。⚠ `js/world-map.js:24` の「`tavern.html:2221`」は実体 **2241** で 20 行ズレ。⚠ `tavern.html` は **CRLF**(`git diff` では気づけない)。ベースライン実測(2026-08-26) = recruit **82/82** / quest_walk **25/25**+neg **46/46** / depart_menu **41/41**。撤退 = `?tavernmap=0`(ページ単位で完結・遷移をまたがない) ✅ **完了 2026-08-28**。マス = **96px**(依頼書の 64px から逸脱 — 素材の格子が 99.8x97.5px なので 64 だと 1.31 倍の拡大でぼける)。⭐⭐⭐ **壊れた golden 3 本は待ち時間を 1ms も伸ばさずセレクタ 1 行(`'#questTable_goblin-mine, #tableArea .table'` = カンマ区切りは文書順で 1 件返す)で復帰**。⚠⚠ **依頼書 §9 の `driver_bgm_town` 37/37 は誤記** — 新設時から一貫して **素 17/17 + neg 15/15**(他 5 枚の依頼書が 17/17 と記録。`git diff 7394692 HEAD -- tools/driver_bgm_town.js audio.js` が空 = 退行ではない)。⚠ 逸脱全 7 件と実測は **§13**。残 = §10 の実機体感 6 項目(特に **`#fireplace` の x50%/y58% は旧 1 枚絵基準なので必ずずれる**)。 |
| 28 | [2026-08-28_ability-scores-5e.md](2026-08-28_ability-scores-5e.md) | **完了 `aea44a8`** | 100% | 能力値を 5e の 6 能力(CHA 込み)へ一本化する土台。**UI は作らない**。⭐ 修正値を B/X → 5e 式へ。**実測は 12 判定中 7 判定が +1・下がる判定は 0**(⚠ 依頼書の予告 6 は誤記だった)。新 driver `verify_ability_scores` **素 24/24 / --negative 39/39 / PENDING 0**。⚠⚠ **`title.html` だけ改行が LF**(他 4 枚は CRLF)。⚠⚠⚠ **委譲すると単体注入の golden 3 本の母集団が静かに 0 になる** → 装置側で復元(期待値は不変)。⭐ 変異 `shadow` は「振る舞いが 1 つも変わらない欠陥」で (3b) だけが赤くなる。撤退 `?ability5e=0` |
| 29 | [2026-08-28_player-sheet-v1.md](2026-08-28_player-sheet-v1.md) | **完了 `3e42886`**(新規 driver `verify_player_sheet` 素 42/42・`--negative` 57/57・PENDING 0・空振り 0・dev-loop 4 項目 `af08a3a`/`702ef0e`/`20150af`/`3e42886`) | 100% | いつでも開けるキャラクターシート(閲覧専用)+ 言語「識る言の葉」。⛔ **#28 の後**(完了済)。⚠⚠⚠ **依頼書 §2-1 の「呼び出し口は 2 経路」は誤りで実際は 3 経路** — `town.html` は `position:fixed` が 0 件ではなく **3 件**で **`#townHud` が下端全幅を占有**(`left:0;right:0;bottom:0;z-index:11`)、`world.html` も **5 件**。→ `pickHost()` = **partyPanel > 表示中の townHud > body へ fixed**。⚠⚠ **`#townHud` はデスクトップで `display:none`** なので「在るか」でなく「**表示中か**」で分岐(在るかだけだとデスクトップの町で永久に押せない)。⚠⚠ **ボタンは `<button>` でなく `<div role="button">`** — `verify_town_map` の (11b) が `#townHud button` を数えているので 5 本目の `<button>` を足すと即赤。⚠⚠ **`index.html` の `hp`/`playerStats` は隊列の先頭**なので主人公が中衛/後衛だと **NPC の HP/AC を「自分の体」として出す**(`!heroIsHead` で `heroRef` へ落として修正)。⚠⚠⚠ **`verify_title_screen` の (2c) は期待値そのものが新仕様と衝突**(「職を選べば出発が有効」)→ 装置追加では直らず**弱めずに強めて**(職だけでは disabled → 言の葉で有効、の 2 相)86/86 復帰。⚠⚠ **(4d) は `newGame()` の prefix 総なめで単純差分では測れない** → `?sheet=0` 出発との**集合差**へ。⭐ **技能は「行ごと伏せる」/ 職業名は自前表 + `HERO_CLASSES` 優先の fallback**(`js/hero-classes.js` は title/town/world の 3 枚だけ・冒頭コメントは実物とズレ)。⭐⭐ **変異の `evaluable`/`allowRed` は `--mutate` 単体実測から決める**(7 本中 5 本が担当外を巻き込む)/ ⛔ **測っていない節を `evaluable` に載せない**(母集団 0 の述語は一律 false = 偽の赤)。⭐ `fixedsave` は **(3d) が緑のまま**(`languagesOf` が重複を潰す)= 表示のテストでは保存の欠陥を捕まえられない。⭐ `fixedbtn` は **(1a)/(1c) が緑のまま**(z-index 62 で押せてしまう)= 「どこにマウントされたか」を見る (1b) だけが網。⚠ **装備欄は出さない** — 装備の唯一の正が無く town/world/title では原理的に表示できない(#30)。⚠ **`title.html` だけ改行が LF**(他 4 枚は CRLF)/ ⛔ **改行の実測に grep の CR カウントを使わない**(Bash 経由で空文字に化け全行ヒット = 嘘の測定。`py` のバイト数えで測る)。golden **8 本すべて非退行**(86/86・85/85・42/42・57/57・24/24・13/13・82/82・25/25)。撤退 = `?sheet=0`。**残 = §10 の実機体感 4 項目 + §7 の絵(`assets/sheet_frame.png` は未発注・404 でも読める)** |
| 34 | [2026-08-28_warrior-ally-action-priority.md](2026-08-28_warrior-ally-action-priority.md) | **承認済** | 0% | 戦士の仲間にも「行動の優先度」を効かせる。⭐ **酒場側は既に 6 職すべてに欄がある**(`(S7)`/`(6a-1)` が機械保証・触らない)。⚠⚠⚠ `ally.buffs.skipNextTurn` は**誰も読まない死にフィールド**(渾身の一撃のデメリットが黙って消える → `ally.stunned`)。⚠⚠ `executeWarriorSkill` は**プレイヤーのグローバル直触り**(仲間に通すと主人公が回復する)。⚠⚠ `apIsWastedCast` に 士気高揚/鉄壁の構え の枝が無い。⚠⚠ `(2e)` のゲート数 **20 は直書き** → 24 へ。撤退=`?warally=0` |
| 35 | [2026-08-28_party-match-loadout.md](2026-08-28_party-match-loadout.md) | **承認済** | 0% | マッチング画面のカードを押すと引き出しが開き、全員分のスキルと傾向をその場で設定。⛔ **#34 の後**。⚠⚠⚠ **背景タップ = 出発を廃止するので golden 4 本が全部止まる**(75/75・82/82・25/25・41/41 が `partyMatchOverlay.click()` / 画面中央クリックに依存)→ `#pmDepart` へ付け替える。⚠⚠ **iOS は `touchend` も止めないと `<select>` を閉じた指で出発する**。⚠⚠ `renderSkillItem` の末尾は `renderCharLoadout()` 直結 = 引き出しが更新されない → 再描画点を 1 つに畳む。⚠ 同職 2 人は同じ設定を共有(設計どおり・注記を出す)。撤退=`?pmsetup=0` |


⭐ **#8 は 2026-08-23 に決着(`11e4678`)。** 廃坑だけ `recruit: 3` へ個別上書き(クリア率 15% → 40% / XP 中央 580 → 1,890)。⚠ ここから **2 つが宿題として残った**:
  1. **シナリオ2 の「n1 で折れる」** — クリア率が両腕とも **0%** で床に張り付き、募集人数では動かない(ユーザー決定で #8 から分離)。→ ✅ **#18 として起草・承認済**(2026-08-23)。⚠ ただし「**n1** で折れる」という診立てそのものが #16 の畳みで失効している(#18 §2-2)
  2. **沼 `lizard-swamp` が未測定** — ★2 で #7 により NPC が 3→2 に減っているのに、#8 §3-5 の規模決定(対象を 2 本に絞る)で母集団から漏れた。1 と同じ ★2 なので一緒に扱うのが自然

⭐ **#16 は 2026-08-23 に着地。** シナリオ2 は既定で **1 ノード (52x26 の卓上バトルマップ 1 枚)**。
罠 8 / 玄室宝箱 9 / 檻 2 / 残影の獣 1 が大部屋の中へ戻った。撤退 = `?s2fold=0`。
⚠ ここから **宿題が 2 件**残った:
  1. **n7 大部屋での間欠停止** — 指紋 `sig = 39/16/9/2/n7/330/0`(タイル (39,16) / 生存敵 9 /
     `hero.pathLen: 0`)。**6 走行中 3 回**。⛔ **静的な配置の欠陥ではない**(敵 12 タイルすべて
     床かつ本番 `aStar` で到達可を実測) = 実行時に隘路へ体が詰まる系。**#11 が出荷した大部屋に
     元からあった**もので #16 が作った欠陥ではないが、畳んだことで**毎回 n7 に入る**ように
     なったため当たる機会が増えた。**別チケット未起草**
  2. **シナリオ2 のクリア率は 0/3 のまま** — 敵は 26 → 11 体に減ったが、まだ全滅する。
     → ✅ **#18 として起草・承認済**(2026-08-23)。⭐ 実測 25 走行で、真因の候補は「敵の数」ではなく
     **主人公 1 人の死が敗北条件であること**へ移った(仲間は生きている)。
     数値調整は #8 の宿題(#8 の 120 走行は畳む前のシナリオ2 を前提にしているので測り直し)

### 進行度の見方 / 完了ぶんの置き場

- **決着したものは [`完了/`](完了/) へ移設する**(2026-08-22 に 7 枚を移動)。
  一覧の行は残す — 行を消すと「なぜそう決めたか」への導線が切れるため。**リンク先だけが `完了/` を指す**。
  ⚠ 移設した .md 内の相対リンクは `../` → `../../` へ直してある。新しく移すときも忘れずに直すこと。
- **進行度の出所**は依頼書ごとに違う。勘で書かない:
  - 完了/見送り = **100%**(コミットハッシュが出所)
  - 実装中 = **dev-loop の `DEV_QUEUE.md` の消化数**。⚠ 数字はすぐ腐るので
    **生の進行は `~/.claude/dev-loop/DEV_QUEUE.md` が唯一の正**。
    (実例: #6 は 2026-08-22 22:03 時点で 4 項目中 3 項目 = 75% と書いていたが、その数十分後に完走した。**% はすぐ腐る**)
  - 未承認 = **0%**。ただし #11 のように「起草時の実測」が済んでいるものはある(実測 ≠ 実装)

⚠ **1 → 2 の順は守ること**。2 は 1 の STEP3 で作る `unitBodyTiles` の比率を出所として使う(判定の出所を 2 つ持たないため)。
⚠ 3 は 1 が終わってから。1 が外周を、3 が内側を担当する分担になっている。
⭐ **4 は 1〜3 と独立**(触る場所が `audio.js` と BGM の表だけ)。1 の待ちに入ったら先に 4 をやってよい。
  → 2026-08-21 に 1 → 4 → 2、2026-08-22 に 9 → 10 → 3 の順で消化した。**1〜4 と 9・10 は決着済み**。
  → 2026-08-22 に **5 も完了** (`dde8457`)。同日 **6 title-screen も完了** (`9f8b1bd`)。
  → **次は 7 / 8 / 11。3 枚ともまだ未承認**なので、着手前に 1 枚ずつ内容を確認すること。
⭐ **11 は 5〜8 と完全に独立**(触る場所がマップ/絵まわりだけで、開始画面とも `DFSlots` とも重ならない)。
  ただし **11 は敵を 5 体 → 9〜10 体に増やす**ので、**難易度の実測は 8 へ合流させる**
  (8 に「廃坑」だけでなく「シナリオ2 の n7 大部屋化ぶん」も宿題として乗る)。

✅ **#6 → #12 の依存は解消済み**。#12 は `title.html:518` / `:540` の遷移先を `town.html` へ
  書き換えるので #6 の完了を待っていたが、**#6 は 2026-08-22 に完了(`9f8b1bd`)**したので着手できる。
⭐ **#12 は #7 / #8 / #11 と独立**(触る場所が新規ページと絵まわりで、募集人数ともシナリオ2 のマップとも重ならない)。

⭐ **#13 は #12 と独立**(触るのが `runEncounter` の戦闘ループだけで、街ページとも募集人数とも重ならない)。
⛔ ただし **#13 → #8 の順は守る**。#13 は「1 部屋 = 1 戦闘」へ寄せるので、エンカ終了ごとの回復 3 種(HP+33% / スロット 25% / セカンドウィンド)の**貰える回数が減る**。**難易度は必ず上がる方向**なので、数値の再調整は #8 でペア比較してから。

### 5〜8(開始画面まわり)の順序

会議記録: [dev-meetings/2026-08-20_ゲーム開始画面.md](../dev-meetings/2026-08-20_ゲーム開始画面.md)

- ⚠ **5 → 6 は必須**。6 は 5 の `DFSlots` API を使う。**5 は UI を作らない**(土台だけを先に固める作法)
- ⚠ **7 → 8 は必須**。7 は `recruit` の器だけ作り、**値は 8 で実測してから入れる**
- ⭐ **(5,6) と (7,8) は互いに独立**。並行して進めてよい。触る場所が重ならない
- ⛔ **8 を飛ばして出荷しない**。7 だけ入れると ★1 の廃坑が NPC 1 人になり、**最初のクエストが最も過酷**になる

### 5〜8 から派生した未起草チケット(必要になったら書く)

- タイトル専用 BGM(`audio.js` に `title` シーンを追加 + 登録不要サイトから mp3 調達)
- タイトルのロゴ画像(codex1 への別依頼。Phase 1 は文字組みで通す)
- ~~パーティ 5 人以上の可否調査~~ → **#14 として起草済み**(2026-08-23)。⚠ 起案理由だった「味方スポーン枠・隊列・UI 枠が 4 人前提」は**実測で 3 点とも成立しないと判明**した。残る論点はカメラのみ

## 使い方

1. こちらの窓で計画を 1 件 = 1 ファイルとして起草する
2. ユーザーが内容を確認・承認する
3. 実装窓に「`実装依頼書/<ファイル名>` を読んで実装して」と渡す
4. 実装が終わったら見出しのステータスを `完了 <コミットハッシュ>` に更新する

## 命名規則

`YYYY-MM-DD_<slug>.md`(例: `2026-08-20_mine-detour-balance.md`)
slug は英小文字ケバブケース。1 ファイル = 1 まとまりの実装単位。

## 依頼書の書き方(最低限これだけは書く)

- **ステータス**: 起草中 / 承認済 / 実装中 / 完了 `<hash>`
- **目的**: 何を実現したいか(手段ではなく目的で書く)
- **背景・現状**: 今どうなっているか。関連ファイルは `index.html:1234` 形式で指す
- **変更範囲**: 触るファイルと、触らないと決めたファイル
- **受入条件**: 実装窓が自分で「終わった」と判定できる条件。可能ならドライバ名まで書く
- **撤退スイッチ**: `?feature=0` 形式の URL パラメータ名(本プロジェクトの慣習)
- **やらないこと**: スコープ外を明示して、実装窓の暴走を防ぐ

## 関連

- 設計書(長期の設計メモ): `~/.claude/plans/`
- Codex へのスプライト依頼文: `C:\Users\PC_User\Desktop\codex1\requests\`
  (こちらはアセット工房への依頼。本フォルダは**本体コードの実装依頼**)
