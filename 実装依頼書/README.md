# 実装依頼書

**用途**: 別窓(実装窓)が稼働している間、この窓で立てた実装計画を置く場所。
実装窓はここの `.md` を 1 枚読めば作業に入れる、という粒度で書く。

## 一覧(新しい窓はここを最初に読む)

上から順に着手する。ステータスが「完了」のものは読まなくてよい。

| # | 依頼書 | ステータス | 進行度 | 一行 |
|---|---|---|---|---|
| 1 | [2026-08-20_mine-wall-clipping.md](完了/2026-08-20_mine-wall-clipping.md) | **完了 `7c59c44`** | 100% | 廃坑の外周 1 タイルを塞ぎ、ゴブリン戦車を 176px + footprint 判定へ |
| 2 | [2026-08-20_wallbox-hardcoded-72px.md](完了/2026-08-20_wallbox-hardcoded-72px.md) | **完了 `f8c3bba`** | 100% | 壁の当たり判定の箱を実測比率へ。⚠ 箱の中心はスプライト中心から ±28px に制限(絵の中心をそのまま追うと下隣のタイルへ出る) |
| 3 | [2026-08-20_paint-mask-subtile.md](完了/2026-08-20_paint-mask-subtile.md) | **見送り `5cfe1e1`** | 100%(調査) | 1 枚絵マスクの内側の半マスズレ。STEP1(道具・実測・目視)まで実施し、**マスク編集は見送り**。⚠ STEP1 の道具は #1 で既に作られていた。⭐ ノード部屋の内側に岩盤は 0 マス = 赤は全部マスク由来 |
| 4 | [2026-08-20_bgm-mine-swap.md](完了/2026-08-20_bgm-mine-swap.md) | **完了 `4090a99`** | 100% | 廃坑の BGM を 3 曲へ(入口 d1 / 坑内 haikou / グリクス戦 boss01) |
| 5 | [2026-08-20_save-slots.md](完了/2026-08-20_save-slots.md) | **完了 `dde8457`** | 100% | セーブスロット 3 つ。**キー名は 1 文字も変えない**(ライブ + アーカイブ方式)。UI なし。STEP1-7 完了(`89f58a9`/`87ffca8`/`b92444e`/`dde8457`)。検証 `tools/verify_save_slots.js` 30/30。⭐ **3 スロット満杯で 78,326 bytes = 5MB の 1.49%** = quota は問題にならない。⭐⭐⭐ 受入条件 7 は「?slots=0 で緑」ではなく **同じ assert 本体を両ページへ当てて「外すと赤」**で測る。⚠ 実機(iOS Safari)で `pagehide`/`visibilitychange` が発火するかは未確認 |
| 6 | [2026-08-20_title-screen.md](完了/2026-08-20_title-screen.md) | **完了 `9f8b1bd`** | 100% | `title.html` 新設。新規/続き → 名乗り(主人公選択)→ 酒場。以後クラス変更不可。dev-loop 4 項目で完走(`a242ab1` STEP1 / `720006b` STEP2-3 / `30e5134` STEP4-6 / `954c41c` STEP7-8 / `9f8b1bd` 実装結果節)。検証 `tools/verify_title_screen.js` **83/83**・既存 golden 7 本非退行。撤退=`?title=0` / `?herolock=0`。⭐ 着手前の実測で **行番号が 8 件中 8 件ズレていた**(`d4ee24d` で訂正して依頼書へ書き戻し) |
| 7 | [2026-08-20_quest-recruit-size.md](2026-08-20_quest-recruit-size.md) | **完了** `459c34a`(dev-loop 4 項目・検証 79/79) | 100% | 「募集 → 増援」。★の数で同行 NPC が 1〜3 人に変わる。**器だけ作り値は入れない** |
| 8 | [2026-08-20_recruit-balance-retune.md](2026-08-20_recruit-balance-retune.md) | **完了 `11e4678`**(120 走行・golden 8 本緑) | 100% | 7 の後に難易度/XP を**ペア比較で実測してから**調整。廃坑の既存宿題も同時に回収。⚠ **2026-08-23 の着手前実測で本文を全面改訂**(旧本文の測定計画は成立しなかった)。⛔⛔ **`?recruit=0` は `index.html` では効かない** — `isRecruitOn()`/`recruitCountOf()` は `tavern.html` にしかなく、既存ドライバ(`auto_debug_run.js`/`probe_n4_stall.js`)は index 直起動なので**両腕とも計 4 人**になる。⭐⭐⭐ 正しい測定台 = **酒場を本番どおり通して `departToScenario()` に遷移させる**(`autoplay` は index へ引き継がれる。実測で OLD=計4人 / NEW=計3人 に割れた)。⛔ **前回窓の「4 試行中 3 回全滅」は #7 のせいではない** — 全部 index 直起動 = 旧仕様の計 4 人だった。✅ **XP は頭割りされない**(`gainXP` は主人公の `currentTotalXp` 一本。呼び口 4 箇所)→ 人数を理由に XP を触る根拠は無い。⭐ 規模はユーザー決定で **シナリオ2 + 廃坑 の 2 本 × N=20 ペア**に限定(★3 以上の 4 本は #7 で人数が変わらず差が原理的にゼロ) |
| 9 | [2026-08-22_boss-latch-during-combat.md](完了/2026-08-22_boss-latch-during-combat.md) | **完了 `24dbb60`** | 100% | 戦闘中も「玉座へ詰めたか」を見るラッチ点を `runEncounter` の手番ループへ 1 本。⚠ 負のコントロールは実プレイではなく **`tools/probe_boss_latch.js`**(`--index` で修正前の index.html を配れる)。⭐⭐⭐ **「戦闘中だったか」は「同じ戦闘だったか」ではない**(`encounterStartedAt` で見る) |
| 10 | [2026-08-22_mine-s4-guard-fog.md](完了/2026-08-22_mine-s4-guard-fog.md) | **完了 `5a0e3c3`** | 100% | 廃坑 s4 へ実質行けない。⚠ **本文の「真因」は実測で訂正済**(霧ではなく**索敵 12.5 タイル AND 視線**)。採用は **案 1 修正版 = 護衛を (51,20)/(51,22) へ**。末尾の「決定」節が唯一の正 |
| 11 | [2026-08-22_bandit-hideout-map-s2.md](2026-08-22_bandit-hideout-map-s2.md) | **完了 `f62f145`**(新規 driver 93/93・既存 p6 244/244・p8 56/56) | 100% | codex1 の `盗賊団のアジト.png` をシナリオ2 の**ボスノード n7 を 52x26 へ大部屋化**して貼る(ユーザー決定=案A + `outdoor:true`)。⭐ **n0〜n6 は 1 バイトも触らない** = 分岐/罠/宝箱/残影の獣が全部生き残る。⭐ 実測済: **52x30 マス**・周期 30.5150x31.1400・位相 21.50/16.20・**1 マス 48px** で焼く(0.87MB / `--check` 両軸 OK)。⚠⚠⚠ **絵は MAP_H=28 より 2 行高い**→上下 2 行(樹冠のみ)を捨てて 52x26・`rect [1,10,26,61]`。⚠⚠⚠ **`outdoor:true` × 敵の居る屋外部屋は前例が無い**(`index.html:31977` のコメントが「屋外ノードに敵を置く日には必ず効く」と名指しで警告)→ STEP1 で実測 |
| 12 | [2026-08-22_town-map-phlan.md](2026-08-22_town-map-phlan.md) | **完了 `227785b`**(検証 85/85・負のコントロール 5 本・既存 15 本非退行) | 100% | codex1 の `harbor-town-rebuilding-player-v1.png` を街 `town.html` にして、**キャラ選択の直後に酒場ではなく街に立つ**。タイルクリックで A* 経路歩き + 看板クリックで施設へ。ユーザー決定 = **案B(新規ページ)** + 銀の鹿亭 (09-10,00-02) + 3 施設(酒場/武器屋/闇市)。⭐ 実測済: **23x15 マス**・周期 63.945x64.410・位相 33.40/7.25・**1 マス 64px** で焼く(1472x960 / 0.53MB / 倍率 **1.00x = 水増しゼロ** / `--check` 両軸 OK)。⚠⚠⚠ **案B は起草者の推奨(案A)ではない** — 武器屋と闇市が `tavern.html` の中にあるため「入口が 2 種類になる」問題が残る → **クエリを足さず `sessionStorage["dragonfighters.enterVia"]` で解く**(#6 が名指しで禁じた事項に触れないことを受入条件 7 で機械的に検査)。⚠⚠ **南橋は絵が半マスずれている**(row 9/10 にまたがる)→ 歩けるのは **row 10 だけ**。✅ **#6 は完了済 `9f8b1bd` なので着手可**。⚠⚠⚠ `index.html` の帰還先は **2 箇所ではなく 4 箇所**(`:34717` リザルト / `:34774` 撤退 / `:13213` エンディング / `:13212` autoplay)。⚠⚠ `verify_title_screen.js` の受入条件 **2. と 8. は必ず赤くなる** → **期待値を書き換えず測定点を移す**(依頼書 §10 に張り替え方の表あり) |
| 13 | [2026-08-23_encounter-mopup-before-rest.md](2026-08-23_encounter-mopup-before-rest.md) | **完了 `3cc3eaf`**(新規 driver 36/36) | 100% | ⚠⚠ **`index.html` の変更は別窓の #12 コミット `3cc3eaf` に紛れて入っている**(相手のファイル単位 add が、同居していた本チケットの 5 hunk ごと取り込んだ。未 push で気づいたが別窓が稼働中のため切り直さずそのままにした)。実測: 休憩の回数が **8 回 → 3 回**、全休憩で `detectReinforcements()` = **0**、休憩明けの再戦闘まで 0.2〜3.4 秒 → **43.5 / 55.8 秒**。非退行 6 本緑 (p8 56 / p9 52 / graph_p6 244 / bgm_mine 37 / grid_s2 93 / spawn_not_on_gate 47)。⚠ 難易度は上がった (回復 3 種の回数が減る) → 数値は #8 へ。撤退=`?mopup=0` / `?mopupsight=0`。<br>【起草時の記録】**手前の 1 体を倒しただけで休憩フェーズに入る**。⭐ 着手前に実測済 (`tools/probe_rest_premature.js` を新設): 休憩 13 回中 **1 回は本番の `detectReinforcements()` が 2 体を返しているのに休憩へ落ちた**(隣のマスにホブゴブリンとゴブリンブルート)。真因 = ①勝利判定が `encounterEnemyIndices` しか見ない ②増援を見る点がラウンド先頭の 1 点だけ ③視界はパーティ全員なのに**視線はリーダーだけ**。⚠⚠ **直すと難易度は上がる** (回復 3 種がエンカ終了ごとなので、分割していたぶん得していた) → 数値は #8 へ合流。撤退=`?mopup=0` / `?mopupsight=0` |
| 14 | [2026-08-23_party-size-5-feasibility.md](2026-08-23_party-size-5-feasibility.md) | **完了**(調査チケット・dev-loop 4 項目 `a1558ba` / `83ac18f` / `01eac91` + STEP4) | 100% | ⭕ **結論: 5 人(主人公1+NPC4)は engine 側は行ける。6 人は ⛔。** 6 人が落ちるのは**下部 HP ミニバーの 1 点だけ**(390x844 で `client 390 / scroll 413` = 23px はみ出す。横スクロールに落ちない上限がちょうど 5 人。⚠ かつ 6 人のカメラは未測定)。⭐⭐ **本命だった desktop のカメラは白だった** — 主人公が画面外のフレーム率は N=4 / N=5 とも **0.03%**(24 走行 / 12 ペア / **41,610 フレーム**、12 ペアすべて差 ±0.01pt)で、依頼書の不合格条件「desktop が 0% から有意に上がる」は**成立しなかった**。危ない側は desktop ではなく **compact + 竜の巣**で、しかも **4 人の時点で既に**クランプ区間が空(N=4 22.40% / N=5 23.82%)。症状が 0.00% で済んでいるのは `[compact-hero-anchor]` の 2 点ハードクランプが吸収しているから → ⛔ **この救済節を壊す改修は 5 人化とセットにできない**。⭐ スポーンは **702 人ぶん**で違反 0(⚠⚠ **依頼書が見落としていた 2 本目の配置点 `placeNodeParty()`(ノード遷移のたび・RING 16 個)も掃いた上で**)/ 隊列 24 標本すべて成立 / 停滞 **0 件**。⚠⚠ **本文を 5 箇所訂正**(① 前提表 #1 に `placeNodeParty()` を追記 ② 受入条件 6 の予測値 70.8/58.0/48.9px は誤りで実測 **75.33/59.06/52px**(撤退チップが `flex:0 0 auto` の非伸縮 52.67px)③ compact 25.9%/16.5% は `[compact-hero-anchor]` **導入前**の値で比較不可 ④ 非退行の基準が古い **79→82 / 55→56** ⑤ **`?autoplay` はカメラ測定に使えない**=`focusCameraOn` が丸ごと止まる → `RUN.auto` を使う)。⚠⚠ **#8 の 120 走行は「4 人が上限」という前提の下で最適化された結果** = 上限を上げるなら測り直しになる。⛔ **本チケットは上限を上げていない**(`MAX = 3` は無改修 / `?party=N` は調査シームで本番導線から呼べない / `grep -c psProbe index.html` = 0)。装置 `tools/probe_party_size.js` 本番 **57/57** ・ `--negative` **22/22**。非退行 6 本緑(recruit_size **82** / p8 **56** / p9 52 / graph_p6 244 / spawn_not_on_gate 47 / encounter_mopup 36)。<br>【起草時の記録】`recruitCountOf()` の clamp 上限 3 (= 4 人) を外せるかの**調査チケット**。本番の挙動は変えず、測って可否を出すだけ。⭐⭐⭐ **着手前実測で、コメント (`tavern.html:3266-3270`) が挙げる 4 つの理由のうち 3 つが既に成立していなかった** — スポーン枠は固定 4 枠でなく**代替候補 24 個**(`resolveAllyInitTile` `index.html:31518`)、隊列は zone 係数で人数非依存(`index.html:24662`)、準備画面のキャラタブは構成員でなく**全 6 職固定**(`renderCharTabs` `tavern.html:4784`。旧 `.partyComp` は死んだ CSS)。⭕ **本物のリスクはカメラだけ** = `computeCameraTarget` の隊列 bbox がクランプ区間を空にする枝(`index.html:6333`)。compact は 4 人で既に空だが人数非依存の `[compact-hero-anchor]` が救っており、**危ないのは desktop(今 0%)**。⚠⚠⚠ **コメントが言う「既存のカメラ回帰 (driver_camera)」は実在しない**(`tools/` に 0 本・`__camAnchorOff` を使う道具も 0 件)→ 測定装置は新規に作る。⭐ 測定台は #8 の `sweep_recruit_balance.js`(酒場経由)を流用。⛔ index 直起動の測定台を作ると人数指定が効かず全部 4 人になる |
| 15 | [2026-08-23_heromark-and-town-signplates.md](2026-08-23_heromark-and-town-signplates.md) | **完了 `372321c`**(新規 driver 46/46・`--negative` 4/4) | 100% | ①自キャラの頭上に ▽ 三角マーカー(街 + ダンジョンの両方) ②街の看板を丸アイコンから**羊皮紙の立て札**(施設名 + 説明 1 行)へ。ユーザー決定 = **両画面に ▽** + **札はテキストだけ(アイコン廃止)**。非退行: verify_town_map **85/85** / graph_p6 **244/244** / grid_p8 **56/0** / doors_p8 **15/15**。撤退=`?heromark=0` / `?signplate=0`(街とダンジョンで**独立に**読む)。<br>⚠⚠⚠ **依頼書 §4 A-2 の `placeUnscaledUi(…, -(hm-w), -46)` は dx が誤りだった** — `left = SX(wx + size/2) + (dx − size/2)` なので `dx = −半幅` だと**スプライト半分ぶん左へずれる**。正は `dx = size/2 − 半幅`(敵の装備バッジ `index.html:14606` と同じ規則)。⚠⚠ **`dy = −46` では名前ラベル(−28)と 1px しか離れない**(bob が +4px 下げる)→ **−50**(実測 gap 5.0px)。⭐⭐ **説明文は紙のまだら模様に食われて読めなかった** → `opacity: 0.85` を**外し**(斑な紙では透かした瞬間に死ぬ)、`--sp-ink-soft` を `#4a3413` へ、**クリームの薄掛け `--sp-wash: 0.30` を 1 層**追加。⭐ **`max-width: 210px` では 16 文字の説明(実測 208 layout px)が入らない** → 280px / padding 左右 16px。⭐ **desktop 1440x900 の実効 zoom は 0.8667** = 説明を依頼書どおり 12px にすると実効 10.40px で受入条件 10px に**余裕 0.4px しか無い** → 13px(実効 11.27px)。⚠⚠ **廃坑は camZ ≈ 0.346** = ワールド px と画面 px を取り違えると装置の閾値を誤る(実際に (Z2d) が一度赤くなった。実装の欠陥ではない)。⚠⚠ **負のコントロールのアンカーは「どのファイルの話か曖昧でない行」を選ぶ** — `platehide` の初版 `transform: translate(-50%, -50%);` は **index.html にも在り**、(Z3) の装置 assert が実行前に止めた。⚠ **iOS 実機の目視は未了**。<br>【起草時の記録】⭐⭐⭐ 着手前実測: **▽ を ally ごとに持たせると壊れる** — NPC 頭が死ぬと主人公が頭へ昇格し (`tryPromoteNewHead` `index.html:17776`)、その時 ally 側の DOM が `remove()` される → **▽ は 1 枚だけ作り毎フレーム主人公を引き直す**。⭐ 96px セルの**頭の天辺は箱の上端 + 32px**(6 職の row3 不透明上端を実測 = 32/33/33/34/32/32)。⭐ 紙は既存 `assets/parchment_plaza.jpg`(平均輝度 149.8 = セピア中間調)を**暗幕なし**で流用 → 新規アセット不要。⚠⚠ `verify_town_map.js` が看板を 4 点で縛る(id / 枚数 3 / **中心の elementFromPoint** / 変異アンカー 5 本の文字列)→ 85/85 非退行が受入条件。⚠ `index.html` を触るので **changelog 必須** |
| 16 | [2026-08-23_bandits-forest-single-room.md](2026-08-23_bandits-forest-single-room.md) | **完了**(非退行 12 本緑・新規 assert 18 本) | 100% | シナリオ2 を **8 ノード → 1 ノード**へ畳み、codex1 の卓上バトルマップ 1 枚 (52x26) だけで完結させる。ユーザー発端 = 「プレイしても出てこない」「手前の部屋とか古いの使ってない?」。⭐⭐⭐ 着手前実測: **1 ノード / kind:"boss" のグラフは本番の `lintRun` を error 0 / warning 0 で通る**(新規 `tools/probe_s2_fold.js --lint`)。⛔⛔⛔ **ただし 1 ノードでは kind は "boss" 一択**("search"/"loot" は `graph-no-boss` で error)→ `KIND_SPAWNS_TRAPS={search:1}` / `KIND_SPAWNS_ROOM_CHESTS={loot:1}` (`js/df-mapdef.js:2082-2083`) に引っかかり、**何も足さずに畳むと罠 8 個と宝箱 8 個が無言でゼロ**になる(廃坑 P8 が実際に払った代償)→ 2 関数に**第 3 引数 `extraKinds`**(既定は 1 ビット不変)。⭐ **呼び口は `applyNodeKindExclusions` (`index.html:4502`) ただ 1 箇所**・判定の出所も 2 関数だけ = index 側で kind を再判定しない。⭐ 実測 (`--kinds`) 敵 26 / 罠 8 / 玄室宝箱 8 / 檻 2 → 畳むと敵 10。⭐⭐⭐ **「出てこない」の正体 = 今は n7 に一度も到達していない**(実プレイ 3/3 敗北 R6/R8/R9)→ 畳めば 1 部屋目がその絵になり構造ごと解消。✅ **シナリオ2 にノードイベントは 1 本も無い**(`registerNodeEvent` の 3 本は全部シナリオ1)。⚠⚠⚠ **残影の獣の (36,13) は大部屋では北東の岩場の中**(#11 が名指しした起点タイル)→ 必ず移す・檻と同一タイル。⚠ **id は `n7` のまま変えない**(driver_grid_s2 93 と driver_graph_p6 244 の変異アンカーが 0 件ヒットで exit3 になる)。ユーザー決定 = 湧き水 rest は**消してよい** / ノード遷移 0 回で**いったんよい** / 罠 8・宝箱 8 は**総数そのまま**。撤退=`?s2fold=0` |
| 17 | [2026-08-23_town-tavern-bgm.md](2026-08-23_town-tavern-bgm.md) | **完了 `a030c04`**(新規 driver 17/17・`--negative` 15/15・非退行 37/85/85) | 100% | 街(`town.html`)と酒場(`tavern.html`)を合成トラック `tavern` から専用 mp3 へ。ユーザー決定 = **街 `village08.mp3` / 酒場 `酒場.mp3` の別曲**・出所は **5 曲とも魔王魂**。⚠⚠⚠ **`BGM_FILES.tavern` という ID は使えない**(`playBgm` が `TRACKS` より先に `BGM_FILES` を見るので、呼び口を直さないまま黙って mp3 へ逸れ、`renderBgmOffline("tavern")` だけが合成を指す二重定義になる)→ ID は `town` / `tavern_room`。⭐ ラウドネス実測済 village08 −14.6 → volume 0.54 / 酒場 −12.6 → 0.43(基準 −19.9 は `dungeon_normal` −15.5 × 0.60 で再現確認)。⭐⭐ **画面のクレジット行は `credit:` から生成されていない**(`audio.js:837` と `index.html:13404` は直書き)→ 5 曲とも魔王魂なので**画面側は変更不要**、`audio.js:331-333` の空 `credit` 3 件も同時回収。撤退=`?townbgm=0` |
| 18 | [2026-08-23_scenario2-clear-rate.md](2026-08-23_scenario2-clear-rate.md) | **完了 `faaaa61`**(新規 `tools/probe_s2_clear.js` 924行・装置assert 12本・`--negative` 6/6・**測定 84 走行**・非退行 golden 6/6) | 100% | シナリオ2 のクリア率 0% の**調査**(本番は 1 バイトも変えていない = 道具が起動時に `git diff HEAD` を引いて dirty なら測らない)。⭐⭐⭐ **9 腕 84 走行で敗北 82 件すべてが「主人公 1 人の死」**、`isPartyWiped()` は **0/82** — Lv・人数・射程・掃討・畳みのどれを動かしても例外ゼロ。被弾比 主人公/パーティ 中央 **0.702**、**19/84 走行で 1.000**(仲間の被弾ゼロ)。⭐⭐⭐ **`?s2fold=0`(8ノード版)はもっと悪い** — 10/10 が **n1** で **23.5 秒**・XP **275** で折れる(既定は n7 まで進んで 113.5 秒・XP 1190)⇒ **#16 の畳みは改善だった / 「場に居る敵を減らす」は逆効果**。⭐ **推奨 Lv4 でも 10%(1/10)** が上限(§2-5 の「0/12」は N=3 の観測だった)。⭐ **廃坑を実際に 1 周して装備込みでも 0/3**。⭐ **同時交戦 最大 12**(門+焚き火+ボス+召喚が 1 戦闘へ)、`?dndrange=0` で 8 へ割れて残敵 6→3・XP 1190→1715 と**最遠まで進む**が **0%**。⚠ §2-4 の「ボス撃破後に敗北」は **84 走行で 0 件**(ボス撃破 2 件は 2 件ともクリア)= 候補4 の効き目は未測定。⚠ 停滞は **84 走行 0 件**(`nodiag` 変異で検出器が生きていることも確認済)。⛔ 直していない — 候補 6 つを効き目の実測値付きで §12-5 に並べて終了。⭐ 次の一手の材料 = **候補1(敗北条件)と候補2(被弾配分)は同じ現象の表裏**。<br>【起草時の実測 25 走行】⭐⭐⭐ 着手前実測 **25 走行**で **「全滅」ではなく「主人公 1 人の死」**だと判明 — `isPartyWiped()` が **25/25 で偽**、仲間生存の中央値は **3/3**、Lv1 の 3 走行では**仲間の HP が 1 も減らない**。敗北は `onHeadDowned` の `if (heroIsHead) return false;` (`index.html:17949`) と、主人公が後衛のときの `ally.isHero` 4 箇所(`:24522`/`:27128`/`:29648`/`:31518`)。⭐ 一方で勝利は「**敵を 1 体残らず**全滅 + ボス撃破」(`:17746`)= **スカーを倒した後に残敵 2 で主人公が落ちて敗北**した走行が実在。⭐ **推奨 Lv 4(主人公 HP 60)まで上げても 0/12**(`dragonfighters.xp` を焼いて Lv1/2/3/4 を各 3 走行)。⭐⭐ 廃坑は同じ Lv1 で **2/3 クリア** — 敵の総数はほぼ同じで、違うのは**入場時に場に居る数**(廃坑 **2 体** vs シナリオ2 **10 体**)。⚠ #8 の「**n1** で折れる」は #16 の畳みで**失効**(既定に n1 が無い)。⚠ #16 の「間欠停止が測定を汚染」は本測定台では **25 走行 0 件**(打切 0%・`aborted` 0 件)。⭐ 停滞は本番の診断ウォッチドッグが既に `clear`/`defeat`/`aborted` で持っている → **本番に計測シームは不要 = changelog も鳴らない**。⛔ 直しはしない — 候補 6 つを数値付きで並べて終わる |
| 19 | [2026-08-23_action-priority.md](2026-08-23_action-priority.md) | **完了 2f33dfd** | 100% | 出発の準備画面で「全般/雑魚/ボス/道中」ごとの技の優先度をプルダウン指定。⭐ 主人公は既存の重み抽選(`pickLeaderAction`)に倍率 1 行、仲間は if 連鎖の先頭へ先出し 1 段(ゲート 20 本は上げるだけ)。⚠⚠⚠ 倍率は `LEADER_W_MAX` クランプの**後**に掛ける／道中バフは戦闘開始の `resetPlayerBuffs()` で**主人公だけ**剥がれる／1戦1回スキルは戦闘開始で使用記録が消えるので道中に許すと 2 回撃てる。撤退=`?actionpri=0` |
| 20 | [2026-08-25_title-bgm-opening.md](2026-08-25_title-bgm-opening.md) | **完了 `5531ffb`**(新規 driver 16/16・`--negative` 14/14) | 100% | タイトル画面(記録選択 + 名乗り)へ専用 BGM `opening.mp3` を敷く。⭐ #6 が「別チケット」として明示的に残していた宿題で、見送り理由(遷移先と同じ曲が頭出しへ戻る)は街が別曲なので消えている。⚠⚠⚠ **`unlock()` は「モジュール内部の」`playBgm` を呼ぶ**(`audio.js:119`)ので `GameAudio.playBgm` を包んだスパイでは **pendingBgm からの再生が永久に見えない** → 呼び口は `town.html` と同じ **ロード + 最初の pointerdown の 2 本**、検証は `__bgmFileState()` と 2 経路。`silent` 変異が (0a)(2a) だけを赤くし **(2b) は緑のまま**という形でこの罠を機械証明した。⚠⚠ 既存 golden `verify_title_screen.js` の**受入条件 10 が「BGM 0 回」を assert していた**ので反転(段数は減らさず `=== 1` + ID 一致へ)。⚠⚠⚠ その golden は **83/83 ではなく 85/85**(レシピ集の記録が古い)。⚠⚠ **`driver_bgm_town` の (0b) が `__bgmFiles()` の件数 9 を直書きしていた**ので曲を足した瞬間に赤 → 9→10 へ更新(退行ではない)。⚠ **変異アンカーは部分文字列一致**なので、ロード時の呼び口(インデント 4)は pointerdown 内の行(インデント 6)に含まれてしまい 2 箇所ヒットした → 末尾コメントまで含めて一意にした。⭐ 実測 opening **−10.3 LUFS → `volume 0.33`**(実効 −19.9 = 基準どおり)/ 出所 = 魔王魂(ID3 タグ無し・ユーザー確認)→ 画面のクレジット行は変更不要。⛔ 鳴り出しは **最初のタップから**(ブラウザの自動再生ポリシー)。⛔ 依頼書 §8 (4b) の「既存 9 件の volume も固定」は **#17 の『耳で下げてよい』決定を壊す**ので id/src/credit だけに縮めた。撤退=`?titlebgm=0`(無音へ) |
| 21 | [2026-08-25_world-map-entry.md](2026-08-25_world-map-entry.md) | **完了 `95fcd35`**(新規 driver 55/55・`--negative` 40/40) | 100% | キャラ選択の後とダンジョン帰還の両方へ、1 枚絵のワールドマップ `world.html` を挟み、主人公の駒を街道の上で歩かせる。ユーザー決定 = **駒を歩かせる / 帰還も挟む / 素材はこのまま + 拠点はコードで重ねる / 導線は点線 / 6 シナリオの地にも札(計 7 枚・入れるのは港町だけ) / BGM は `fierd.mp3`(魔王魂)**。⭐⭐⭐ **通行マスクを作らない** — 街道の描き幅は 1 マス 64px に対し ~10px しかなく量子化すると絵と食い違う → **ノード + エッジのルートグラフ**を持ち、そのエッジを画面の点線として描く(線とデータは同一配列 = 罠 C)。ユーザー追加指示「歩ける導線はルート線で分かるように」がこの方式そのもの。⚠⚠⚠ **間に 1 ページ挟むと一回性キーが 3 本壊れる** — `exitVia`(town が消費)/`enterVia`(酒場が消費)/**`lastResult`(酒場 `:4121` が消費 = 消すとリザルト画面が黙って出なくなる)**→ world は 3 本とも**消さない**。⚠⚠ **`?town=0` の判定より前に world へ飛ばすと撤退路が死ぬ** → 順序は `?town=0` → `?world=0` → `world.html`。⭐ 帰還地は既存 `sessionStorage["dragonfighters.currentScenario"]` で分かる(`removeItem` は全文 grep で 0 件)= index 側に新しい書き込み不要。⭐⭐ **水マスクは完全に分離する**(海 100.0% / 湖 72.1% / 陸 0.0%)ので「線が水を渡らない」は機械 assert にできるが、⚠⚠ **道マスクは東半分(岩山・溶岩)で使えない**(岩肌の暖色が同じ色域)→ 道の有無は目視。⭐ 素材は 1536x1024 = 24x16 マスにちょうど乗る(焼き直し不要)/ `codex1\` 直下の同名ファイルと sha256 一致。⭐ 素材には**港町 + 3 集落 + 街道網が既に描かれている**(港町 = タイル (5,8)-(6,9)、桟橋 2 本)が、⚠⚠⚠ **砦も神殿も坑口も描かれていない**ので 6 枚の札は**描かれた集落を避けて地形で置く**(全候補 water 0.0% / 最小間隔 2.24 タイル)。⭐ 札の文言の唯一の正は **`tavern.html` の `place:`**(`:2221` 他)で、`world.html` からは 6,800 行の別ページを読めないため**意図的に重複**する → (7a) が **tavern.html の実体と機械照合**してドリフトを止める。⭐ 点線は**明色 + 暗い縁取り 1 色**でよい(全区間の直下 luma mean 72.2〜116.7 / max 146.6 を実測)。⚠ `fierd.mp3` −13.5 LUFS → **volume 0.48**・ID は **`world`**(`field`/`town` にすると #17 の罠)・⛔ volume は assert しない。⚠ **`driver_bgm_town` の (0b) が件数直書き 10 なので曲を足すと必ず赤**(→11、退行ではない)。⚠ `verify_title_screen.js` は遷移が 1 段増えるので**必ず赤くなる** — 期待値を緩めず #12 と同じ「測定点を通り抜けた後へ移す」で回収。撤退=`?world=0`(`?town=0` とは独立) |
| 22 | [2026-08-25_town-world-exit.md](2026-08-25_town-world-exit.md) | **完了 `3dda3f5`**(新規 driver 23/23・`--negative` 4/4・PENDING 0) | 100% | 街から地方全景へ「出る」導線。港町フランの北の街道口 **(6,0)** に 4 枚目の立て札「町の外へ」(`sign` は (5,2))。⭐⭐⭐ **`exitVia="town"` を 1 行書けば `js/world-map.js:170` の fail-safe が `phlan` を返す**ので **`js/world-map.js` も `world.html` も 1 文字も触らずに成立した**(`verify_world_map.js` が**無変更**で 55/55・`--negative` 40/40)。⭐ `noexitvia` 変異が「書かないと駒が `pier` に立つ」= ⚓ アンカーの『新キー不要』という誤りを機械的に再現。⚠⚠⚠ **起草の主張が 3 件崩れた** — ① §2-5 の「札は `max-width:280px` に張り付く / (6,1) だと 4px 交差」は**偽**(実測は全部 client 210px = stage 240px。交差するのは中心間 **3.75 タイル未満**だけで (6,1) = 4 タイル差では 12px の隙間が残る)→ 負のコントロール `signcrowd` を **(7,1) = 3 タイル差**へ変更して (1b) だけが赤くなることを機械証明(本番の (5,2) は据え置き)。② **札の幅は一律ではない**(`shop` だけ 165px = 文字数で決まる)。③ §2-3 の「件数直書きは 16 箇所」は過小で **実際は 17 箇所** — 数え落としは **(8c) の `=== 2`**(`gate` は `requiresPlazaUnlock` を持たないので**闇市の解禁前から出る**)→ ⭐⭐⭐ **件数直書きの grep は「増える側」だけでなく「減る側 / 別の母集団の数」も掛ける**。⭐⭐ **出口タイルの到達は `pagehide` で「消える直前」を同期で焼き付けて遷移先から読む**(着いた rAF の中で `location.href` が走るのでポーリングでは原理的に間に合わない)。⭐⭐ **「不変」は番兵を置いてから測る**(`lastResult` の null 同士の一致は証拠にならない)。⚠⚠ ドライバの purge は `__drvSeeded` で **1 タブ 1 回**なので (5b) は**同じタブで `page.goto` を 2 回**踏む(新しいタブだと空振り)。⭐⭐ 撤退の受入は「OFF で緑」でなく **同じ conjunction を ON/OFF の両方へ当てて崩れる**((5c))。既存 golden **8 本すべて非退行**(85/85・46/46・55/55・86/86・30/30・17/17・16/16・37/37 = **期待値の書き換え 0 件**)。撤退 = `?world=0`(#21 と共有) |
| 23 | [2026-08-25_quest-walk-to-site.md](2026-08-25_quest-walk-to-site.md) | **完了 `e7f3ea1`**(新規 driver 25/25・`--negative` 46/46・PENDING 0・dev-loop 4 項目 `85b6de3`/`f0f185c`/`d588584`/`e7f3ea1`) | 100% | 受注した依頼の地まで**地方全景を歩いて向かう** + **未解放の拠点を地図から隠す**。⚠⚠⚠ **未解放ノードを「歩けなく」すると開始直後に廃坑へ到達できず詰む**(起草時に実グラフで実測 — `cleared=[]` で未解放 5 拠点を消すと phlan から届くのは `phlan,pier,cross_n` だけ。街道網は**環状**なので **1 つずつ消す単体テストでは永久に緑**)→ **隠すのは見た目だけ**にし `EDGES`/`findPath`/当たり判定は 1 バイトも触らない。⚠⚠ **地図を 1 段挟むと `autoplay`/`evade` が確実に落ちる**(遷移先にクエリを足さない確定作法のため。`probe_s2_clear` / `sweep_recruit_balance` の 2 本が死ぬ)→ dev URL では素通し。⚠ **`exitVia` を書かずに飛ぶと駒は港町でなく桟橋に立つ**(`world.html:476` が空を `"title"` と読む)。⭐ 既存 `verify_world_map.js`(**2026-08-25 実測 55/55**)は (7b-dom)/(7d)/(7e) の母集団が壊れるが、**装置へ `cleared`=6 本を仕込めば 55/55 のまま**(⛔ assert の文面も本数も減らさない)。⭐ ユーザー決定 3 件 = 出発は地方全景へ直行 / **生成クエストは据え置き**(`id` は 2 経路とも `"generated-quest"`)/ 入場は確認をはさむ。撤退 = `?questwalk=0`。**【実装で判明】** ⚠⚠⚠ **`tavern.html` は `js/world-map.js` を読み込んでいなかった**(§5-2 は見える前提で書かれていた。1 行足して解決 — 無いと `viaWorld` が永久 false =「何も起きないのに (1c)(4a)(4c)(4d)(4e) が全部緑」)。⚠⚠ **§2-3 の「影響なし」表は誤りで `verify_recruit_size.js` が実際に壊れた**(遷移の横取りが `/index.html` だけ → 酒場のタブが本当に `world.html` へ行き `Execution context was destroyed`。正規表現を `/(index\|world)\.html/` へ広げて 82/82 復帰)。⭐ **「着地先を見ない」は「遷移しても平気」ではない**。⚠ 母集団が壊れたのは 3 本でなく **2 本**((7e) は札でなく**ノード**を押すので減らない)。⚠ `verify_title_screen` の golden は 83/83 でなく **86/86**。⚠⚠ 変異 `eatquery` は条件①だけ潰しても (4d) が緑のまま(`target += params` で条件②が代わりに止めていた)= **dev URL を素通しさせているのは①②の 2 つ**。⭐ 確認ダイアログは `onclick` でなく `bindAsk()` で 1 回だけ登録(`click` + `touchend` 併用)。**残 = §9 の実機体感 4 件**(歩く距離の体感 / 未解放の点の見た目 / compact の文字サイズ / iOS のタップ) |
| 24 | [2026-08-26_codex-map-request-skill.md](2026-08-26_codex-map-request-skill.md) | **完了 `ddf1857`** | 100% | Codex へ MAP を発注して DF のタイル格子へ乗せる作法を `.claude/skills/codex-map-request/` へ。新 driver `tools/verify_codex_map_skill.js` **17/17 PENDING 0 / --negative 21/21 空振り 0**。⚠⚠⚠ **スキル 4 枚は git に入らない**(`.gitignore:2` が `.claude/` を丸ごと除外。既存の impl-request / dev-meeting も同じ)→ commit に入るのは `tools/` の 2 ファイルだけ。⭐⭐⭐ **消えた測定器は「復元」できた** — `line_response`/`comb_score` が生存しており、`phlan-harbor` の 6 数値を**完全一致**で再現。⚠⚠⚠ **罠 D(新規): 中心無しの広域探索は 3 方式とも倍音を拾う**(素朴 argmax / 反櫛の差 / 離散フーリエ。廃坑入口で 91.40 = 2x45.70 が本物に勝ち、港町は基本波より 32px 成分が強い)→ `--fit` は**探索中心を必須**にした。⚠⚠ **§4 のコード片と (1b) が矛盾していた**(tile を中心にすると周期 45.70 は原理的に測れない)。⭐⭐ **(3a) の基準は「控えた SHA」でなく `assets/` の現物**(焼き直しがバイト一致すると実測)。詳細は §12 |
| 25 | [2026-08-26_stag-tavern-dnd-map.md](2026-08-26_stag-tavern-dnd-map.md) | **完了 `037726a`**(新規 driver `verify_tavern_map` 42/42・`--negative` 66/66・PENDING 0・dev-loop 4 項目 `8036ce5`/`638b479`/`4aaea2b`/`d80e7f7`/`037726a`) | 100% | 銀の鹿亭を 1 枚絵から**歩ける D&D マップ**へ(港町フラン方式)。卓 3 つ = シナリオ1〜3。⚠⚠⚠ **`#tableArea .table` を押す golden がちょうど 3 本**(`verify_recruit_size:333` / `verify_quest_walk:636` / `driver_depart_menu_clean:147`)あり、歩きを 1 段挟むと #23 と同じ壊れ方をする → セレクタを `#questTable_goblin-mine` へ。⭐⭐⭐ **`goToTable` には town.html の `goToFacility` に無い「同じ卓へ歩行中の再クリックを無視する」ガードが要る**(ドライバは 420ms ごとに押し続け、`walkPath` は先頭で `stopWalk()` して `t0` を打ち直す = `MS_PER_TILE 340` に対して 420ms でぎりぎり進むのは**偶然であって設計ではない**)→ 変異 `reclick` が機械証明。⚠⚠ `@media (max-width:560px)` の 2 列スクロールグリッド(`position:relative !important`)はマップと原理的に両立しない → **削除せず** `body:not(.tavernMapOn)` で括る。⚠⚠ **シナリオ4〜6 は「奥の間へ」の暫定扉へ逃がす**(消すと進行が詰む。新しい建物は作らず既存 `renderTables()` を流用し、#26 で扉ごと撤去)。⚠ `js/world-map.js:24` の「`tavern.html:2221`」は実体 **2241** で 20 行ズレ。⚠ `tavern.html` は **CRLF**(`git diff` では気づけない)。ベースライン実測(2026-08-26) = recruit **82/82** / quest_walk **25/25**+neg **46/46** / depart_menu **41/41**。撤退 = `?tavernmap=0`(ページ単位で完結・遷移をまたがない) ✅ **完了 2026-08-28**。マス = **96px**(依頼書の 64px から逸脱 — 素材の格子が 99.8x97.5px なので 64 だと 1.31 倍の拡大でぼける)。⭐⭐⭐ **壊れた golden 3 本は待ち時間を 1ms も伸ばさずセレクタ 1 行(`'#questTable_goblin-mine, #tableArea .table'` = カンマ区切りは文書順で 1 件返す)で復帰**。⚠⚠ **依頼書 §9 の `driver_bgm_town` 37/37 は誤記** — 新設時から一貫して **素 17/17 + neg 15/15**(他 5 枚の依頼書が 17/17 と記録。`git diff 7394692 HEAD -- tools/driver_bgm_town.js audio.js` が空 = 退行ではない)。⚠ 逸脱全 7 件と実測は **§13**。残 = §10 の実機体感 6 項目(特に **`#fireplace` の x50%/y58% は旧 1 枚絵基準なので必ずずれる**)。 |
| 28 | [2026-08-28_ability-scores-5e.md](2026-08-28_ability-scores-5e.md) | **完了 `aea44a8`** | 100% | 能力値を 5e の 6 能力(CHA 込み)へ一本化する土台。**UI は作らない**。⭐ 修正値を B/X → 5e 式へ。**実測は 12 判定中 7 判定が +1・下がる判定は 0**(⚠ 依頼書の予告 6 は誤記だった)。新 driver `verify_ability_scores` **素 24/24 / --negative 39/39 / PENDING 0**。⚠⚠ **`title.html` だけ改行が LF**(他 4 枚は CRLF)。⚠⚠⚠ **委譲すると単体注入の golden 3 本の母集団が静かに 0 になる** → 装置側で復元(期待値は不変)。⭐ 変異 `shadow` は「振る舞いが 1 つも変わらない欠陥」で (3b) だけが赤くなる。撤退 `?ability5e=0` |
| 29 | [2026-08-28_player-sheet-v1.md](2026-08-28_player-sheet-v1.md) | **完了 `3e42886`**(新規 driver `verify_player_sheet` 素 42/42・`--negative` 57/57・PENDING 0・空振り 0・dev-loop 4 項目 `af08a3a`/`702ef0e`/`20150af`/`3e42886`) | 100% | いつでも開けるキャラクターシート(閲覧専用)+ 言語「識る言の葉」。⛔ **#28 の後**(完了済)。⚠⚠⚠ **依頼書 §2-1 の「呼び出し口は 2 経路」は誤りで実際は 3 経路** — `town.html` は `position:fixed` が 0 件ではなく **3 件**で **`#townHud` が下端全幅を占有**(`left:0;right:0;bottom:0;z-index:11`)、`world.html` も **5 件**。→ `pickHost()` = **partyPanel > 表示中の townHud > body へ fixed**。⚠⚠ **`#townHud` はデスクトップで `display:none`** なので「在るか」でなく「**表示中か**」で分岐(在るかだけだとデスクトップの町で永久に押せない)。⚠⚠ **ボタンは `<button>` でなく `<div role="button">`** — `verify_town_map` の (11b) が `#townHud button` を数えているので 5 本目の `<button>` を足すと即赤。⚠⚠ **`index.html` の `hp`/`playerStats` は隊列の先頭**なので主人公が中衛/後衛だと **NPC の HP/AC を「自分の体」として出す**(`!heroIsHead` で `heroRef` へ落として修正)。⚠⚠⚠ **`verify_title_screen` の (2c) は期待値そのものが新仕様と衝突**(「職を選べば出発が有効」)→ 装置追加では直らず**弱めずに強めて**(職だけでは disabled → 言の葉で有効、の 2 相)86/86 復帰。⚠⚠ **(4d) は `newGame()` の prefix 総なめで単純差分では測れない** → `?sheet=0` 出発との**集合差**へ。⭐ **技能は「行ごと伏せる」/ 職業名は自前表 + `HERO_CLASSES` 優先の fallback**(`js/hero-classes.js` は title/town/world の 3 枚だけ・冒頭コメントは実物とズレ)。⭐⭐ **変異の `evaluable`/`allowRed` は `--mutate` 単体実測から決める**(7 本中 5 本が担当外を巻き込む)/ ⛔ **測っていない節を `evaluable` に載せない**(母集団 0 の述語は一律 false = 偽の赤)。⭐ `fixedsave` は **(3d) が緑のまま**(`languagesOf` が重複を潰す)= 表示のテストでは保存の欠陥を捕まえられない。⭐ `fixedbtn` は **(1a)/(1c) が緑のまま**(z-index 62 で押せてしまう)= 「どこにマウントされたか」を見る (1b) だけが網。⚠ **装備欄は出さない** — 装備の唯一の正が無く town/world/title では原理的に表示できない(#30)。⚠ **`title.html` だけ改行が LF**(他 4 枚は CRLF)/ ⛔ **改行の実測に grep の CR カウントを使わない**(Bash 経由で空文字に化け全行ヒット = 嘘の測定。`py` のバイト数えで測る)。golden **8 本すべて非退行**(86/86・85/85・42/42・57/57・24/24・13/13・82/82・25/25)。撤退 = `?sheet=0`。**残 = §10 の実機体感 4 項目 + §7 の絵(`assets/sheet_frame.png` は未発注・404 でも読める)** |
| 34 | [2026-08-28_warrior-ally-action-priority.md](2026-08-28_warrior-ally-action-priority.md) | **完了 `692fb3e`**(driver_action_priority 75 → **92/92 PENDING 0** / `--negative` N1〜N11 全部赤・空振り 0 / 既存 golden 11 本 非退行) | 100% | 戦士の仲間にも「行動の優先度」を効かせる。⭐ 酒場側は既に 6 職すべてに欄がある(触っていない)。⚠⚠⚠ `ally.buffs.skipNextTurn` は**誰も読まない死にフィールド** → `ally.stunned` へ書いた。⚠⚠ `executeWarriorSkill` は**プレイヤーのグローバル直触り**なので仲間には絶対に通さない。⚠⚠ (2e) のゲート数 20 → **24**。⚠⚠⚠ **region 内のコメントに「乱数 < apGateP(」と書くと 1 本として数えられる**(実際に 25 本になった)。⚠⚠⚠ **変異を全部同時に注入すると互いを覆い隠す**(N9 と N10 が両方空振りした → 2 経路の最大値で数える)。⚠ `driver_grid_p8` の単発の赤は**まず 1 回再実行する**(実プレイ系はフレークする)。撤退=`?warally=0` |
| 35 | [2026-08-28_party-match-loadout.md](2026-08-28_party-match-loadout.md) | **完了 `194d109`+`97f350d`+`85b5b15`**(新規 driver `verify_party_match_setup` **36/36 PENDING 0** / `--negative` **M1〜M7 の 7 本 空振り 0** / 既存 golden **7 本** 非退行・期待値の変更 0 件・dev-loop 4 項目) | 100% | マッチング画面のカードを押すと引き出しが開き、全員分のスキルと傾向をその場で設定。⛔ **#34 の後**(完了済)。⚠⚠⚠ **背景タップ = 出発を廃止するので golden が全部止まる** → `#pmDepart` へ付け替える。⚠⚠⚠ **止まるのは「4 本」ではなく 5 本**(依頼書 §2-2 の表に `driver_party_view_reopen` **35/35** が無い。⭐ **`grep -rn "partyMatchOverlay" tools/` を自分で打たないと 1 本落ちる**)。⚠ `driver_action_priority` の基準 **75/75 も古い**(#34 で **92/92**)。⭐⭐⭐ **付け替える前に一度走らせて「本当に赤いか」を確かめたのが効いた** — `tavern.html` だけ直した時点で 5 本とも赤(**80/82・21/25・34/41・63+FATAL・2/6**)= 背景タップが本当に死んだ証明になり、付け替え後に全部復帰。⚠⚠⚠ **依頼書が知らない `review` モードが `9b6f3b8` で増えていた**(準備画面の 🎴 から開き直す = 開示フェーズを 1 コマも通らない)→ `#pmDepart` は **id 共通・ラベルだけ出し分け**(「出発する」/「準備へ戻る」)。⚠⚠ **iOS は `touchend` も止めないと `<select>` を閉じた指で出発する**。⚠⚠ **カードのタップには `touchend`→`click` のゴーストクリック除けが要る**(無いと実機の指 1 回で開いて即閉じる)。⭐⭐ **カードの伝播止めは無条件にしない** — `phase === "done"` の時だけ(無条件だと開示中に `skipRest` が走らず画面の大半が死に領域)。⚠⚠ `renderSkillItem` の末尾は `renderCharLoadout()` 直結 = 引き出しが更新されない → **再描画点を 3 箇所へ一本化**(呼び口 18 箇所のうち 3 箇所だけ差し替え)。⭐⭐⭐ **`(4c)` の verbatim 拘束は期待値を弱めず「測定点を移す」で解いた**(1 条件 → 4 条件へ強化・35 本のまま)。⚠⚠⚠ **変異 M6 は `max-height: 42vh` を消しても赤くならない**(390px では `@media (max-width:720px)` の 30vh が勝つ → 両方潰す)。⚠⚠ **M7 のアンカー `const equippedIds = apEquippedIdsFor(...)` は 2 箇所で同形**(引き出し側だけを狙うなら `nameOf` の 1 行版)。⚠ 同職 2 人は同じ設定を共有(設計どおり・注記を出す)。撤退=`?pmsetup=0`。**残 = §9 の実機体感 8 項目**(筆頭 = iPhone で `<select>` を閉じた指で出発しないか) |
| 36 | [2026-08-29_player-sheet-5e-layout.md](2026-08-29_player-sheet-5e-layout.md) | **完了 `a51dad3`+`c1e2ed4`+`46cd7c5`**(verify_player_sheet 42 → **70/70 PENDING 0** / `--negative` **101/101 空振り 0**(変異 7→15 本) / 既存 golden 6 本 非退行 / codex1 の紙地・枠 4 点 取り込み済) | 100% | キャラクターシートを本物の 5E シートの体裁へ(区画 5 → 11 の 3 段組・能力値ボックス・セーヴ・攻撃・人物欄)。⚠⚠⚠ **セーヴの唯一の正は `playerStats`(戦闘系)で `js/abilities.js` ではない** — 5e 修正値から出すと画面が嘘になる(統合は #32)。⚠⚠⚠ **区画 5 → 11 で `verify_player_sheet` (0s9) が件数直書きのため必ず赤**(退行ではない)。⚠⚠ **「空の枠を出す」は #29 の `blankrow` 規律と衝突する** → `avail`/`blank`/`inDom` の 3 値へ拡張して**緩めずに**解く。⚠ `hero-classes.js` は index/tavern に載っていない・`Noto Serif JP` は **index にしか載っていない**(同じシートが 5 ページで違う書体で出ている)。⛔ `tavern.html` は `<script src>` を **1 行足すだけ**(`#prep` は触らない)。golden 基準 2026-08-29: player_sheet 42/42 / ability 24/24 / title 86/86 / town 85/85 / world 57/57 / tavern 43/43 / action_priority 92/92。撤退 = `?sheet5e=0` |
| 37 | [2026-08-29_run-chronicle.md](2026-08-29_run-chronicle.md) | **完了 `b069a4a`+`f8bb159`+`b2d638a`+`6aabbf3`+`d5484ff`**(新規 driver `verify_run_chronicle` **73/73 PENDING 0** / `--negative` **N1〜N8 の 8 本 空振り 0** / 既存 golden **14 本** 非退行・期待値の変更 0 件 / dev-loop 4 項目・停止 0 回。✅ **push 済み**) | 100% | 帰還後レポート「冒険の年代記」。1 回の冒険で誰が何に倒れ、誰がどれだけ働き、何を使わずに終えたかを酒場の羊皮紙で読み返せる(直近 5 件)。⚠⚠⚠ **HP を引く点は 14 でなく 43 箇所 / 37 関数**(第二の書き方 `hp = Math.max(0, X.hp - dmg)` が 29 箇所)→ 個別フックは採らず **手番ディスパッチ `for (const actor of units)` の 1 点ラップ + 手番外 6 点 = 7 点**で覆った。⛔ **アクセサ方式は不採用**(`.hp` の読みが高頻度)。実測コスト **0.78µs / 手番**。⚠⚠⚠ **`combatLogLines` は `COMBAT_LOG_MAX = 18` のリングバッファ**で年代記の源にできない(自前配列 `EVENT_MAX = 40` + **倒れた行を守る splice**)。⭐⭐⭐ **`keysOf()` は `dragonfighters.` の前置詞総なめ**なので新キーはスロット振り分けも新規ゲームでの消去もタダ(前置詞を変えると壊れる)。⭐ 死の集約点は **6 点**(`defeatEnemy` 1 / `onHeadDowned` 1 / `ally.alive=false` 4)。⚠ 檻の解放・一括消去の `alive=false` 2 点は**撃破ではない**ので刺さない。⚠ **`defeatEnemy` は冒頭でなく `enemy.alive=false` の直後**(冒頭だと多頭ハイドラの早期 return を数える)。⭐ **技の発動の記録口は 3 点だけ**(`showRollAtAlly` が仲間の 33 箇所を 1 点で覆う)——**呪文スロット消費型は既存台帳 `max − 残` から導出**して 1 箇所も刺していない。⚠ z-index は **170**。⚠⚠⚠ **`snapshot(null)` の覗き見で idle/lastBlow を封印すると本番が壊れる**(封は決着時のみ)。⛔ **`world.html` を 1 バイトも触らない**。⚠⚠ **起草時の主張が 9 件崩れた**(§13-4)——(4c) の「`enterVia` / `lastResult` 0 回」は実測 **8 行 / 14 回**(golden の真の述語は「**getItem すらしていない**」)、(5c) の `body.ui-compact` は酒場に付かず実体は **`body.compact`**、§7 の `#townHud` 制約は `town.html` 専用で酒場に**無関係**、N3 は **(1a) のみ**が赤・N6 は **(3a)(3b)** が赤。⭐⭐⭐ **負のコントロールの基準に `git show HEAD:` を使わない**(コミットした瞬間 HEAD=作業ツリーで全滅)→ **凍結した配信バイトへ実行時注入**。⭐⭐⭐ **N3(罠 B)は「倒した直後に数える」形では空振りする**→ 記録口 `RunChronicle.kill` で **80 回積んで溢れさせてから**数える((1z5) 新設で 72→73)。撤退 = `?chronicle=0`。残 = 実機体感 5 項目 |


⭐ **#8 は 2026-08-23 に決着(`11e4678`)。** 廃坑だけ `recruit: 3` へ個別上書き(クリア率 15% → 40% / XP 中央 580 → 1,890)。⚠ ここから **2 つが宿題として残った**:
  1. **シナリオ2 の「n1 で折れる」** — クリア率が両腕とも **0%** で床に張り付き、募集人数では動かない(ユーザー決定で #8 から分離)。→ ✅ **#18 として起草・承認済**(2026-08-23)。⚠ ただし「**n1** で折れる」という診立てそのものが #16 の畳みで失効している(#18 §2-2)
  2. **沼 `lizard-swamp` が未測定** — ★2 で #7 により NPC が 3→2 に減っているのに、#8 §3-5 の規模決定(対象を 2 本に絞る)で母集団から漏れた。1 と同じ ★2 なので一緒に扱うのが自然

⭐ **#16 は 2026-08-23 に着地。** シナリオ2 は既定で **1 ノード (52x26 の卓上バトルマップ 1 枚)**。
罠 8 / 玄室宝箱 9 / 檻 2 / 残影の獣 1 が大部屋の中へ戻った。撤退 = `?s2fold=0`。
⚠ ここから **宿題が 2 件**残った:
  1. **n7 大部屋での間欠停止** — 指紋 `sig = 39/16/9/2/n7/330/0`(タイル (39,16) / 生存敵 9 /
     `hero.pathLen: 0`)。**6 走行中 3 回**。⛔ **静的な配置の欠陥ではない**(敵 12 タイルすべて
     床かつ本番 `aStar` で到達可を実測) = 実行時に隘路へ体が詰まる系。**#11 が出荷した大部屋に
     元からあった**もので #16 が作った欠陥ではないが、畳んだことで**毎回 n7 に入る**ように
     なったため当たる機会が増えた。**別チケット未起草**
  2. **シナリオ2 のクリア率は 0/3 のまま** — 敵は 26 → 11 体に減ったが、まだ全滅する。
     → ✅ **#18 として起草・承認済**(2026-08-23)。⭐ 実測 25 走行で、真因の候補は「敵の数」ではなく
     **主人公 1 人の死が敗北条件であること**へ移った(仲間は生きている)。
     数値調整は #8 の宿題(#8 の 120 走行は畳む前のシナリオ2 を前提にしているので測り直し)
| 38 | [2026-08-29_mercenary-roster.md](2026-08-29_mercenary-roster.md) | **完了 `c6862a9`+`f112f53`+`edd42d0`+`b7c70ef`+`1f518d2`+`097344a`**(新規 driver `verify_mercenary_roster` **44/44 PENDING 0** / `--negative` **10 本 空振り 0**(巻き添え 1 件 = `noretreatswitch`→(5a)) / 既存 golden **10 本** 非退行・期待値の変更 0 件 / dev-loop 4 項目・停止 0 回。✅ **push 済み**) | 100% | 傭兵名簿。使い捨て NPC を永続化し Lv 成長。⭐ 装備は職業別のまま据置で規模を「大→中」へ / ⚠⚠ `verify_recruit_size` の (D) は顔ぶれの多様性を確率で測っている(名簿が空なら従来と同一挙動)。⚠⚠⚠ **#37 が置き場所を使い切っていた** —— 依頼書の「HUD へ 📜 傭兵名簿を 1 つ足す」は成立せず、左上の縦列 3 段目(📖)へ移し **記録棚が非表示なら CSS の兄弟セレクタで詰める**。⚠⚠ **依頼書の変異アンカーは 3 本がそのままでは空振りした**(`nocap` は上限を守る 3 箇所 + 抽選側の full 判定を全部外して初めて赤 / `reuseid` は `next` の単調増加の押し上げごと外して初めて赤 / `alwaysroster` を `return null` で書くと編成ごと例外になり §1 §3d §4 まで巻き添えで全滅)。⭐⭐⭐ **キー集合だけの (5a) は `alwaysroster` を検出できなかった** → 値の型と「空文字でないこと」まで突き合わせて強化(指紋 = `trait`/`line` が `string:empty`)。撤退 = `?roster=0` |

| 39 | [2026-08-31_darkvision-visible.md](2026-08-31_darkvision-visible.md) | **完了・push 済 `ed704e8`+`df48c0f`+`14ad37d`+`a97f60c`+`9a5315e`**(新規 driver `verify_darkvision` **25/25 PENDING 0** / `--negative` **38/38・変異 12 本とも空振り 0** / 既存 golden **10 本すべて基準どおり・期待値の変更 0 件**・dev-loop 4 項目・停止 0 回) | 100% | 実装済みの職業別視界(8/10/12 タイル)を、名乗り・傭兵名簿・マッチング・キャラシートの 4 箇所へ出す。⭐⭐⭐ **会議の候補④「未踏の部屋を塗り残す」は着手前実測で実装済みと判明して差し替えた** —— `fogOfWar` の grep がゼロ件でも、実体は `exploredTiles`/`renderLighting` という名前の **3 状態フォグ**として出荷済みだった(会議の前提が 5 件崩れた)。⚠⚠⚠ **`CLASS_SIGHT` は既存ドライバ 3 本が裸の識別子で読む**ので識別子と shape を `index.html` に残す。⚠⚠ **legacy 表も一緒に移す**(`driver_speech_boss` の `warriorSight===4` / `driver_wall_face` の `pinOuter===330` は旧値を期待)。⚠⚠⚠ **`js/hero-classes.js` が「数値を写すな」を明文で禁止**しているので写しでなく**移設**する(負のコントロール `shadowsight` で機械検査)。⚠ **#38 が「#39」を候補③に予約していた**ので 5 箇所を番号でなく中身で書き直す。撤退 `?darkvision=0`(表示のみ・挙動には効かない)<br>【実装窓の実測】⚠⚠⚠ **`CLASS_SIGHT` を読むドライバは「3 本」ではなく 4 本だった** — `driver_grid_p5` が**表の行そのものをテキストで握る変異アンカー**を持ち、移設でヒット 0 件 → 赤ではなく **`exit 3` でドライバごと死ぬ**(= 非退行の測定が黙って消える)。⭐⭐⭐ **行を動かすときは識別子だけでなく「その行のテキスト」で `grep tools/*.js` する**。⚠⚠⚠ **§4-3 の実装雛形に潜在クラッシュ**(`d.traits` は `HERO_CLASSES` の無いページで null → `d.traits &&` のガードが要る)= **雛形コードも「主張」であって実測ではない**。⚠⚠ **(0g) の素朴なパターンは `index.html` の武器射程表 `RANGE` に 5 件当たって永久に赤い** → 効くのは **行の三つ組の形**(`tiles`→`inner`→`outer`)。⚠⚠⚠ **§8 (3c) の「compact = ≤900px」は誤り**(実体は `tavern.html` の `@media (max-width: 720px)`)。⚠⚠⚠ **演出を開いてから `setViewport` で縮めると演出ごと畳まれる** → compact は **390x844 で最初から開く**。⭐⭐⭐ **(3a) は `openPrep` 経由では原理的に測れない**(`buildParty()` が `Math.random()` で職を選ぶ)→ 本番シーム `window.__pmTest.play(sc)` に `selection.partyMembers` を直書きして顔ぶれを固定(乱数を潰すのであって測定点は弱めない)。⭐ **(1b)(1c) は全員 `tiles=8` だと「8 を直書きした実装」でも緑になる** → `dwarf`/`elf` を混ぜ **最小値を経路Bから導出**する母集団ガードを併置。⭐ **撤退は「空文字にする」でなく「要素ごと作らない」**(§8 が「0 個」を要求するので §5-2 の雛形から意図的に外した)。⭐⭐⭐ **`legacydrop` は本ドライバでは赤くならないので手で確認** → `driver_speech_boss` 19/19→**18/19**(`warriorSight=8`)/ `driver_wall_face` 54/54→**52/54**(`outer=660` と `(3d)` フォグ α が較正外)= **legacy 表を一緒に移した §2-4 の判断が正しかった証明**。⭐ 撤退は**表示だけ**で `getSight()` の tiles/inner/outer は 6 職とも 1 つも動かない(変異 `retreatkills` が機械証明)。⭐ **`CLAUDE.md` の腐った 3 行**(フォグ / DM ナレーション UI / フェーズ表示)は実体つきで訂正済(タイピング速度まで実装済 = `NARRATION_CHAR_MS = 70`)。残 = §9 の実機体感 6 項目。 |
| 40 | [2026-09-01_world-walk-steps.md](2026-09-01_world-walk-steps.md) | **完了 `b42f904`+`b731644`+`64d3ceb`+`56877b4`**(新規 driver `verify_world_steps` **30/30 PASSED PENDING 0** / `--negative` **46/46・変異 12 本とも空振り 0** / 既存 golden **4 本すべて非退行** = `verify_world_map` 57/57・`verify_quest_walk` 25/25・`verify_town_exit` 素 23/23・`verify_title_screen` 86/86) | 100% | 地図を「1 タップ = 最大 5 マス」で刻んで進む。⭐ 依頼の「点線の間隔が長い」は着手前実測で崩れた — エッジは平均 3.18 マス・最長 6.40 マスで、長いのは **1 タップの移動**(最長 22.55 マス / 8.02 秒)のほう。⚠⚠⚠ **刻み点を `WORLD_MAP.NODES` へ足すと既存 golden が 4 本赤くなる**(sha1 `876c5f6336f96811` / (7f) の「14 件」/ (2a) の「線 14 本」/ (3b) の母集団)→ **派生レイヤ**で持つ。⚠⚠ 点線 SVG は `pointer-events: none` なので印は HTML の `<div>`、クラスは `.worldStep`(`.worldNode` を着せると (3c) が誤爆)。⭐ 実測: 刻み点は **1 件** `lake_n__lakeside@1` (896,416) / 細分化後 15 区間 / 1 タップの最長 286.2px / `phlan`→`temple` は **8 タップ**。⚠⚠⚠ **崩れた主張 3 件**: ① **押し口は 2 箇所でなく 6 箇所**(`mouse.click` だけでなく **`waitForNavigation` と対で書かれた 3 箇所**を数え落としていた。押す先が `phlan` = 入場ノードなので「着くまで押す」ループでは一致する瞬間が原理的に来ない。直す前は 53/57)→ `walkNextTo()` で解決 ② **§6-2 の実装雛形が通りすがり入場で fatal**(`if (WM.has(atId))` だけだと中継の `phlan` で town.html へ飛ぶ → `if (lastArrival.arrived && WM.has(atId))`)③ **CRLF はファイルごとに違う**(`world.html` は CRLF / `js`・`tools` は LF → 変異アンカーは必ず 1 行に閉じる)。撤退 `?walkstep=0` |
| 41 | [2026-09-01_town-tavern-npc-crowd.md](2026-09-01_town-tavern-npc-crowd.md) | **完了 `daf7468`+`c3d2af0`+`51f8fd4`+`d35b7ce`**(新規 driver `verify_npc_crowd` **32/32 PASSED PENDING 0** / `--negative` **58/58・変異 13 本とも空振り 0** / 既存 golden **5 本すべて非退行** = `verify_tavern_map` 43/43・`verify_town_map` 85/85・`driver_heromark_signplate` 46/46・`verify_quest_walk` 25/25・`verify_town_exit` 素 23/23) | 100% | 酒場と街に NPC を置く(酒場 8 体 / 街 11 体)。⭐ **NPC は歩けないタイルにしか立たない**=マスクを 1 文字も変えず到達性 assert が構造的に壊れない。⚠⚠⚠ **既存 golden 4 本が「札の中心の elementFromPoint」を測っており、街の札は 242px = 3.8 タイル幅** → 起草時の候補 5 件が実測で潰れた。⚠ **巡回は端点でなく経路上の全マス**で札との交差を見る。⚠⚠⚠ **§2-6「NPC は移動先の計算に 1 ミリも影響しない」は `ev.stopPropagation()` を足した瞬間に嘘になった** — `.npcUnit` が 96x96 の「タップを食う板」になり、`verify_town_map` がタイル中心の実座標で押す 7 点のうち 3 点を巡回が覆って **85→84** に落ちた(被覆率 38%/15%/8% = **間欠的な赤**)。直し方は巡回 2 本の**端点だけ**移動 + 再発防止の (1f)。⚠ **スプライトは足元タイルから左右 ±48px はみ出す**(SPRITE 96 > 街の TILE 64)ので「隣の列なら安全」は偽。⭐⭐⭐ **(2b) は §8 の変異 1 本では原理的に赤くならない**(z-index 3 < 札 4)→ `zorder` を「z-index 5 + `mason` を **(10,1)**」の複合へ。**(11,2) でも緑のまま**だったのは、(2b) が見るのが札の **中心の 1 点**で、mason(11,2) の矩形 x[688..784] が中心 x=672 を含まないため。撤退 `?npc=0`(⛔ `display:none` で残さず **DOM ごと作らない**)。⭐ **STEP4 も完了**(2026-09-02) = codex1 へ町人 6 種を発注し 613 秒で納品。⭐⭐⭐ **棚卸しで `villager-*` 6 体が 2026-07-17 に納品済みなのに台帳未登録のまま眠っていた**ことが判明し(§2-2 の「町人は 1 枚も無い」は **ゲーム側の assets を測った結果** で、納品元の codex1 を見ていなかった)、合計 **12 体** を取り込んだ。⚠⚠ 納品に **浮遊ゴミ(最大 279px、bbox を 68px 広げる)とマゼンタ残り(最大 3.37%、全部 alpha=255)** があり `tools/clean_codex1_frames.py` を新設(⭐ パッカ本体は 1 行も触らず `-clean` 派生へ逃がし、既存 15 枚のバイト不変を `git status` で確認)。⚠⚠⚠ **`town-guard` の char_ratio だけ 0.7160 で他(0.61 前後)と違う — 写すな**(槍が頭より高く bbox 608px に対し体は 513px。素の 0.604 だと体高 49px = パーティ比 -15% の小男になる)。⭐ **受入 32/32 は期待値を 1 つも書き換えていない**(`sprite:` はどの assert にも焼いていない)。golden **5 本非退行** = `verify_tavern_map` 43/43(changelog で tavern.html を触った**後**の値) / `verify_town_map` 85/85 / `driver_heromark_signplate` 46/46 / `verify_quest_walk` 25/25 / `verify_town_exit` 素 23/23。残＝実機体感 10 項目 |
| 42 | [2026-09-02_world-step-tuning.md](2026-09-02_world-step-tuning.md) | **完了・push 済 `edc97d6..d2c0d12`** = `3c632d6`+`f4ccc37`+`180f20d`+`d2c0d12`(`verify_world_steps` **30 → 33/33 PASSED PENDING 0** / `--negative` **56/56・変異 12 → 15 本とも空振り 0** / 既存 golden **6 本すべて非退行・期待値の変更 0 件** = `verify_world_map` 57/57・`verify_quest_walk` 25/25・`verify_town_exit` 素 23/23・`verify_title_screen` 86/86・`verify_tavern_map` 43/43) | 100% | 刻みを 2.5 マスへ半減(`STEP_MAX_PX` 320→160)+ 印を中継点と同寸(44px/9px)+ 札より下の層へ(z-index 4→3)。⭐ **実機で #40 の 5 マス / 6px が 2 つとも「足りない」と判定された出戻り**。⚠⚠⚠ **「間隔を半分」だけで受入条件 (2d) が破れる**(`cross_n__swamp@1`×沼地札 / `fort__lakeside@1`×砦札。**マーカーの大きさとは無関係**)。⭐⭐⭐ **崩れた主張 — 「矩形の交差」「中心の奪取」「隅の奪取」は 3 つの別条件だった**(依頼書は 2 つと書いていた)。起草時の「クリック点は 1 つも奪われない」は**札の中心 1 点だけ**を測った結果で、**隅は奪われる**: z4/32px→**2 件** ・ z3/32px→0 件 ・ z4/44px→**2 件** ・ z3/44px→0 件 = **犯人は層だけで、マーカーの大きさは無関係**。⭐⭐⭐ さらに **押し込む向きで奪う隅が変わる**(z4 で下から出ると **下の隅**、変異 `signsteal` の `-40px` で帯へ押し込むと **上の隅**)→ **(2d) を「中心 + 四隅」の 5 点で設計したのが正しかった**(中心 1 点だけなら素の z4 も変異も**両方空振り**していた)。⭐⭐⭐ **(2f) の 44 / 9 直書きは「非推奨」ではなく「原理的に不可能」** — 画面値は zoom 0.8125 込みで **35.75px / 7.31px**、中継点との差は **0.000px** → `wayRef` と突き合わせる以外に書きようが無い。⚠⚠ **依頼書の数値は地図座標 / ドライバの detail は画面座標**(逃げ幅 2.0px = **1.6px** / 重なり 44.0x20.0px = **35.8x16.3px**)→ **合わないときはまず単位を疑う**。⚠ **§2-6 の「(3b) は cap=160 で 10 回」は誤りで実測 11 ホップ**、**§8 の (3c) は起点だけ 15→24 種へ直してタップ数 27 を据え置いていた**(実測 **45 タップ**)→ ⭐ **見込み表は「一緒に動く数」を全部たどる**。⭐⭐⭐ **変異 `coarsestep` が「(0b) は緑のまま (1e) だけ赤」を機械証明した** = 「cap をページから読む assert」は必ず「人間が要求した値」を縛る assert と対にする。⭐ **変異の `to` のコメントは飾りではない** — `var STEP_MAX_PX = 320;` だけだと `from` と同じ **22 バイト**になり、起動時ガード「置換の前後が同じ長さ」で **exit 3 でドライバごと死ぬ**。⚠ **orchestrator が項目1 の完了条件を「31/31 FAILED 0」と指示した**が依頼書 §4 は「STEP1 だけなら (2d) が赤 = 想定どおり」→ worker は assert を弱めず `blocked` で正しく停止(**完了条件は依頼書の記述と突き合わせてから worker へ渡す**)。⚠ **先行 worker の「`PORT` / `MAX_TAPS` は未変更」報告が実物と食い違った**(実物は `:169`=9600 / `:670`=16 へ変更済)→ **他 worker の報告は実物を grep で確かめてから使う**。撤退は既存 `?walkstep=0` を流用(⛔ 新設しない)。⭐⭐⭐ **追補 `f8b07d3`(同日 第 2 回・§14)= 止まれる点 9px → 13px / 拠点 14px → 17px** — 実機で「止まるスポットが、止まれない点線の点と区別がつかない」と判定された。⭐⭐⭐ **真因は「比べる相手」** — (2f) は「刻み点 vs 中継点」を縛っていたが、ユーザーが見比べていたのは **「止まれる点 vs 点線の点(`stroke-width: 6` = 直径 6px)」**で、9px との差は **1.5 倍**しかなかった(色はどちらも `--route-ink`)。⇒ **「A を B と同じ大きさに」を受入条件にすると、A が本当に紛れる相手 C を機械が一度も見ない。**⭐ **(2f) は 1 バイトも直さずに済んだ**(`wayRef` の実測から引く設計が効いた)。⚠⚠⚠ **直す必要があったのは負のコントロールのアンカーだけ** — `smallstep` が `width: 9px; height: 9px;` を**逐語**で握っており、放置すると変異が当たらない(救い = `--negative` がアンカー不一致を **exit 3** で落とす)。⇒ **見た目の px を動かすときは、その px を逐語で握る変異を先に grep する。** 検証 = `verify_world_steps` **33/33** / `--negative` **56/56**(変異 15 本・空振り 0)/ 既存 golden **5 本非退行**。⚠ 残懸案 = 沼地・砦の 2 点は点が大きくなったぶん札の下に隠れる面積が増えた(逃げ 2.0px は不変なので押せる) |
| 43 | [2026-09-03_world-heromark.md](2026-09-03_world-heromark.md) | **完了・push 済 `c1c85e0..05fc35a`**(= `d4db293`+`1d076db`+`6ac5653`+`05fc35a`)(新規 driver `verify_world_heromark` **18/18 PASSED FAILED 0 PENDING 0** / `--negative` **24/24・変異 7 本とも空振り 0** / 既存 golden **7 本すべて非退行・期待値の変更 0 件** = `verify_world_map` 57/57・`verify_world_steps` 33/33・`verify_quest_walk` 25/25・`verify_town_exit` 素 23/23・`driver_heromark_signplate` 46/46・`verify_title_screen` 86/86・`verify_tavern_map` 43/43。✅ **push 済 `c1c85e0..05fc35a`**) | 100% | ワールドマップでも主人公の頭上に ▽ を出す(3 ページ目)。⭐⭐⭐ ▽ を主人公の 96px セルの**内側**(実測の余白 **11px**)に収めることが、`verify_world_map` (7f)「駒が札を 10% 以上隠さない」を ▽ にも継承させる唯一の条件 — (7f) は `heroGeom()` から計算した箱しか見ないので、外へ出すと**永久に気づかない**(変異 `markbox` が機械証明)。⚠ z-index は town の 5 を写経せず **6**(世界地図は主人公 5 > 札 4 で並びが逆)。撤退 `?heromark=0`(3 ページ目・独立読み・sessionStorage へ写さない)。⭐⭐⭐ **崩れた主張 — 変異 `markhit`(CSS の `pointer-events: none` → `auto`)が空振りした。** アンカーは当たり `pointer-events="auto"` まで届いていたのに **▽ が奪った点は 0 件**で (2a) は緑のまま。真因は**幾何** = ▽ は主人公の頭上(ノード中心から **65〜78 map px 上**)に浮いており、ノードの当たり判定箱は 44px(中心 **±22 map px**)なので**原理的に交差しない**。⇒ #38 の作法どおり**変異のほうを直し**、「CSS を `auto` にする」+「`placeHero` の ▽ の `top` をノード中心へ落とす」の**複合変異**へ作り替えて 1/120 点(`node(pier):中心`)を奪わせた。**教訓 = 「`pointer-events` を切り替える変異」は、その要素が実際に測定点と重なる位置に居るときにしか欠陥にならない。設定と幾何は別条件。** ⭐⭐⭐ **その複合変異が装置自身の穴を 1 件見つけた** — 項目 3 が書いた (2a) は `stolen` を**数えて detail に出しているだけ**で `why` へ入れておらず、**見出しが主張している条件を一度も判定していなかった**。素のアームは `pointer-events: none` で自明に緑なので「緑」と「判定していない」が見分けられない状態だった(⛔ 受入条件は 1 バイトも弱めず、見出しが最初から主張していた条件を足しただけ)。**教訓 = 「測って detail に出す」と「判定する」は別。** ⭐ **1 変異 = 1 置換の前提も崩れた** → 凍結機構を `edits: [{from,to},…]` へ拡張(検算は置換ごと / 2 本目は 1 本目を当てた後の body で数える = アンカーの食い合いも見る)。⚠ **起草時の行番号が 2 件ずれていた**(`:491`→`:492` / `:1075`→`:1074`。同日・同 HEAD なのにずれた = 座標でなく構造で書く)。⭐ (4b) は「実装済みの (1a)(1b)(2a) を**両アームへ当てる**」設計にしたので、撤退アームでも `measureMark`/`measureHits` の 3 点セットが要る(⛔ `measure()` だけだと `markalways` が空振りする)。⚠ changelog は鳴らない(`world.html` は `check_changelog.py` の `GAME_LOGIC` 外)。✅ **実機体感もユーザー確認済み(2026-09-03「ひとまずオッケー」)= 残タスク 0**。⭐⭐⭐ ただし **宣言された穴**が 1 つ残っている — `--hm-w`/`--hm-h` と `HM_W`/`HM_H` を**同率で縮めると 18/18 緑のまま ▽ が小さくなる**(相対比較の assert は「両方が同じだけ壊れる変更」を原理的に検出できない。同型が `driver_grid_p7` (4c)(4f) と `driver_heromark_signplate` の `labelTop` にもある)。⭐⭐⭐ **assert は決定を記録するもので、決定を先取りするものではない** → 打ってよいかの基準は**「その値はもう決まっているか?」**。「ひとまず」= 暫定合格なので**上限は打たず、塞ぐなら下限だけ**(源は ▽ と独立した「拠点の点 17px」か「`.worldSign` の高さ」)。詳細は §12-8 |
| 44 | [2026-09-03_enemy-name-label.md](2026-09-03_enemy-name-label.md) | **完了 `1545fa6`+`d0f4124`+`92a2f4d`+`c866791`**(新規 driver `verify_enemy_name_label` **30/30 PASSED FAILED 0 PENDING 0** / `--negative` **58/58・変異 17 本とも空振り 0** / 既存 golden **4 本すべて非退行・期待値の変更 0 件** = `driver_grid_p7` 44/44・`driver_heromark_signplate` 46/46・`driver_cast_circle` 53/53・`driver_sce1_events` 214/214。dev-loop 4 項目・停止 0 回。⛔ **push 未実施**(ユーザー承認事項)) | 100% | 敵 50 種の頭上に名前札を出し、札を 70% へ。⚠ 敵の頭上は既に埋まっている(バッジ -46..-18 / 状態 -27..-11.4 / HP -10..0)ので札は -27・バッジは -58 へ。⭐ 添字並列の DOM 配列が **10 → 11 本**になり `driver_cast_circle.js:402` の撤去ループが黙って取りこぼす。⭐⭐⭐ 比の assert だけでは「素と 70% を同率で膨らませる」変更が緑で通るので、**札の高さ ÷ 最小の敵のスプライト高 <= 0.30** の絶対量 (2f) を併置(源が CSS とゲームデータで独立)。⚠⚠ 母集団ガードは「立たなければ FAIL」(skip=緑 にすると assert が静かに消える) — 変異 `bigonly` が規則自体を検査。撤退 `?namelabel=0`<br>【実装窓の実測】⚠⚠⚠ **起草の主張が 7 件崩れた**(どれも受入条件を弱めず**変異のほうを直した** = #38 の作法): ① **「敵 46 種」は実測 50 種**(top-level キー 50 / `name:` 50 で 1:1・`rosterMin` 54。(0b) は件数を写経せず 2 経路一致で縛ってあるので腐らない)② **(2b) の帯 0.68〜0.73 は実測を通らない**(`#warriorLabel` は 52→38.2 = **0.7346**)。真因は「字送りが線形に縮まない」ではなく **「縮まない固定費 6px」** = `border-width: 0.7px` を Chrome が端末の 1px へ丸める分 + 共通定数 4px で、**名前 span 単体は 36→25.2 = ちょうど 0.7000**。固定費は文字数に依存しないので**短い札ほど比が上がる**(3 文字 0.7346 / 9 文字 0.708)⇒ 箱の帯を 0.68〜0.75 へ広げ、**同時に「名前テキストの幅の比 0.68〜0.72」を AND で追加**(⛔ CSS は 1px も触っていない。⛔ 敵の札へ測定点を移すのは**原理的に不可能** — 撤退アームに `.enemyLabel` は 1 枚も無く NPC 名はランダム)③ **(2d)「味方と異なる」だけでは変異 `noenemycss` が空振り**(CSS を丸ごと消すと `rgba(0,0,0,0)` = 「異なる」ので緑)⇒ **alpha > 0 を AND** ④ **`hpshift` は無条件だと空振り** — 負のコントロールは素も撤退も**同じ変異ポートから配る**ので両アームが等しく動く ⇒ `NAME_LABEL_ON` で分岐(⭐⭐⭐ **恒等 assert は「片方のアームだけを壊す変更」でしか赤くならない**)⑤ **`nonull` の (0d) は原理的に赤にならない**(`push(null)` は**撤退枝にしかない**)⇒ (4a) に 5 本目「OFF の `labelArrayLen === enemies.length`」を AND で足して移設 ⑥ **`noteardown` の (3d) も原理的に赤にならない**((3d) は本ドライバの配信 `index.html` しか見ず、別ドライバの撤去ループとは無関係)⇒ **(3e) を新設** = 「配信した 2 本のソースを突き合わせ、`createEnemyDom` が push する配列 11 本が撤去ループに 1 本残らず並ぶ」(⛔ 件数は期待値に持たない = 12 本目が増えた日にも効く)⑦ **`bothgrow` は `.allyLabel` だけだと空振り**((2f) が見るのは**敵の札**)⇒ 味方+敵 4 本を**同率** 1.4545 倍(11→16px / 7.7→11.2px)へ。⭐⭐⭐ **罠A を実物で 1 回測った** — `driver_cast_circle.js` の撤去ループから 11 本目を手で消しても **53/53 PASS のまま**(検算 6.11 も PASS)= **あのドライバでは永久に検出できない**ことを実証(復元 + `git diff --quiet` で検算済)。(3e) が唯一の検出器。⭐⭐⭐ **`bothgrow` が §2-11(e) を機械証明した** — (2a) 0.7176 / (2b) 箱 0.7269・テキスト 0.7001 / (2c) ばらつき 0.00px は**全部緑のまま**、**(2f) だけ 0.3588 > 0.30 で赤** = 比だけで縛った 3 本をすり抜ける変更を絶対量 1 本が落とした(`record` = 「緑のまま通る節」も同じ走行で記録する仕掛けを新設)。⭐ **`bigonly` は 1 行置換で書けない**(小さい敵 7 種のどの数値も名簿の中で一意にならない)ので **変換関数 `transform` で配る機構**を新設し、(n0a) の代わりに「名簿の最小 素 **54** / 変異 **72** が境界 70 をまたいだ」を検算。⚠ このときの (2f) は「**測れないから赤**」なので detail に `population: none` を出して「値が悪いから赤」と区別。⚠⚠⚠ **踏んだ罠 3 つ**: 変異の `to` が素に元から居ると (n0a) が破れる(`statusdetach` の空白なし形は else 枝に、`noclear` の接頭辞形は素の行に元から居る)/ **置換前後が同じ長さだと `exit 3`**(`-27);`→`-30);` は同長 ⇒ 小数を足す)/ **配置関数の呼び出し行を丸ごとアンカーにすると (0g) が赤**(`FORBIDDEN_IN_SELF` と `SELF_MARK` の 2 段に同時に当たる ⇒ 短いアンカー)。⭐ 撤退の実測: 札 DOM **8→0 枚** / 状態列の class `status-slot`→`enemy-status-slot` / 主人公の札 **38.2x14.4→52.0x20.0px**(比 0.7188)/ バッジ **-58→-46** / `labelArrayLen` 8/8 / HP バーとスプライトは**主人公も敵 3 体も 4 辺とも 0.00px 差** / 事故 0 件。⭐ ON の実測: 札の高さは 3 種とも 14.38px で完全一致 / 札の下端は HP バー上端の **2.63px 上**(味方の 2px 食い込みは敵では解消)/ バッジ下端と札上端の隙間 **3.00px**(罠B の 9px の重なりが消えた)/ 最小の敵 rat は 14.38÷54.00 = **0.2662**。残 = 実機体感 6 項目。⭐ **唯一の本当のリスクは iPhone 縦 390x844 で font 7.7px の札が読めるか** — 読めなければ 8.5px 前後へ戻せる(受入は比で書いてあり (2f) も 0.287 で上限内 = **ドライバを 1 バイトも直さずに調整できる**) |
| 45 | [2026-09-03_road-events.md](2026-09-03_road-events.md) | **完了 `a9485f3`+`475839d`+`413ca36`+`4d7db24`+`4027d87`**(新規 driver `verify_road_events` **25/25 PASSED FAILED 0 PENDING 0** / `--negative` **43/43・変異 14 本とも空振り 0** / 既存 golden **7 本すべて基準どおり** = `verify_world_steps` **33/33(3 回連続)**+`--negative` 56/56・`verify_world_map` 57/57・`verify_quest_walk` 25/25・`verify_world_heromark` 18/18・`verify_town_exit` 素 23/23・`verify_title_screen` 86/86・`verify_tavern_map` 43/43。**期待値の変更は 1 件だけ**(heromark (3a) の +1 キー)。dev-loop 5 項目・停止 0 回。⛔ **push 未実施**(ユーザー承認事項)) | 100% | 街道の出来事 Phase 1。停留所 17 箇所で低確率の 1 枚テキスト + 二択、d20 で結末が分岐。⚠⚠⚠ 罠A = **`js/skill-check.js` は world.html に載っていない**(`grep -l` はコメントを拾うので 5 枚返す)→ `if (window.SkillCheck)` で書くと例外も出ずに全イベントが静かに消える。⚠⚠⚠ 罠B = **party の 4 人分は sessionStorage / localStorage は主人公 1 人だけ**。⭐ 器は `#worldEventBox`(⛔ `#worldEnterAsk` を流用すると `verify_world_steps` (4c) が赤)。⭐ 判定パネルは z-index **105** で `#worldStage` の scale の外。⭐ 期待イベント数は横断 1 回で **0.85〜0.91 件**(38% は無風)。撤退 `?roadevent=0` / 種 `?roadseed=N`<br>【実装窓の実測】⭐⭐⭐ **依頼書が予見していなかった最大の副作用 = 器が全画面モーダル(`position: fixed; inset: 0`)なので、地図を実クリックで歩く既存 golden のタップを構造的に全部飲む。**§8 が要求した「既存 golden 7 本の非退行」が **この機能自身によって成立しなくなった**(実測 = `verify_world_steps` **間欠**(3 回中 1 回だけ緑・指紋「1px も進まなくなった」)/ `verify_world_map` **51/57**(指紋「押した先が別要素(worldEventBox)」「遷移待ちタイムアウト」)/ `verify_quest_walk` **20/25**(指紋「12 回押しても着かない: mine」)/ `verify_world_heromark` **17/18**(これだけ器と無関係で (3a) の正当な期待値更新))。**#41 の `stopPropagation` 事件と同型だが 1 段悪い** —— #41 は NPC が 96x96 の板だったので巡回の端点をずらして避けられたが、**全画面はデータをずらして避ける道が原理的に無い**。⇒ 教訓 = **「モーダルを 1 枚足す」は、そのページを実クリックする既存ドライバ全部への破壊的変更。着手前にそのドライバを全部列挙する。**⚠⚠⚠ **ユーザー決定「map と quest_walk は `?roadevent=0` を URL へ足す」は物理的に不可能だった** —— `verify_world_map` (7e) / `verify_quest_walk` (2d) / 同 (3a) が **歩く当のページで `location.search === ""` を assert** しているので、クエリを足すとその 3 本が赤くなる = ⛔「期待値は 1 つも書き換えない」と両立しない。⇒ **3 本とも「直すのは押し口だけ」**(両ドライバが `⛔ ?walkstep=0 を URL へ足して逃げない` と同じ規律を既に明文で持っている)= `dismissRoadEvent()`(器が開いたら**判定を伴わない選択肢**「立ち去る」を押して進む)を入れ、**期待値の変更 0 件**かつ **3 本とも「出荷される姿(イベントあり)」を測り続ける**形にした(撤退アームで走らせると #39 の「撤退アームだけの受入条件は永久緑」に落ちる)。⚠ 器を閉じるクリックは **タップ数に数えない**((3b) の刻み回数と (4b) の `arrivalCount` 増分が狂う)/ `ARM_MS`(260ms)のゴーストクリック除けは **導入と結末で 2 回**待つ。⚠⚠⚠ **(3c) の `if (walkStepOff) return false;` は実質デッドコード** —— 撤退モードでは `goToPoint` が経路全部を 1 ホップで歩き切り `onArriveStep` が必ず `onArriveNode` へ抜けるので `maybeRoadEvent` にそもそも到達しない ⇒ 変異 `retreatfire` が空振りし、#38/#43 の作法どおり **変異のほうを** `walkPath` の rAF から発火させる形へ作り替えた(**「ガードを 1 行消す」変異は、そのガードに到達する経路が実在するときにしか欠陥にならない** = #43 の `pointer-events`「設定と幾何は別条件」と同型)。⭐ **地形は 4 種では割れない**(会議の主張。実データだと `mountain` が 9 件 = 母集団 17 の半分超)→ **5 種**へ増やし、実装は `TERRAIN_RANK`(刻み点は**より辺境な側が勝つ**)だけで座標表 0 件のまま §2-5 の割りを完全再現。⭐ **変異 `fireevent` を作り替えた** —— 旧「刻み点到着で確認ダイアログを開く」は #45 の後は**正しい振る舞い**になり欠陥を検出しなくなるので、**器の取り違え**(`RE.open(ev,…)` → `elAsk.classList.add("show")`)へ。これで (4c) の条件②(`askOpen()` が false)が今後も機械で守られる。⚠ **`verify_world_steps` 側にも `?roadseed=45` が要った** —— 素の一巡で 1 件も出ない確率が **約 7%** あり、そのとき `fireevent` が「たまたま何も起きなかった」で空振りする(この種での実測 = 3 件発火)。⭐ (4c) は **見出しの文言だけ**書き換え(assert 本体は 1 バイトも触っていない。実際に守っているのは ① pathname が /world.html ② `askOpen()` が false ③ 刻み点に着いたタップが 1 件以上、の 3 つ)。残 = §9 の実機体感 10 項目 + push |
| 46 | [2026-09-03_walk-block-and-vfx-fixes.md](2026-09-03_walk-block-and-vfx-fixes.md) | **完了 `6aeb1dd`+`e3dfb4a`+`0e1076c`+`e5caadf`**(新規 driver `verify_walk_block` **23/23 PASSED FAILED 0 PENDING 0** / `--negative` **54/54・変異 16 本とも空振り 0** / 既存 golden **4 本すべて非退行・期待値の変更 0 件** = `driver_cast_circle` **56/56**(項目2 で 53→56)・`driver_grid_p8` 56/56・`verify_enemy_name_label` 30/30・`probe_paint_overlay` assert 無し。dev-loop 4 項目・停止 0 回。⛔ **push 未実施**(ユーザー承認事項)) | 100% | 段差の迂回 / 魔法陣を足元へ / 頭上バッジ廃止。撤退 `?walkblock=0` / `?castanchor=0` / `?enemybadge=1`(3 本ともページ内で完結)。⭐⭐⭐ **陣のズレは `zTf` が `scale(camZ)` を前置し `transform-origin:50% 64%` と衝突するせい**(実測 camZ=0.25 で 54.06px、予測式と小数第2位まで一致)→ **後置する姉妹関数 `zTfAnchored`** で修正、camZ=1 では 1 ビットも変わらない。⚠⚠⚠ **既存 golden 53/53 が緑だった理由は依頼書の見立てより深かった** —— `driver_cast_circle.js:312` の `* camZ` 欠落と 5.4 の部分一致に加え、**そのページは camZ=1 でしか走っていなかった**(camZ≠1 の腕が 1 本も無い)→ camZ=0.25 の腕 5.6/5.7/5.8 を新設して **53→56**。⭐⭐⭐ **絵からのマスク自動生成は AUC 0.66-0.69 で不成立**(実測して棄却。変異 `overblock` が「なぜ採らなかったか」を機械で示す = 疑似乱数 53% で塗ると塞がり率 58.75%→**80.60%**、到達不能な床 **167 マス**)。⚠ マスクには**規則①②④**があるが定義本体がリポジトリに無い(§2-4 に復元)<br>【実装窓の実測】⚠⚠⚠ **依頼書が自己矛盾していた** —— §4-1 STEP C の候補の筆頭 `(35,8)(36,8)`(確度 **中**)と `(33,12)`〜`(36,12)`(低)は、**同じ §4-1 の「⛔ 絶対に `#` を書かない」リストの内側**(行8 col31-41 の軌道の床 / マスク行10 col33-36 の倒れた板)。⇒ **⛔ リストが勝つ**と判断(候補表は「暫定・確度 中/低・道具で確認してから」、⛔ は「絶対に」。かつ §4-1 冒頭が「**迷ったら入れない側へ倒す** — 塞ぎ漏れは体感が変わらないだけだが塞ぎすぎは詰みを生む」と優先順位を先に決めている。通すには `verify_walk_block` (1f) を弱めるしかなく、それは「期待値を変更に合わせて緩める」そのもの)。⭐ **道具で見た結論も残す** = `(35,8)(36,8)` には確かに X 字の筋交いを入れた**木の架台**が描かれており、**⛔ リストの「行8 は全部レールの敷かれた床」という括りのほうが実物より 1 段大雑把**(レールが row8 を走るのは col37-41 だけ)→ **保護範囲を狭めるのは受入条件の緩和なのでユーザー判断が要る**として §12-8 の残課題へ。⭐ **実際に塞いだのは 2 マス** = **`(39,10)`**(岩棚の縁の石の段差と木の擁壁。候補・確度低を 8 倍拡大で確認)+ **`(33,11)`**(⚠ **候補表に無い 1 マス**。岩盤の背の稜線がタイル中心を通り、しかも `(30,12)(31,12)(32,12)` と `(34,11)(35,11)(36,11)` で既に塞がれている**斜めの尾根の唯一の切れ目**だった。承認された 2 枚の拡大図の中で・同じ道具で確認でき・⛔ に触れないので追加)。塞がり率 **527→529 / 58.75%→58.97%**。⭐⭐⭐ **効果は経路長で機械的に出た** —— 南半分(泉の間 = 玉座)への歩数が **ボス 40→44 / 護衛 43→47** = 尾根を突っ切らず **4 タイル遠回り**するようになり、北側 7 体とゲート 3 つは **1 歩も変わっていない**(`(39,10)` 単独では歩数は 1 歩も動かない = 体感を作っているのは `(33,11)` のほう)。⭐ 撤退は「#46 前のマスクをもう 1 セット持つ」ではなく **`applyPaintingBlocking` に 7 つ目の門番を足す**形(絵のマスクを 1 つの正に保つ。ON のときは Set が空でループも走らない)。⚠⚠⚠ **(4a)「?walkblock=0 で 58.8% へ戻る」は依頼書のままだと永久緑** —— 1 マスも足さなければ素のアームも 58.75% なので自明に成立する ⇒ ① 撤退アームが本当に撤退アームか ② 足した `#` が **1 マス以上** ③ 撤退で外れた数 = 足した数 ④ **素のアームとの差**がちょうどその数、の **4 条件 AND** へ。⚠ 依頼書の **58.8% は丸め**で実測 **58.75%(527/897)** = 丸めた % に帯を張ると ±1 マス(0.11%)を素通りさせるので**マス数そのもの**で縛った。⚠⚠⚠ **変異 `sealp8` は依頼書どおり (37,16) 1 マスだけでは原理的に空振りする** —— 行16 の開き run が col34-**37**、行17 が col**36**-39 なので **`(36,16)`-`(36,17)` が並行の 4 近傍リンクとして残る** = (37,16) は「唯一の連結点」ではない ⇒ #38 の作法どおり**変異のほうを直し**、★P8 で開けた **4 マス全部**((37,16)(36,17)(37,18)(38,19))を塞ぐ = **P8 前の状態へ戻す**形へ。⭐⭐⭐ **そのとき (1a) は緑のままだった** = up/left/right の 3 ゲートは全部北半にあるので「**ゲートへ行けるか**」だけを見る assert では**部屋の半分が死んでも気づけない** = (1b)(1d) が別に要る理由の機械証明。⚠ **変異 `sealrail`(枕木 col32-38/row20 を 7 マス)を塞いでも到達不能は 1 マスも出ない**(row19/row21 に迂回路)⇒ 「絵の規則②を破ったこと」を捕まえられるのは **(1f) だけ** = ⛔ (1f) を「到達性で代用できる」と考えて消してはいけない。⚠ 起点は `(19,11)` ではなく **`(21,11)`**(起草窓が left ゲートと読み違え)/ (1a) の **`down(36,24)` は着手前から到達不能**(`exits:[]` の終端ノードなのに `nodeGateTile` が辺の中点を返し門番が 4 方角を無条件に開ける)→ 除外を**理由付きで 1 つだけ**宣言し「除外が黙って増えたら (1a) 自身が赤」/ (1d)「連結成分 1 つ」も着手前から赤(実測 `[369,1]`)→ 言い換えても厳しさは落ちていない(変異 `island` が実証)。⚠⚠⚠ **§2-6「火炎コアも同型の欠陥」は誤り** —— `.fxFireImpactCore` は `animation` が transform を animate しており **CSS カスケードで inline style が負ける**(実測 `inline="translate(-50%,-50%)"` / `computed=matrix(0.45,0,0,0.45,0,0)`)= `zTf` は**一度も効いていなかった**。ズレは camZ 非依存の**定数 (+30,+30)px** ⇒ `zTfAnchored` ではなく **CSS の margin 中央アンカー**で修正し、変異 `fireonly` も定義し直した(依頼書版なら空振り)。⚠⚠ **§8 の計測レシピ `el.parentElement.getBoundingClientRect()` も使えない**(`#vfxLayer` は CSS を 1 行も持たない = `position:static` で包含ブロックにならない)→ `left:0;top:0` の 0x0 マーカーで**原点そのものを実測**。⚠⚠⚠ **§8 の golden 表も 1 行崩れた** —— バッジ既定 OFF で `verify_enemy_name_label` が **30/30→27/30**。落ちた (1h)(3c)(4a) はどれも `population: none` = **母集団の喪失**(#44 の 3 本はバッジの矩形そのものを測っている)⇒ ⭐ **期待値を 1 バイトも弱めず装置で母集団を復元**(`BADGE_QUERY='enemybadge=1'` で全アームを開く)= **30/30 に復帰**。根拠は本チケットの (3b) が恒等を独立に証明していること(**最大差 0.000px**)。⭐ **恒等の決定論レシピ** = `shakeX=shakeY=0` → `setCamZoom(0.8125)` → 絶対タイルで `createEnemy`+`createEnemyDom` → `camX = wx-(innerWidth/2)/camZ` → **`updatePositions()` ではなく `renderWorld()` を直呼び**(前者はカメラのグライド分岐を持つので非決定的)。⚠ 変異 `origin00` は (2e) を**原理的に赤にできない**(origin は rect の位置しか動かさず width は `f*camZ*w` のまま)→ record へ。残 = §9 の実機体感 5 項目 + push |
| 47 | [2026-09-03_road-harvest.md](2026-09-03_road-harvest.md) | **完了 `a30bb39`+`c492253`+`abf0267`+`24e1e7a`**(新規 driver `verify_road_boon` **20/20 PASSED FAILED 0 PENDING 0** / `--negative` **48/48・変異 15 本とも空振り 0** / 既存 golden **15 本すべて基準どおり・期待値の変更 0 件** = `verify_road_events` 25/25・`verify_world_steps` 33/33・`verify_world_map` 57/57・`verify_quest_walk` 25/25・`verify_world_heromark` 18/18・`verify_town_exit` 素 23/23・`verify_title_screen` 86/86・`verify_tavern_map` 43/43・`verify_ability_scores` 24/24・`verify_darkvision` 25/25・`verify_mercenary_roster` 44/44・`verify_recruit_size` 82/82・`verify_run_chronicle` 73/73(⭐ 単独実行。並走させると 71/73 に揺れる)・`driver_grid_p8` 56/56(1 回目 55/1 は flake、再実行で確定)・`verify_player_sheet` **66/70 FAILED={(2c)(2d)(8a)(8f)} = 着手前と同じ集合 = 非退行**(#48 の担当)。dev-loop 4 項目・停止 0 回。⛔ **push 未実施**(ユーザー承認事項)) | 100% | 街道の実り Phase 2。成功した出来事の結果を `dragonfighters.roadBoon`(sessionStorage 1 キー・配列・上限 3)で次の潜行へ渡し、`index.html` が起動時に 1 度だけ消費する。効果は 2 種(糧 = 全員 maxHp +3 / 備え = 最初の交戦で敵の初手を潰す)で新機構ゼロ。⚠⚠⚠ 罠A = **恩恵を `check:false` の枝に付けると既存 golden 3 本を巻き込む**(`verify_world_steps:774` / `world_map:683` / `quest_walk:831` が `filter(x => !x.check)[0]` を機械的に押して index.html まで進む)→ 恩恵は**判定に勝った枝だけ**。⚠⚠⚠ 罠B = **`appendLog` は innerHTML 代入で index.html に escape ヘルパが 0 件**→ label はホワイトリスト正規表現を通す。⭐ 消費は `pendingSummon` 型(⛔ `questFlags` は removeItem 0 件 + 「無い = 開放」の逆向き既定なので手本にしない)。⭐ `verify_road_events` (2c) は `setItem` を assert していないので world 側へ 1 キー書いても緑。⚠ **`verify_player_sheet` は着手前から FAILED = {(2c),(2d),(8a),(8f)}**(#45 `475839d` の積み残し・集合で見る)。撤退 `?roadboon=0`(world / index で独立)。⭐⭐⭐ **依頼書の主張が 4 件崩れた** = ① **ポート 9770 は空きではなかった**(`verify_road_events` が `--negative` で 9761〜9774 を占有する。**ポートの空きは base だけでなく `--negative` のレンジまで数える**)→ 9790 へ ② **`taintlabel` の当たり先が誤り**(BOONS を汚しても (2e) の観測はドライバの仕込みなので届かない → `index.html` の白名簿を外す変異へ移設。**変異は「仕様の言葉」でなく「その assert が実際に読む値の供給口」へ当てる**)③ **`boxleak` 1 本では paint() 側を検出できない**(通常導線では `close()` が先に消す)→ (1e) に **器を閉じずに描き直す探針**を足し変異を `boxleak_close`/`boxleak_paint` の 2 本に割った ④ ⭐⭐⭐ **`dismissboon`(罠A の再現)が 1 度空振りした** —— **門番が 2 本あった**(`world.html` が判定なしの枝で `outcome=null` を渡すので `!choice.check` を外しても `!outcome.success` が同じ枝を落とす = `!choice.check` は現状**冗長な二重の門番**)。**⛔ 変異の設計を「名指しされた 1 行を消す」で終わらせず、消して欠陥が実際に発現するかまで筋を追う**。⚠⚠ **実プレイ系 golden は逐次で走らせる**(`verify_run_chronicle` は並走で 71/73・単独で 73/73)。⚠ **単発の赤はまず 1 回だけ再実行**(`driver_grid_p8` の 55/1 は変異アームの観測が `null` になる flake だった) |
| 48 | [2026-09-03_sheet-skillcheck-pages.md](2026-09-03_sheet-skillcheck-pages.md) | **完了 `26a3f24`+`777cd34`+`5d00fc5`+`d9fd5d2`**(`verify_player_sheet` 素 **73/73 PASSED FAILED 0 PENDING 0**(着手前 66/70 FAILED 4)/ `--negative` **111/111・変異 17 本とも空振り 0**(着手前 91/101 FAILED 10)/ 既存 golden **2 本とも基準どおり・期待値の変更 0 件** = `verify_ability_scores` 24/24・`verify_road_events` 25/25(⭐ どちらも単独実行)。⭐ **本番コード 0 バイト** = 触ったのは `tools/verify_player_sheet.js` の 1 ファイルのみ。dev-loop 4 項目・停止 0 回) | 100% | `verify_player_sheet` の着手前からの赤 4 本 `{2c,2d,8a,8f}` を直す。真因 = #45 `475839d` が `world.html` へ `js/skill-check.js` を載せ、world でも技能/習熟が出るようになったのに期待表が「伏せる」のまま。⭐ **緩めるのではない** — 照合マスは 24→36 に増える。⭐ 期待を「載っている script」から導出し、集合 {index,tavern,world} は新 assert (0e) で固定(⚠⚠⚠ 導出だけだと `world.html` から script を剥がす退行に追随して**永久緑**になる = 変異 `noscworld` で機械証明)。⚠ `--negative` は素の赤のせいで 91/101 に落ちていた(偽の赤 6 本)。⚠⚠ base ポート **9470→9620**(変異 17 本で 9621〜9637。⛔ base でなく**占有幅**で空きを見る — 9470 は既に `driver_party_view_reopen` の 9480 と重なっていた)。本番コード 0 バイト・changelog 不要・撤退スイッチ無し。⚠⚠⚠ **依頼書の主張が 3 件崩れた** = ① **§2-5 / §8 / §6-1 の予測「`noscworld` では (2c)(2d)(8f) は緑のまま」は外れた** — 実測の赤は **(0e)(2c)(2d)(8f) の 4 本**で、**緑のままだったのは (8a) だけ**。導出ヘルパは予測どおり完全に追随して緑になったが、**依頼書 §5 自身が要求した固定値の母集団ガード**((2c) `hiddenTotal===20` / (2d) `nSC===3` / (8f) `withSC===3`)が導出に追随しないので**その 1 行だけで**赤くなる = **依頼書が §5 と §2-5 で自己矛盾していた**(対処 = §6-1 推奨(i)の `targets:['0e']` / `allowRed:['2c','2d','8f']`)。⭐ **罠の本体は依然として真** — 固定値ガードを持たない **(8a) は実測で緑のまま**なので、(0e) が無ければこの退行は一生捕まらない ② **§6-2 のサンプル `p.file === 'tavern.html' ? [] : SKILLC` は誤り**(index.html が伏せる側に入り `hiddenTotal` が 22 でなく 24 になる)→ `SC_PRE45 = ['index.html','tavern.html']` で判定して文書どおり 22 ③ **§4-1 の「`probeRealPage` の中で `p.hasSkillCheckSrc` を載せる」は実物と食い違った**(`probeRealPage` は `mutKey` を受け取らないシグネチャ)→ `mutKey` を持つ `measureAll` 側で載せた。⭐ 設計上の気づき = **(2c)(2d)(8f) の固定値ガードは (0e) と役割が重複している**(将来「集合の固定は (0e) 1 本へ寄せる」判断もありうる。本チケットでは変えない)。**締め = 素 73/73 / `--negative` 111/111(変異 17 本)/ 本番コード 0 バイト / commit `26a3f24`, `777cd34`, `5d00fc5`, `d9fd5d2`** |
| 49 | [2026-09-04_roll-target-readout.md](2026-09-04_roll-target-readout.md) | **完了 `bbcb091`+`066e5fb`+`3a2f28a`+ 項目4(本行を書いたコミット = 件名 `#49 項目4 — 締め`)**(新規 driver `verify_roll_target` 素 **30/30 PASSED FAILED 0 PENDING 0** / `--negative` **53/53 PASSED FAILED 0 PENDING 0・変異 16 本とも空振り 0**(PENDING 0 は (n9a) が機械確認)/ 既存 golden **3 本とも実装前の基準どおり・期待値の変更 0 件** = `driver_action_priority` **92/92 PENDING 0**・`driver_cast_circle` **56/56**・`verify_enemy_name_label` **30/30**(⭐ 3 本とも単独実行・単発の赤による再実行 0 回)。`index.html` **+75/-3** で**呼び口 204 箇所は無改修**(DOM 生成点 3 関数だけ差し替え)。dev-loop 4 項目・停止 0 回) | 100% | ロールの吹き出しに判定行 `出目 8+ → 成功 +4` を 1 行足す。既存 3 行は 1 文字も変えず**末尾に append するだけ**なので、恒等 assert が「入力が出力の前方一致プレフィックス」という強い形で書ける。⭐ **呼び口 204 箇所は無改修** — DOM を作るのは `showRollAtPlayer`/`showRollAtEnemy`/`showRollAtAlly` の**生成点 3 関数だけ**なので、そこで純関数 `rollTargetLine(html, type)` を 1 本挟めば攻撃・セーヴ・カウンター・敵の攻撃に同時に効く(#37 の年代記フックと同じ形)。⚠⚠⚠ **勝敗は `type` 引数から引く。算術(合計 ≥ 目標)から引くと `CRIT!` の吹き出しに「失敗 -3」と出る**(ナチュラル20 の `isThreat` と無抵抗の `helpless` は合計が AC に届かなくても命中になる)→ 変異 `verdictbymath`。⚠⚠⚠ **上へ逃がすのは `margin-top`。`transform` は効かない**(`.rollPop` の `@keyframes rollRise` が全キーフレームで `transform` を書いており CSS アニメーションが勝つ。#46 と同型)→ 変異 `xformlift`。⚠⚠ **必要出目の下限は vs AC=2 / vs DC=1 で規則が違う**(攻撃はナット1 が自動ミス、セーヴにその特例は無い)→ 変異 `samefloor1`/`samefloor2` で「両者が同じ値になったら赤」。⚠⚠ **判定行の HTML は 4 形態**(`.big` の有無 / `<br>` の無い形が 1 本 / セーヴは `saveModDD` が `+3装+2` の 2 項)→ 修正項は読まず `raw = 目標 − 合計 + 出目` で出す。⭐ **検証は実プレイに頼らない** — 純関数を `window.__rollTargetFmt` に露出し合成入力を直接食わせる決定論方式(実プレイは母集団ガードのみ)。ラベル 80 語(`HIT`/`MISS`/…)は**英語のまま触らない**。新 driver `tools/verify_roll_target.js`(port **9880** / 変異 9881-9896 / 予備 9897 / 撤退アーム 9898)・負のコントロール 16 本・撤退 `?rolltarget=0`・changelog 必須。会議 = `dev-meetings/2026-09-04_roll-target-readout.md`。<br>⚠⚠⚠ **依頼書の指定が実物でずれた点が 6 件**(全文は §12-6)= ① **`onlyplayer` の「`edits[]` で 2 箇所」は実物 3 箇所とも同一文字列**で一意にならず exit 3 → `transform` 方式へ(⭐ 変異アンカーの箇所数は目視でなく「置換して一意になるか」で決まる)② **`looseanchor` は `mTot` を条件から外すだけでは (3b)(3c) が緑のまま** → AND を **2 本**外す必要があった(`FUMBLE!` は `=` も `vs` も持たない / `INITIATIVE` は `vs` を持たない)③ **`skillmangle` は白名簿が `"skill"` を弾くため原理的に (4c) を赤にできない** → 4 点同時変異 + **(4c) を 2 経路照合へ強化**(⛔ 期待値は緩めず強めた)④ **(5c) の「`offsetWidth` 200px 未満」だけでは `sidebyside` を検出できない**(素 115〜126px / inline 化しても 160〜168px = **変異の前後とも閾値の同じ側**)→ **AND を 1 本追加**(⭐ 閾値 1 本だけの assert は空振りしうる = 必ず変異を当ててから確定する)⑤ 項目1 の観測メモの `INITIATIVE` の実形 `1d20(<b>8</b>)` は誤りで、**本番テンプレは `1d20(${n})` = `<b>` を付けない** ⑥ ⭐⭐⭐ **装置の観測打ち切りを「`.rollPop` 総数 20 枚」にすると (0d) が「測れない」赤になる** — 交戦の頭でイニシアチブが人数ぶん一斉に出るため**観測 24 枚が全部 init** で窓が閉じ、注釈対象が 1 枚も入らない → 打ち切りは **§4-5 の AND を満たす吹き出しの数(8 枚)**へ。⭐ 教訓 = **母集団の打ち切りは「全体の枚数」でなく「測りたい母集団の枚数」で切る**。**締め = 素 30/30 / `--negative` 53/53(変異 16 本)/ 既存 golden 3 本 非退行・期待値の書き換え 0 件 / `index.html` +75-3・呼び口 204 箇所 無改修。残 = §9 の実機体感 6 項目(筆頭 = iPhone 縦持ちで 4 行が 1.3 秒で読めるか)** |
| 50 | [2026-09-04_cone-spell-cast-rate.md](2026-09-04_cone-spell-cast-rate.md) | **完了 `f1917cf`+`004b5e3`+`4c69862`+ 項目4(本行を書いたコミット = 件名 `#50 項目4 — 締め`)**(新規 driver `verify_cone_cast` 素 **19/19 PASSED FAILED 0 PENDING 0** / `--negative` **41/41 PASSED FAILED 0 PENDING 0・変異 14 本とも空振り 0**(PENDING 0 は (n9a) が機械確認)/ 既存 golden **5 本とも基準どおり・期待値の変更 0 件** = `driver_action_priority` **92/92 PENDING 0**・`driver_field_step7` **79/79 PASS**・`driver_cast_circle` **56/56**・`verify_roll_target` **30/30**(`--negative` **53/53**)・`verify_enemy_name_label` **30/30**(⭐ 5 本とも単独実行・単発の赤による再実行 0 回)。`index.html` **+114/-65** で `mageAI` の梯子・`RANGE` テーブル・`tavern.html` の説明文は無改修。dev-loop 4 項目・停止 0 回) | 100% | バーニングハンズ/コーンオブコールドの発射率を 5.6% → 実用域へ。⭐ 実測 = 54 手番中 51 回が**無言で通常攻撃へ降格**、呪文スロットは 1 発も減らない。⚠⚠⚠ 真因は「円錐 9 マスに味方が 1 人でも居たら方向ごと破棄」する拒否権だが、**下流の被害者列挙は `enemiesInArea` = 敵のみ**で味方は元々 1 ダメージも受けない(酒場の説明文も『PT 巻き込みなし』)= **起きえない誤射を防ぐために発射率を捨てていた**。⭐ 反実仮想の実測 = 斜めだけ足す 20.4% / 拒否権だけ外す 51.9% / **両方(清潔優先→味方入り許可)64.8%**。⚠ 残り 35% は「宣言射程 medium=8 マス vs 円錐の実効射程 3 マス」の食い違い → `allyFireball` と同じ「詰め寄る」で回収。撤退 `?conecast=0`。<br>⚠⚠⚠ **依頼書の指定が実物でずれた点が 11 件**(全文は §12-5)= ① ⭐⭐⭐ **§4 の ⚠⚠ 警告そのものが誤りだった** — 「清潔パスの `break` を消すと `cnt` がずれる」は**完全な no-op**(`rejected` は true のままなので直後の `if (rejected) continue;` で方向ごと捨てられ、途中まで足した `cnt` はどちらにせよ使われない。味方入りパスは元から全タイル合計)= **`break` は純粋な最適化で消しても 1 ビットも変わらない**。変異 `nobreak` は「短絡**も棄却も**落として その方向を採る」形へ作り替え(撤退アームで旧探索の鏡と 142 件食い違い (1a) が赤) ② ⭐⭐ **空撃ちのガードは 2 本ある** — `if (cnt <= 0) continue;` を消しても採用条件 `cnt > bestCount` の初期値 0 で `0 > 0` が偽 = 無害。`zerofoe` は `best` への昇格まで書く形へ作り替え(敵 0 体の標本 109 件で (2c) が赤) ③ ⭐⭐⭐ **`only4dir` は担当節 (2a) では原理的に捕まらない** — 素(8 方向・2 段)88.3% / 4 方向 76.7% / 撤退 23.8% で **3.23 倍**は敷居 3.00 倍を割らない(**拒否権の寄与が斜めの寄与を圧倒する**。⚠ 盤面のせいではなく実プレイ値でも 9.3 倍で同じ結論)→ **assert は 1 文字も緩めず担当節を (2b) へ移した**(清潔な方向が斜めにしか無い標本 74 件で必ず赤)。⚠⚠ 副産物 = **(2a) の「3 倍以上」は `vetoback` しか捕まえられない弱い敷居**(`vetoback` 単独は 2.30 倍) ④ ⚠⚠⚠ **(3c) の「隣接 1 マスなのに円錐へ入らない盤面」は 8 方向では原理的に作れない**(円錐の被覆は chebyshev ≤ 3 を全部覆う)→ 「実効射程内 0〜3 マスでは 1 件も詰め寄らない」+「距離 0 では `allyBasicAttack` へ落ちる」へ移設(検出力は不変) ⑤ ⚠⚠⚠ **「距離 4〜8 なら円錐の外」は偽** — 斜め step3 lat=±2 が chebyshev **5** まで届く → (3a)(3b) の盤面はドライバ側の独立幾何 `coneSet` から先に作る。`reachdrift` の担当節は **(3a) 単独**(両方書くと (3c) が緑のままで落ちる) ⑥ ⭐ **合成盤面の初版は (2a) が原理的に成立しない構造だった**(旧 52.5% / 新 100% = 1.9 倍。⚠⚠ (0b) は「差 50 件以上」しか要求しないので**母集団が緑のまま本体が死ぬ**)→ 術者 1〜5 マス・前衛 2〜4 人へ修正し 23.8% → 88.3% = **3.72 倍**・差の出る標本 155 件。⭐⭐ 教訓 = **母集団ガードは「差が出ること」でなく「後続の assert の敷居を満たす差が出ること」まで縛る** ⑦ **§2-1 の「1 文字違わぬ写し」は誤り**(`allyConeOfCold` 側にコメントが 1 行多い)→ 単一文字列で両方を掴めず置換を 2 回に分割。装置側に `replaceInFn(body, 関数の見出し, from, to)` を新設 ⑧ **仕込みは 3 点セット** — `knownSpells` / `partySkills`(配列)に加え **`sessionStorage['dragonfighters.partyMembers']` が必須**(無いと `buildParty` がランダムに組み魔法使いが来ない run が出る)。Lv3 の呪文スロット上限は 5。⛔ `noteAoeOutcome` のキーは **`skill.name` = `"バーニングハンズ"`(日本語)** ⑨ **§2-1 の行番号 12 件はズレ 0**(HEAD が起草基準 `739a9e4` と同一。#6 の「8 件中 8 件ズレ」とは逆のケース) ⑩ ⛔ **`driver_field_step7.js` の `conePick` は旧アルゴリズムのままの鏡** — その (P2) の cone **80.5%** を本チケットの修正後の実測値として引用してはいけない(実測値の正は `verify_cone_cast` だけ) ⑪ ⚠ baseline worktree `%TEMP%\df_step7_baseline` が `index.html` を失って残ることがある → `git worktree remove --force` で作り直す。**締め = 素 19/19 / `--negative` 41/41(変異 14 本)/ 既存 golden 5 本 非退行・期待値の書き換え 0 件 / 効果 = 合成盤面の発射率 23.8% → 88.3%(3.72 倍)、実プレイ 90 秒は `attempts 8 / cast 0`(実装前)→ `attempts 7 / cast 6`(実装後)/ `attempts 8 / cast 5`(締め)、(4b) で バーニングハンズ 87.5% = コーンオブコールド 87.5% = 探索が 1 本に寄った機械的証明。残 = §9 の実機体感 5 項目(筆頭 = ⭐⭐ 炎が味方を包む絵面が許せるか(味方 1 人 71% / 2 人 14%)・⚠⚠ 斜めは `spawnConeFlames` / `spawnGroundFx` / `spawnFireImpactBurst` が初めて通る経路)** |
| 51 | [2026-09-04_road-caravan-ambush.md](2026-09-04_road-caravan-ambush.md) | **完了 `357e0ae`+`4dbdd25`+`74ebbbf`+`6f4d755`+ 項目4(本行を書いたコミット = 件名 `#51 項目4 — 締め`)**(新規 driver `verify_road_ambush`(base 9970 / 変異 9971〜9990)素 **28/28 PASSED FAILED 0 PENDING 0** / `--negative` **65/65 PASSED FAILED 0 PENDING 0・変異 20 本とも空振り 0**(PENDING 0 は (n9a)(n9b) が機械確認)/ 既存 golden **14 本すべて基準どおり・期待値の変更 0 件・逐次実行で再実行 0 回** = `verify_road_events` 25/25・`verify_road_boon` 20/20・`verify_world_steps` 33/33・`verify_world_map` 57/57・`verify_quest_walk` 25/25・`verify_world_heromark` 18/18・`verify_town_exit` 素 23/23・`verify_title_screen` 86/86・`verify_ability_scores` 24/24・`verify_darkvision` 25/25・`verify_mercenary_roster` 44/44・**`verify_player_sheet` 73/73 FAILED 0**(⭐ 依頼書の「FAILED 4 本が着手前から赤」は #48 着地で**解消済み**)・`verify_recruit_size` 82/82・`verify_run_chronicle` 73/73。⛔ **母集団は数え直した = 依頼書の 13 本は古く実測 15 本、うち 1 本は自分自身 ⇒ golden 14 本**。dev-loop 4 項目・停止 0 回) | 100% | 街道の襲撃 — ワールドマップ横断中にまれに「隊商が魔物に襲われている現場」へ出くわし、**助けに入る / 見捨てて回り込む**を選べる。⭐⭐⭐ **戦闘は作らない** — 7.9-3「隊商護衛」(馬車 `caravanWagon` / 街道テクスチャ / ウェーブ状態機械 / 敗北条件)が**丸ごと出荷済み**で、新規は「横断中に割り込んでくる導線」だけ。ユーザー決定 = **1 波の短期決戦** / 判定は「戦うか」ではなく「**どう戦うか**」(成功=奇襲 = #47 の `applyRoadVigilance` 流用 / 失敗=乱戦。⛔ 失敗で戦闘が消える設計にしない)/ **勝ち = 傷を持ち越して先へ進む・負け = 無傷だが港町まで戻される** / 報酬 = XP(配線ゼロ)+ 金貨(`clearGold` の素通し 1 本追加・**馬車が全損したら 0**)。<br>⚠⚠⚠ **起草中に実測で見つけた設計の罠が 6 件**(全部 `--negative` の変異 20 本へ翻訳済み)= ① **`EVENTS` に 7 件目を足すと golden が非決定的に赤くなる** — `verify_road_events` の `measureChoice` が固定種で 1 件発火させ (3f)(`:1449`)が **`filter(c => c.check)[0]` を押す**ので、`pickEvent` の引きが動くと押される先が襲撃に変わり index.html へ遷移して落ちる(⭐ 件数は直書きされていない(`:469`)= **赤くなる理由は件数ではなく引きのズレ**)→ 別表 `AMBUSH` + 別振り ② **`rnd()` を共有すると決定論が全滅** — `rndState` は単一の可変状態で 1 回引くだけで以後の全ての引きが 1 つずれる → 専用ストリーム `ambRnd()`(変異 `sharedrng`)③ **「判定なしの枝」が戦闘へ繋がると既存 golden 3 本が落ちる** — `verify_world_steps:774`/`verify_world_map:683`/`verify_quest_walk:831` が `filter(x => !x.check)[0]` を機械的に押す → **見捨てる側が判定なし**(⭐ D&D の作法と番人の要求が完全に一致)④ **world.html に 2 本目の `sessionStorage.removeItem` を書けない** — (2c) が**配信バイト**を文字列検索して `rm===1` を要求 → 消費は**空文字上書き** ⑤ **素直に作ると港町フランへワープする** — world.html は主人公位置を保存せず `spawnFor` が未知 scenarioId に `"phlan"` を返す → `world.html:740` の**呼び口**に 1 段(⛔ `js/world-map.js` は sha1 固定)⑥ **`currentScenario` を上書きすると本命クエストが襲撃に化ける**(誰も元に戻さない)→ 専用キー `dragonfighters.roadBattle`(#47 `roadBoon` と同型 = world が書き index が消す)。<br>⭐ #50 §12-6 の教訓を先取りし、§0 に **(0e)「§1 の 3 経路と §2 の勝敗 2 経路を全部踏める種が存在する」** を新設(母集団ガードは「差が出ること」でなく「**後続 assert の敷居を満たす差が出ること**」まで縛る)。新 driver `tools/verify_road_ambush.js`(base **9970**)・撤退 `?ambush=0`(world / index で独立)・changelog 必須。会議 = `dev-meetings/2026-09-04_road-caravan-ambush.md`。街道の卓上マップは **#52(codex 発注)**で別枠(発注文 = `codex1/requests/2026-09-04_road-ambush-map.md`)。<br>⚠⚠⚠ **依頼書の主張が実測で崩れた点が 12 件**(全文は §12-1)= ① ⭐⭐⭐ **§4 の「(0b) は写経していないので緑」は誤りで赤が正しい** — 検索対象が 0 本のとき「1 つも出てこない」は**自明に真**になり母集団が立たない → #48 の則で assert を緩めず予測のほうを訂正(母集団ガード `MIN_STRINGS = 4`)② **§6-1 の挿入位置が誤り** — `roadBattle` の消費は「シナリオ解決の手前(`:10170`)」では**遅すぎる**。`_scenIdForTex`(テーマ)と `_builtinScenId`(分岐グラフ)が両方この値を要るので**テクスチャ解決の直前(`:3383`)へ上げた**(下ろすと襲撃に**本命クエストの地形と分岐グラフが載る**)③ ⭐⭐⭐ **§6-5 の前提が崩れた** — 「敗北時に `roadReturn` を `removeItem` すれば `spawnFor` のフォールバックで港町へ戻る」は**成立しない**。`currentScenario` は本命クエスト id のままなので `SITES` に当たり、実測 `spawnFor("dungeon","lizard-swamp")` → **`"swamp"`** = **負けたのに目的地の前へワープ** → 消すのではなく `roadReturn` へ **`"phlan"` を書く**形へ訂正 ④ **§2-12 の「`verify_player_sheet` は FAILED 4 本が着手前から赤」は解消済み**(#48 着地。基準は FAILED 0 へ)⑤ **golden 母集団 13 → 実測 15 本(自分自身を除いて 14 本)** ⑥ ⛔ **`__world` に窓を 1 つも足せない**(`verify_road_events` (4b) が「既存 25 + `roadEvent` = ちょうど 26 個」を**集合で固定**。足して赤にして戻した)⑦ ⛔ **`js/road-events.js` に `return ((t ^ (t >>> 14)) >>> 0) / 4294967296;` を 2 本目として書けない**(`verify_road_events` の変異 `seedignore` の逐語アンカーが 2 ヒットして**起動時検算 exit 3**。0 本にしても exit 3)→ `ambRnd` の最終行を 2 行に割った ⑧ **`showResult` も `paint()` を通るので結末画面でも `armAt` がリセットされる**(踏んだ症状 = 「街道の出来事を畳めなかった」で歩行が 2 タップ目で停止)⑨ **盤面を `AMBUSH_FIELD` へ束ねた**(隣窓 #52 の依頼。⚠ `df-mapdef.js resolve()` 規則④で `themeId:"caravan-road"` にできるのは**カスタム幾何を持たない今だけ**)⑩ **変異 `woundtoolate` は依頼書どおりでは 1 行置換にならず空振り** → assert を 1 文字も緩めず**欠陥そのもの(適用した hp が後から `hp = maxHp` で上書きされる)の再現**へ書き直した ⑪ **変異 `nospawnresume` の担当節は (3a) だけでなく (3a)(3c)**(③の訂正で敗北の帰還も同じ resume 枝を通るようになった。**実走で確定**)⑫ **`gameoveramb` / `gameovernever` は同じアンカーを共有する**(素のソースから独立に組むので一意性検算は各 1 件で通る)。<br>⚠ **残課題(未対処 / §12-4)**= 勝利すると `lastResult.scenarioId="road-ambush"` が `tavern.html:5058` の `!== "generated-quest"` ガードを**通り** `progress.cleared` に載る。読み手は `has("dragon-lair")` と `has(sc.unlockAfter)` だけなので**解放は 1 つも動かない**が、ガードの**外**にある `plazaStateTV.totalQuestsCleared`(`:5053`)は **+1 される**(闇市「怪しい扉」の常設化カウンタ)。⛔ `tavern.html` は本チケットの禁止ファイルなので手を付けていない。/ 撤退ボタンで襲撃から抜けると `roadWounds` は書かれず `roadReturn` も襲撃地点のまま = **無傷で襲撃地点へ戻る**(既存の「撤退はペナルティなし」設計どおりだが実機体感の確認項目)。残 = §9 の実機体感 7 項目 + 実装で増えた 2 件(§12-5)。 |
| 52 | [2026-09-04_road-ambush-map.md](2026-09-04_road-ambush-map.md) | **承認済**(2026-09-04) / **着手は #51 着地待ち** | 0% | codex 納品の街道マップ(**29x19 マス** / `codex1/maps/road-ambush-v1.png` / md5 `9c5435d4…`)を DF の格子へ焼き、#51「街道の襲撃」の戦場を専用の 1 枚絵にする。⭐ 素材は納品済みで `--fit` 実測も完了 = `phase (42.05, 35.45)` / `period (50.525, 49.635)` / `cells (29, 19)` / `tile 48`(48/50.525 = **0.950x の縮小** = 水増しゼロ / 異方性 **1.78%** は盗賊アジト 2.03% より良い)。<br>⚠⚠⚠ **最大の罠 = 屋外テーマの上に 1 枚絵は原理的に載らない。** `js/df-mapdef.js` の `resolve()` は規則④で「**屋外テーマ(caravan-road)のカスタム幾何 → 既定値 + console.warn / isCustom=false**」と明示的に禁じている。理由もコードが書いており、**帯マスク・空/丘の地平線レンダラ・視界制限オフは `flags.bandMask` ではなく themeId から引かれる**ため、mapDef 側で bandMask を切っても**地平線レンダラだけが残ってカスタム幾何の上に空と丘が描かれる**(`index.html:3640` `FIELD_THEMES` / `:3765-3766` `fieldSkyRow`/`fieldApronRow` は `FIELD_MODE && FIELD_GEO_ACTIVE` を見る / `:4646` 帯マスクだけが `flags.bandMask`)。⇒ **themeId を非屋外テーマへ移す**(推奨 = `bandits-forest` 配下 = `n7big` と完全に同じ経路)。⛔ **`FIELD_THEMES` から `caravan-road` を外す回避は不可 — 7.9-3「隊商護衛」が全損する**(変異 `fieldtheme` / `fieldset` の 2 本で両側から挟む)。<br>⚠ **罠 B = 座標が全部動く** — #51 の `themeId` / `wagonSpawns [{tx:9,ty:14}]` / `spawns` / `start (6,13)` はどれも**歩行帯 row13-15 前提**。⇒ #51 の実装窓へ「盤面の値を 1 つの名前つき定数 `AMBUSH_FIELD` に束ねてほしい」と依頼済みで、#52 は**そのオブジェクト 1 つの差し替え**で済むはず(着手時に実在を確認する)。⚠ `paintingAspectFits` は縦横比の**完全一致**を要求するので `mapDef.rooms[0].rect` と `tileBounds` を同値にする。⭐ `gates` は**書かない**(`waves` の防衛戦なので出口へ歩く必要がない)。⛔ 焼き出力は `assets/<out>.**jpg**`(PNG ではない)。検算 3 指標(ドリフト 4.0 / 位相 2.0 / score 70%)は**緩めない**。撤退 = `?ambush=0` と `?mapdef=0`。changelog 必須。 |

### 進行度の見方 / 完了ぶんの置き場

- **決着したものは [`完了/`](完了/) へ移設する**(2026-08-22 に 7 枚を移動)。
  一覧の行は残す — 行を消すと「なぜそう決めたか」への導線が切れるため。**リンク先だけが `完了/` を指す**。
  ⚠ 移設した .md 内の相対リンクは `../` → `../../` へ直してある。新しく移すときも忘れずに直すこと。
- **進行度の出所**は依頼書ごとに違う。勘で書かない:
  - 完了/見送り = **100%**(コミットハッシュが出所)
  - 実装中 = **dev-loop の `DEV_QUEUE.md` の消化数**。⚠ 数字はすぐ腐るので
    **生の進行は `~/.claude/dev-loop/DEV_QUEUE.md` が唯一の正**。
    (実例: #6 は 2026-08-22 22:03 時点で 4 項目中 3 項目 = 75% と書いていたが、その数十分後に完走した。**% はすぐ腐る**)
  - 未承認 = **0%**。ただし #11 のように「起草時の実測」が済んでいるものはある(実測 ≠ 実装)

⚠ **1 → 2 の順は守ること**。2 は 1 の STEP3 で作る `unitBodyTiles` の比率を出所として使う(判定の出所を 2 つ持たないため)。
⚠ 3 は 1 が終わってから。1 が外周を、3 が内側を担当する分担になっている。
⭐ **4 は 1〜3 と独立**(触る場所が `audio.js` と BGM の表だけ)。1 の待ちに入ったら先に 4 をやってよい。
  → 2026-08-21 に 1 → 4 → 2、2026-08-22 に 9 → 10 → 3 の順で消化した。**1〜4 と 9・10 は決着済み**。
  → 2026-08-22 に **5 も完了** (`dde8457`)。同日 **6 title-screen も完了** (`9f8b1bd`)。
  → **次は 7 / 8 / 11。3 枚ともまだ未承認**なので、着手前に 1 枚ずつ内容を確認すること。
⭐ **11 は 5〜8 と完全に独立**(触る場所がマップ/絵まわりだけで、開始画面とも `DFSlots` とも重ならない)。
  ただし **11 は敵を 5 体 → 9〜10 体に増やす**ので、**難易度の実測は 8 へ合流させる**
  (8 に「廃坑」だけでなく「シナリオ2 の n7 大部屋化ぶん」も宿題として乗る)。

✅ **#6 → #12 の依存は解消済み**。#12 は `title.html:518` / `:540` の遷移先を `town.html` へ
  書き換えるので #6 の完了を待っていたが、**#6 は 2026-08-22 に完了(`9f8b1bd`)**したので着手できる。
⭐ **#12 は #7 / #8 / #11 と独立**(触る場所が新規ページと絵まわりで、募集人数ともシナリオ2 のマップとも重ならない)。

⭐ **#13 は #12 と独立**(触るのが `runEncounter` の戦闘ループだけで、街ページとも募集人数とも重ならない)。
⛔ ただし **#13 → #8 の順は守る**。#13 は「1 部屋 = 1 戦闘」へ寄せるので、エンカ終了ごとの回復 3 種(HP+33% / スロット 25% / セカンドウィンド)の**貰える回数が減る**。**難易度は必ず上がる方向**なので、数値の再調整は #8 でペア比較してから。

### 5〜8(開始画面まわり)の順序

会議記録: [dev-meetings/2026-08-20_ゲーム開始画面.md](../dev-meetings/2026-08-20_ゲーム開始画面.md)

- ⚠ **5 → 6 は必須**。6 は 5 の `DFSlots` API を使う。**5 は UI を作らない**(土台だけを先に固める作法)
- ⚠ **7 → 8 は必須**。7 は `recruit` の器だけ作り、**値は 8 で実測してから入れる**
- ⭐ **(5,6) と (7,8) は互いに独立**。並行して進めてよい。触る場所が重ならない
- ⛔ **8 を飛ばして出荷しない**。7 だけ入れると ★1 の廃坑が NPC 1 人になり、**最初のクエストが最も過酷**になる

### 5〜8 から派生した未起草チケット(必要になったら書く)

- タイトル専用 BGM(`audio.js` に `title` シーンを追加 + 登録不要サイトから mp3 調達)
- タイトルのロゴ画像(codex1 への別依頼。Phase 1 は文字組みで通す)
- ~~パーティ 5 人以上の可否調査~~ → **#14 として起草済み**(2026-08-23)。⚠ 起案理由だった「味方スポーン枠・隊列・UI 枠が 4 人前提」は**実測で 3 点とも成立しないと判明**した。残る論点はカメラのみ

## 使い方

1. こちらの窓で計画を 1 件 = 1 ファイルとして起草する
2. ユーザーが内容を確認・承認する
3. 実装窓に「`実装依頼書/<ファイル名>` を読んで実装して」と渡す
4. 実装が終わったら見出しのステータスを `完了 <コミットハッシュ>` に更新する

## 命名規則

`YYYY-MM-DD_<slug>.md`(例: `2026-08-20_mine-detour-balance.md`)
slug は英小文字ケバブケース。1 ファイル = 1 まとまりの実装単位。

## 依頼書の書き方(最低限これだけは書く)

- **ステータス**: 起草中 / 承認済 / 実装中 / 完了 `<hash>`
- **目的**: 何を実現したいか(手段ではなく目的で書く)
- **背景・現状**: 今どうなっているか。関連ファイルは `index.html:1234` 形式で指す
- **変更範囲**: 触るファイルと、触らないと決めたファイル
- **受入条件**: 実装窓が自分で「終わった」と判定できる条件。可能ならドライバ名まで書く
- **撤退スイッチ**: `?feature=0` 形式の URL パラメータ名(本プロジェクトの慣習)
- **やらないこと**: スコープ外を明示して、実装窓の暴走を防ぐ

## 関連

- 設計書(長期の設計メモ): `~/.claude/plans/`
- Codex へのスプライト依頼文: `C:\Users\PC_User\Desktop\codex1\requests\`
  (こちらはアセット工房への依頼。本フォルダは**本体コードの実装依頼**)
